---
phase: 04-estoque-e-reserva-de-fardos
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - app/api/upload/route.ts
  - app/api/reservas/route.ts
  - src/features/upload/types.ts
  - src/features/upload/hooks/use-upload.ts
  - src/features/upload/components/import-preview.tsx
autonomous: false
requirements: [STOK-01, STOK-02, STOK-03, STOK-04, STOK-05, STOK-06, STOK-07]

must_haves:
  truths:
    - "Apos upload, sistema le estoque e reserva fardos automaticamente (D-01)"
    - "Card de resumo pos-upload exibe secao de estoque com SKUs fardo/prateleira/reservados (D-14)"
    - "SKUs com cobertura parcial aparecem destacados no resumo (D-15)"
    - "Se estoque indisponivel, secao mostra aviso orientando usar re-reserva (D-16)"
    - "Endpoint POST /api/reservas permite re-reserva manual complementar (D-02)"
    - "Migration aplicada ao banco de dados remoto"
  artifacts:
    - path: "app/api/upload/route.ts"
      provides: "Upload route estendido com chamada de reserva automatica"
      contains: "executeReservation"
    - path: "app/api/reservas/route.ts"
      provides: "Endpoint de re-reserva manual"
      exports: ["POST"]
    - path: "src/features/upload/components/import-preview.tsx"
      provides: "Secao de estoque no card de resumo"
      contains: "estoque"
  key_links:
    - from: "app/api/upload/route.ts"
      to: "src/features/fardos/utils/reservation-engine.ts"
      via: "import executeReservation"
      pattern: "import.*executeReservation.*from.*reservation-engine"
    - from: "app/api/reservas/route.ts"
      to: "src/features/fardos/utils/reservation-engine.ts"
      via: "import executeReservation"
      pattern: "import.*executeReservation.*from.*reservation-engine"
    - from: "src/features/upload/hooks/use-upload.ts"
      to: "app/api/upload"
      via: "fetch POST reads estoque field"
      pattern: "data\\.estoque"
---

<objective>
Integrar reserva automatica no fluxo de upload, criar endpoint de re-reserva manual, aplicar migration ao banco, e exibir feedback de estoque no card de resumo.

Purpose: Este plano conecta toda a logica de reserva (Plans 01 e 02) ao fluxo real do usuario. Apos este plano, o lider faz upload e ve imediatamente quantos SKUs tem fardo, quantos vao para prateleira, e quais tem cobertura parcial. O botao de re-reserva permite atualizar quando estoque muda.

Output: Upload route estendido, endpoint de re-reserva, UI de feedback, migration aplicada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-estoque-e-reserva-de-fardos/04-CONTEXT.md
@.planning/phases/04-estoque-e-reserva-de-fardos/04-RESEARCH.md
@.planning/phases/04-estoque-e-reserva-de-fardos/04-01-SUMMARY.md
@.planning/phases/04-estoque-e-reserva-de-fardos/04-02-SUMMARY.md

<interfaces>
<!-- Reservation engine criado no Plan 02 -->
From src/features/fardos/utils/reservation-engine.ts:
```typescript
export async function executeReservation(
  importacao_numero: number,
  forceRefresh?: boolean
): Promise<ReservationResult>
```

From src/features/fardos/types.ts:
```typescript
export interface ReservationResult {
  skus_fardo: number
  skus_prateleira: number
  fardos_reservados: number
  parciais: string[]
  indisponivel: boolean
}

export interface StockSummary {
  skus_fardo: number
  skus_prateleira: number
  fardos_reservados: number
  parciais: string[]
  indisponivel: boolean
}
```

<!-- Upload route handler atual -->
From app/api/upload/route.ts:
```typescript
// Retorno atual (linha 194-206):
return Response.json({
  success: true,
  dayReset,
  importacao_numero,
  summary: {
    total_validos: newGroups.length,
    filtered_status: body.filtered_status ?? 0,
    filtered_envio: body.filtered_envio ?? 0,
    duplicados,
    por_tipo: porTipo,
    por_grupo: porGrupo,
  },
})
```

<!-- Tipos de upload existentes -->
From src/features/upload/types.ts:
```typescript
export interface ImportSummary {
  total_validos: number
  filtered_status: number
  filtered_envio: number
  duplicados: number
  por_tipo: Record<TipoPedido, number>
  por_grupo: Record<string, number>
}
```

<!-- Hook de upload existente -->
From src/features/upload/hooks/use-upload.ts:
```typescript
interface UploadApiResponse {
  success: boolean
  dayReset: boolean
  importacao_numero: number
  summary: ImportSummary
  error?: string
}
```

<!-- ImportPreview props -->
From src/features/upload/components/import-preview.tsx:
```typescript
interface ImportPreviewProps {
  parseResult: ParseResult
  summary: ImportSummary | null
  importacao_numero: number | null
  step: string
  onConfirm: () => void
  onReset: () => void
}
```

<!-- Auth guard pattern (Phase 2/3) -->
From app/api/upload/route.ts:
```typescript
const authSupabase = await createAuthClient()
const { data: { user }, error: authError } = await authSupabase.auth.getUser()
if (authError || !user) return Response.json({ error: 'Nao autorizado' }, { status: 401 })
const { data: userData } = await authSupabase.from('users').select('role').eq('id', user.id).single()
if (!userData || !['admin', 'lider'].includes(userData.role)) return Response.json({ error: 'Sem permissao' }, { status: 403 })
```

<!-- Supabase admin client -->
From src/lib/supabase/admin.ts:
```typescript
export const supabaseAdmin: SupabaseClient<Database>
```

<!-- Stock cache invalidation -->
From src/features/fardos/utils/stock-cache.ts:
```typescript
export function invalidateCache(key: string): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema push e endpoint de re-reserva</name>
  <files>app/api/reservas/route.ts</files>
  <read_first>
    - supabase/migrations/00003_alter_reservas_schema.sql
    - app/api/upload/route.ts
    - src/features/fardos/utils/reservation-engine.ts
    - src/lib/supabase/server.ts
    - src/lib/supabase/admin.ts
  </read_first>
  <action>
1. **[BLOCKING] Aplicar migration ao banco remoto:**
   Executar `supabase db push` para aplicar a migration 00003 que altera a tabela reservas.
   - Se exigir autenticacao interativa, usar `SUPABASE_ACCESS_TOKEN` env var
   - Se falhar, marcar como checkpoint para intervencao manual
   - Verificar que a migration foi aplicada: consultar schema da tabela reservas

2. Criar `app/api/reservas/route.ts` com POST handler para re-reserva manual (D-02):
   ```typescript
   import { NextRequest } from 'next/server'
   import { createClient as createAuthClient } from '@/lib/supabase/server'
   import { executeReservation } from '@/features/fardos/utils/reservation-engine'
   import { supabaseAdmin } from '@/lib/supabase/admin'

   export async function POST(request: NextRequest) {
     // 1. Auth guard (mesmo padrao do upload route)
     const authSupabase = await createAuthClient()
     const { data: { user }, error: authError } = await authSupabase.auth.getUser()
     if (authError || !user) {
       return Response.json({ error: 'Nao autorizado' }, { status: 401 })
     }
     const { data: userData } = await authSupabase
       .from('users').select('role').eq('id', user.id).single()
     if (!userData || !['admin', 'lider'].includes(userData.role)) {
       return Response.json({ error: 'Sem permissao' }, { status: 403 })
     }

     // 2. Buscar ultimo importacao_numero do config
     const { data: numConfig } = await supabaseAdmin
       .from('config').select('valor').eq('chave', 'ultimo_importacao_numero').single()
     
     if (!numConfig || Number(numConfig.valor) === 0) {
       return Response.json({ error: 'Nenhuma importacao ativa' }, { status: 400 })
     }

     const importacao_numero = Number(numConfig.valor)

     // 3. Executar re-reserva com forceRefresh=true (invalida cache -- D-02, D-05)
     try {
       const result = await executeReservation(importacao_numero, true)
       return Response.json({ success: true, ...result })
     } catch (error) {
       console.error('[reservas] Erro na re-reserva:', error)
       return Response.json(
         { error: 'Erro ao atualizar reservas' },
         { status: 500 }
       )
     }
   }
   ```

   NOTA: A re-reserva e "complementar" (D-02) porque `executeReservation` ja consulta reservas existentes no banco e exclui fardos ja reservados. SKUs que ja tem cobertura total nao sao re-processados (o subset sum recebe demanda - ja reservado como input efetivo).
  </action>
  <verify>
    <automated>grep -c "executeReservation" app/api/reservas/route.ts && grep -c "forceRefresh" app/api/reservas/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - supabase db push executed successfully (migration 00003 applied)
    - app/api/reservas/route.ts contains "export async function POST"
    - app/api/reservas/route.ts contains "executeReservation(importacao_numero, true)"
    - app/api/reservas/route.ts contains "auth.getUser()"
    - app/api/reservas/route.ts contains "['admin', 'lider']"
    - app/api/reservas/route.ts contains "ultimo_importacao_numero"
    - app/api/reservas/route.ts contains "Nenhuma importacao ativa"
  </acceptance_criteria>
  <done>Migration aplicada ao banco. Endpoint POST /api/reservas funcional com auth guard, busca ultimo importacao_numero, e chama re-reserva com cache invalidado.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar reserva no upload route e feedback no card de resumo</name>
  <files>app/api/upload/route.ts, src/features/upload/types.ts, src/features/upload/hooks/use-upload.ts, src/features/upload/components/import-preview.tsx</files>
  <read_first>
    - app/api/upload/route.ts
    - src/features/upload/types.ts
    - src/features/upload/hooks/use-upload.ts
    - src/features/upload/components/import-preview.tsx
    - src/features/fardos/utils/reservation-engine.ts
    - src/features/fardos/types.ts
    - .planning/phases/04-estoque-e-reserva-de-fardos/04-CONTEXT.md
  </read_first>
  <action>
1. **Estender `app/api/upload/route.ts`:**
   - Adicionar import: `import { executeReservation } from '@/features/fardos/utils/reservation-engine'`
   - Apos o bloco de insert (apos linha 191 "upsert ultimo_importacao_numero"), adicionar:
     ```typescript
     // 13. Reserva automatica de fardos (D-01)
     let estoque: { skus_fardo: number; skus_prateleira: number; fardos_reservados: number; parciais: string[]; indisponivel: boolean } | undefined

     try {
       const reservaResult = await executeReservation(importacao_numero)
       estoque = {
         skus_fardo: reservaResult.skus_fardo,
         skus_prateleira: reservaResult.skus_prateleira,
         fardos_reservados: reservaResult.fardos_reservados,
         parciais: reservaResult.parciais,
         indisponivel: reservaResult.indisponivel,
       }
     } catch (error) {
       console.error('[upload] Erro na reserva automatica:', error)
       estoque = {
         skus_fardo: 0,
         skus_prateleira: 0,
         fardos_reservados: 0,
         parciais: [],
         indisponivel: true,
       }
     }
     ```
   - Modificar o Response.json final para incluir `estoque`:
     ```typescript
     return Response.json({
       success: true,
       dayReset,
       importacao_numero,
       summary: { ... }, // manter igual
       estoque,
     })
     ```

2. **Atualizar `src/features/upload/types.ts`:**
   - Adicionar ao arquivo (manter os tipos existentes):
     ```typescript
     /** Resumo de estoque retornado pelo upload (D-14, D-15, D-16) */
     export interface EstoqueSummary {
       skus_fardo: number
       skus_prateleira: number
       fardos_reservados: number
       parciais: string[]
       indisponivel: boolean
     }
     ```

3. **Atualizar `src/features/upload/hooks/use-upload.ts`:**
   - Adicionar `EstoqueSummary` ao import de types
   - Atualizar `UploadApiResponse` (linha ~11):
     ```typescript
     interface UploadApiResponse {
       success: boolean
       dayReset: boolean
       importacao_numero: number
       summary: ImportSummary
       estoque?: EstoqueSummary
       error?: string
     }
     ```
   - Adicionar estado: `const [estoque, setEstoque] = useState<EstoqueSummary | null>(null)`
   - No `handleConfirm`, apos `setSummary(data.summary)` (linha ~128), adicionar: `setEstoque(data.estoque ?? null)`
   - No toast de sucesso (linha ~146), adicionar info de estoque se disponivel:
     ```typescript
     const estoqueInfo = data.estoque && !data.estoque.indisponivel
       ? ` — ${data.estoque.fardos_reservados} fardos reservados`
       : ''
     toast.success(
       `Importacao #${data.importacao_numero} concluida — ${data.summary.total_validos} pedidos importados${estoqueInfo}`
     )
     ```
   - No `handleReset` (linha ~200), adicionar `setEstoque(null)`
   - No setTimeout de retorno ao idle (linha ~155), adicionar `setEstoque(null)`
   - Adicionar `estoque` ao retorno do hook

4. **Atualizar `src/features/upload/components/import-preview.tsx`:**
   - Adicionar `EstoqueSummary` ao import de types
   - Adicionar prop `estoque: EstoqueSummary | null` ao `ImportPreviewProps`
   - Na secao de success (apos o breakdown por grupo de envio, antes do fechamento do `<>`):
     ```tsx
     {/* Secao de estoque (D-14, D-15, D-16) */}
     {estoque && (
       <div className="space-y-2 pt-2 border-t">
         <p className="text-sm font-medium text-muted-foreground">Estoque</p>
         {estoque.indisponivel ? (
           <Alert>
             <AlertTitle>Estoque indisponivel</AlertTitle>
             <AlertDescription>
               Reserva pendente. Use &quot;Atualizar Reservas&quot; na tela de fardos quando o estoque estiver disponivel.
             </AlertDescription>
           </Alert>
         ) : (
           <>
             <div className="grid grid-cols-1 sm:grid-cols-3 gap-3">
               <div className="text-center p-3 rounded-lg bg-muted/50">
                 <p className="text-xl font-semibold">{estoque.skus_fardo}</p>
                 <p className="text-xs text-muted-foreground">SKUs com fardo</p>
               </div>
               <div className="text-center p-3 rounded-lg bg-muted/50">
                 <p className="text-xl font-semibold">{estoque.skus_prateleira}</p>
                 <p className="text-xs text-muted-foreground">SKUs prateleira</p>
               </div>
               <div className="text-center p-3 rounded-lg bg-muted/50">
                 <p className="text-xl font-semibold">{estoque.fardos_reservados}</p>
                 <p className="text-xs text-muted-foreground">Fardos reservados</p>
               </div>
             </div>
             {estoque.parciais.length > 0 && (
               <div className="flex items-center gap-2 text-sm text-amber-600">
                 <span className="text-base">⚠</span>
                 <span>{estoque.parciais.length} SKU{estoque.parciais.length > 1 ? 's' : ''} com cobertura parcial</span>
               </div>
             )}
           </>
         )}
       </div>
     )}
     ```

5. **Propagar prop `estoque` na pagina de upload:**
   - Verificar se `upload-client.tsx` ou `page.tsx` passa props para `ImportPreview`
   - Adicionar `estoque={estoque}` onde `ImportPreview` e renderizado
   - O valor vem do hook `useUpload` que agora exporta `estoque`
  </action>
  <verify>
    <automated>grep -c "executeReservation" app/api/upload/route.ts && grep -c "estoque" src/features/upload/components/import-preview.tsx && grep -c "EstoqueSummary" src/features/upload/types.ts && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/api/upload/route.ts contains "import { executeReservation }"
    - app/api/upload/route.ts contains "executeReservation(importacao_numero)"
    - app/api/upload/route.ts response contains "estoque" field
    - src/features/upload/types.ts contains "export interface EstoqueSummary"
    - src/features/upload/types.ts contains "indisponivel: boolean"
    - src/features/upload/hooks/use-upload.ts contains "estoque" in state
    - src/features/upload/hooks/use-upload.ts contains "data.estoque"
    - src/features/upload/components/import-preview.tsx contains "Estoque indisponivel"
    - src/features/upload/components/import-preview.tsx contains "SKUs com fardo"
    - src/features/upload/components/import-preview.tsx contains "cobertura parcial"
    - src/features/upload/components/import-preview.tsx contains "Atualizar Reservas"
    - npm run build exits 0
  </acceptance_criteria>
  <done>Upload route chama reserva automatica apos insert. Card de resumo exibe secao de estoque com SKUs fardo/prateleira/reservados, indicador de cobertura parcial, e aviso de estoque indisponivel. Build compila sem erros.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao humana do fluxo completo de estoque e reserva</name>
  <files>app/api/upload/route.ts, app/api/reservas/route.ts, src/features/upload/components/import-preview.tsx</files>
  <action>
    Verificacao humana do fluxo completo. O usuario deve testar upload com reserva automatica, conferir secao de estoque no card de resumo, e validar schema da tabela reservas no Supabase.
  </action>
  <what-built>
    Fluxo completo de estoque e reserva de fardos integrado ao upload:
    1. Migration aplicada ao banco (reservas sem pedido_id, com partial unique index)
    2. Algoritmo subset sum com margem de 20% e desempate por menos fardos
    3. Leitura de estoque externo via Google Sheets com retry 3x e cache 2min
    4. Reserva automatica apos upload com feedback no card de resumo
    5. Endpoint POST /api/reservas para re-reserva manual
  </what-built>
  <how-to-verify>
    1. Acessar http://localhost:3000 e fazer login como lider
    2. Ir para /upload e importar uma planilha .xlsx do ERP
    3. Confirmar importacao e verificar:
       a. Card de resumo exibe secao "Estoque" com SKUs com fardo, SKUs prateleira, fardos reservados
       b. Se houver SKUs com cobertura parcial, aparece aviso amarelo
       c. Se Google Sheets estiver indisponivel, aparece aviso "Estoque indisponivel"
    4. No Supabase (dashboard ou query), verificar tabela reservas:
       a. Colunas: id, codigo_in, sku, quantidade, endereco, status, importacao_numero, created_at (SEM pedido_id)
       b. Reservas com status='reservado' tem codigo_in unico
    5. Testar re-reserva: curl -X POST http://localhost:3000/api/reservas -H "Cookie: ..." (ou via browser dev tools)
    6. Executar npm run test -- todos os testes passam
  </how-to-verify>
  <verify>
    <automated>npm run test && npm run build</automated>
  </verify>
  <done>Usuario verificou visualmente que o fluxo de upload com reserva automatica funciona, card de resumo exibe estoque, e re-reserva responde corretamente.</done>
  <resume-signal>Digite "aprovado" ou descreva problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/upload | Request de upload com dados parseados |
| client -> /api/reservas | Request de re-reserva manual |
| /api/* -> Google Sheets | Leitura de estoque externo |
| /api/* -> Supabase | Persistencia de reservas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Spoofing | /api/reservas | mitigate | Auth guard com getUser() + role check (admin/lider) identico ao upload route |
| T-04-08 | Tampering | /api/upload response | accept | Response e read-only para o client; estoque field e informativo |
| T-04-09 | Repudiation | reservation-engine | mitigate | Reservas persistidas com importacao_numero e created_at para auditoria |
| T-04-10 | Denial of Service | /api/reservas | mitigate | Rate limiting natural: re-reserva e operacao manual do lider, nao automatizada |
</threat_model>

<verification>
- `npm run build` -- compila sem erros
- `npm run test` -- todos os testes passam (subset-sum, stock-parser, reservation-engine)
- Upload com planilha funcional exibe secao de estoque no resumo
- Tabela reservas no Supabase tem schema correto (sem pedido_id)
</verification>

<success_criteria>
- Upload automaticamente executa reserva e exibe resultado no card
- Card mostra SKUs fardo, prateleira, reservados, parciais, e aviso de indisponibilidade
- Endpoint /api/reservas permite re-reserva com cache invalidado
- Migration aplicada ao banco remoto
- Todos os testes passam, build compila
</success_criteria>

<output>
After completion, create `.planning/phases/04-estoque-e-reserva-de-fardos/04-03-SUMMARY.md`
</output>
