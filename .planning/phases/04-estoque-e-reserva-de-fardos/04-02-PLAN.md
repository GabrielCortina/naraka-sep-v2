---
phase: 04-estoque-e-reserva-de-fardos
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/features/fardos/utils/stock-cache.ts
  - src/features/fardos/utils/stock-parser.ts
  - src/features/fardos/utils/reservation-engine.ts
  - src/features/fardos/utils/__tests__/stock-parser.test.ts
  - src/features/fardos/utils/__tests__/reservation-engine.test.ts
autonomous: true
requirements: [STOK-01, STOK-02, STOK-03, STOK-04, STOK-06]

must_haves:
  truths:
    - "Sistema le planilha de estoque via Google Sheets API com retry 3x e cache de 2 minutos"
    - "Parser identifica fardos disponiveis com codigo_in, sku, quantidade e endereco"
    - "SKU com fardo disponivel no estoque externo vai para lista de fardos"
    - "SKU sem fardo disponivel vai para lista de prateleira"
    - "Cada fardo fisico (codigo_in) so pode ser reservado uma vez globalmente"
    - "Reservation engine orquestra: le estoque, calcula demanda, executa subset sum, persiste reservas"
  artifacts:
    - path: "src/features/fardos/utils/stock-cache.ts"
      provides: "Cache in-memory com TTL de 2 minutos"
      exports: ["getCached", "setCache", "invalidateCache"]
    - path: "src/features/fardos/utils/stock-parser.ts"
      provides: "Leitura e parse da planilha de estoque com retry"
      exports: ["fetchStock", "withRetry"]
    - path: "src/features/fardos/utils/reservation-engine.ts"
      provides: "Orquestrador de reserva: estoque + demanda + subset sum + persistencia"
      exports: ["executeReservation"]
    - path: "src/features/fardos/utils/__tests__/stock-parser.test.ts"
      provides: "Testes do parser de estoque"
      min_lines: 40
    - path: "src/features/fardos/utils/__tests__/reservation-engine.test.ts"
      provides: "Testes do engine de reserva"
      min_lines: 60
  key_links:
    - from: "src/features/fardos/utils/stock-parser.ts"
      to: "src/lib/google-sheets.ts"
      via: "import getSheetData"
      pattern: "import.*getSheetData.*from.*google-sheets"
    - from: "src/features/fardos/utils/reservation-engine.ts"
      to: "src/features/fardos/utils/subset-sum.ts"
      via: "import findOptimalCombination"
      pattern: "import.*findOptimalCombination.*from.*subset-sum"
    - from: "src/features/fardos/utils/reservation-engine.ts"
      to: "src/lib/supabase/admin.ts"
      via: "import supabaseAdmin"
      pattern: "import.*supabaseAdmin.*from.*supabase/admin"
---

<objective>
Implementar leitura de estoque externo (Google Sheets API com retry e cache) e orquestrador de reserva que conecta estoque, demanda e subset sum para persistir reservas no Supabase.

Purpose: Este plano cria toda a infraestrutura de I/O entre Google Sheets e Supabase para o fluxo de reserva. O stock-parser le e parseia a planilha, o cache evita chamadas repetidas, e o reservation-engine orquestra o fluxo completo de reserva.

Output: stock-cache.ts, stock-parser.ts, reservation-engine.ts com testes unitarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-estoque-e-reserva-de-fardos/04-CONTEXT.md
@.planning/phases/04-estoque-e-reserva-de-fardos/04-RESEARCH.md
@.planning/phases/04-estoque-e-reserva-de-fardos/04-01-SUMMARY.md

<interfaces>
<!-- Tipos criados no Plan 01 -->
From src/features/fardos/types.ts:
```typescript
export interface StockItem {
  codigo_in: string
  sku: string
  quantidade: number
  endereco: string
}

export interface SubsetResult {
  fardos: StockItem[]
  soma: number
  cobertura: 'total' | 'parcial' | 'nenhuma'
}

export interface ReservationResult {
  skus_fardo: number
  skus_prateleira: number
  fardos_reservados: number
  parciais: string[]
  indisponivel: boolean
}
```

<!-- Funcao pura do subset sum criada no Plan 01 -->
From src/features/fardos/utils/subset-sum.ts:
```typescript
export function findOptimalCombination(fardos: StockItem[], demanda: number): SubsetResult
```

<!-- Google Sheets API existente -->
From src/lib/google-sheets.ts:
```typescript
export async function getSheetData(range: string): Promise<string[][] | null | undefined>
```

<!-- Supabase admin client -->
From src/lib/supabase/admin.ts:
```typescript
export const supabaseAdmin: SupabaseClient<Database>
```

<!-- Schema da tabela pedidos (para calcular demanda) -->
From supabase/migrations/00001_initial_schema.sql:
```sql
CREATE TABLE pedidos (
  id UUID PRIMARY KEY,
  sku TEXT NOT NULL,
  quantidade INTEGER NOT NULL,
  importacao_numero INTEGER NOT NULL,
  ...
);
```

<!-- Schema da tabela reservas (pos-migration 00003) -->
```sql
-- Apos migration 00003:
-- reservas: id, codigo_in, sku, quantidade, endereco, status, importacao_numero, created_at
-- Partial unique index: idx_reservas_codigo_in_reservado ON reservas(codigo_in) WHERE status='reservado'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cache in-memory e stock-parser com retry e testes</name>
  <files>src/features/fardos/utils/stock-cache.ts, src/features/fardos/utils/stock-parser.ts, src/features/fardos/utils/__tests__/stock-parser.test.ts</files>
  <read_first>
    - src/lib/google-sheets.ts
    - src/features/fardos/types.ts
    - vitest.config.ts
    - .planning/phases/04-estoque-e-reserva-de-fardos/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1 (parse valido): rows com headers [SKU, QUANTIDADE, CODIGO UPSELLER, ENDERECO] + 3 data rows -> retorna 3 StockItem com campos corretos
    - Test 2 (colunas faltando): rows sem coluna SKU -> retorna array vazio
    - Test 3 (linhas invalidas): row com quantidade NaN ou codigo_in vazio -> ignora essa linha
    - Test 4 (case insensitive headers): headers em lowercase "sku", "quantidade" -> funciona normalmente
    - Test 5 (cache hit): segunda chamada retorna cached sem chamar getSheetData novamente
    - Test 6 (cache expired): apos expirar TTL, chama getSheetData novamente
    - Test 7 (retry): getSheetData falha 2x e sucede na 3a -> retorna dados
    - Test 8 (retry exaurido): getSheetData falha 3x -> throw error
  </behavior>
  <action>
1. Criar `src/features/fardos/utils/stock-cache.ts`:
   ```typescript
   const CACHE_TTL_MS = 2 * 60 * 1000 // 2 minutos (D-05)

   interface CacheEntry<T> {
     data: T
     expires: number
   }

   const cache = new Map<string, CacheEntry<unknown>>()

   export function getCached<T>(key: string): T | null {
     const entry = cache.get(key) as CacheEntry<T> | undefined
     if (!entry || Date.now() > entry.expires) {
       if (entry) cache.delete(key)
       return null
     }
     return entry.data
   }

   export function setCache<T>(key: string, data: T): void {
     cache.set(key, { data, expires: Date.now() + CACHE_TTL_MS })
   }

   export function invalidateCache(key: string): void {
     cache.delete(key)
   }
   ```

2. Criar `src/features/fardos/utils/stock-parser.ts`:
   - Exportar `withRetry<T>(fn: () => Promise<T>, maxRetries = 3, baseDelay = 1000): Promise<T>` -- retry com backoff exponencial (D-03): delays 1s, 2s, 4s
   - Exportar `fetchStock(forceRefresh = false): Promise<StockItem[]>`:
     a. Se `forceRefresh`, chamar `invalidateCache('estoque')`
     b. Tentar `getCached<StockItem[]>('estoque')` -- se hit, retornar
     c. Chamar `withRetry(() => getSheetData('Estoque'))`
     d. Parsear headers da primeira row (case-insensitive com trim e toUpperCase): mapear indices de SKU, QUANTIDADE, CODIGO UPSELLER, ENDERECO
     e. Se coluna obrigatoria faltando, logar `console.error('[estoque] Colunas faltando: ...')` e retornar `[]`
     f. Iterar rows[1..n], criar StockItem para cada row valida (pular se codigo_in vazio, sku vazio, quantidade NaN ou <= 0)
     g. Chamar `setCache('estoque', items)` e retornar

3. Criar `src/features/fardos/utils/__tests__/stock-parser.test.ts`:
   - Mockar `@/lib/google-sheets` com `vi.mock`
   - Mockar `../stock-cache` para controlar cache nos testes
   - Testar todos os behaviors listados acima
   - Para testes de retry, usar `vi.fn()` que rejeita N vezes e resolve depois
   - Para teste de cache, verificar que `getSheetData` nao e chamado na segunda invocacao

NOTA: No mock do vitest, usar `vi.mock('@/lib/google-sheets', () => ({ getSheetData: vi.fn() }))` e importar com `vi.mocked()`.
  </action>
  <verify>
    <automated>npx vitest run src/features/fardos/utils/__tests__/stock-parser.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/features/fardos/utils/stock-cache.ts contains "export function getCached"
    - src/features/fardos/utils/stock-cache.ts contains "export function setCache"
    - src/features/fardos/utils/stock-cache.ts contains "export function invalidateCache"
    - src/features/fardos/utils/stock-cache.ts contains "2 * 60 * 1000"
    - src/features/fardos/utils/stock-parser.ts contains "export async function fetchStock"
    - src/features/fardos/utils/stock-parser.ts contains "export async function withRetry"
    - src/features/fardos/utils/stock-parser.ts contains "getSheetData('Estoque')"
    - src/features/fardos/utils/stock-parser.ts contains "CODIGO UPSELLER"
    - src/features/fardos/utils/stock-parser.ts contains "Math.pow(2, attempt)"
    - npx vitest run src/features/fardos/utils/__tests__/stock-parser.test.ts exits 0
  </acceptance_criteria>
  <done>Cache, parser e retry funcionais com todos os testes passando. fetchStock le planilha de estoque com retry 3x, cache de 2min, e parsing robusto por nome de header.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reservation engine com testes</name>
  <files>src/features/fardos/utils/reservation-engine.ts, src/features/fardos/utils/__tests__/reservation-engine.test.ts</files>
  <read_first>
    - src/features/fardos/utils/stock-parser.ts
    - src/features/fardos/utils/subset-sum.ts
    - src/features/fardos/types.ts
    - src/lib/supabase/admin.ts
    - app/api/upload/route.ts
    - .planning/phases/04-estoque-e-reserva-de-fardos/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1 (fardo vs prateleira D-06): SKU "ABC" com fardo no estoque -> skus_fardo=1; SKU "DEF" sem fardo -> skus_prateleira=1
    - Test 2 (demanda agregada D-07): 3 pedidos com SKU "ABC" quantidades [100, 200, 150] -> demanda total 450
    - Test 3 (visao global D-04): fardo "IN-001" ja reservado no banco -> nao aparece nos disponiveis
    - Test 4 (cobertura parcial D-08): demanda 1000, fardos somam 600 -> reserva 600, parciais inclui SKU
    - Test 5 (unicidade D-06): cada fardo fisico reservado uma vez apenas
    - Test 6 (nenhum pedido): importacao_numero sem pedidos -> resultado zerado
    - Test 7 (persistencia): fardos selecionados sao inseridos na tabela reservas com status 'reservado' e importacao_numero
  </behavior>
  <action>
1. Criar `src/features/fardos/utils/reservation-engine.ts`:
   - Importar `supabaseAdmin` de `@/lib/supabase/admin`
   - Importar `fetchStock` de `./stock-parser`
   - Importar `findOptimalCombination` de `./subset-sum`
   - Importar `ReservationResult` de `../types`

   Exportar `async function executeReservation(importacao_numero: number, forceRefresh = false): Promise<ReservationResult>`:

   a. Chamar `fetchStock(forceRefresh)` para ler estoque externo. Se throw (retry exauriu), retornar `{ skus_fardo: 0, skus_prateleira: 0, fardos_reservados: 0, parciais: [], indisponivel: true }` (D-03, D-16)

   b. Calcular demanda por SKU desta importacao (D-07):
      ```typescript
      const { data: pedidos } = await supabaseAdmin
        .from('pedidos')
        .select('sku, quantidade')
        .eq('importacao_numero', importacao_numero)
      ```
      Agregar em `Map<string, number>` (sku -> soma de quantidade)

   c. Buscar reservas existentes com status 'reservado' (D-04):
      ```typescript
      const { data: reservasExistentes } = await supabaseAdmin
        .from('reservas')
        .select('codigo_in')
        .eq('status', 'reservado')
      ```
      Criar `Set<string>` de codigos_in ja reservados

   d. Para cada SKU com demanda:
      - Filtrar fardos do estoque que tem esse SKU E nao estao no Set de ja reservados
      - Se nenhum fardo disponivel -> incrementar `skus_prateleira` (D-06)
      - Se tem fardos -> chamar `findOptimalCombination(fardosDisponiveis, demanda)`
      - Se `cobertura === 'nenhuma'` -> incrementar `skus_prateleira`
      - Se `cobertura === 'total'` ou `'parcial'`:
        - Insert na tabela reservas: `{ codigo_in, sku, quantidade, endereco, status: 'reservado', importacao_numero }`
        - Tratar erro 23505 (unique_violation) como "fardo ja reservado" e pular
        - Adicionar codigos_in inseridos ao Set de ja reservados (para proximo SKU)
        - Incrementar `skus_fardo` e `fardos_reservados`
        - Se `parcial`, adicionar SKU a lista `parciais`

   e. Retornar `ReservationResult`

2. Criar `src/features/fardos/utils/__tests__/reservation-engine.test.ts`:
   - Mockar `@/lib/supabase/admin` com mock do supabaseAdmin (encadear .from/.select/.eq/.in retornando dados mockados)
   - Mockar `./stock-parser` com mock do fetchStock
   - Mockar `./subset-sum` com mock do findOptimalCombination (para isolar testes do engine)
   - Testar todos os behaviors acima
   - Verificar que insert na tabela reservas e chamado com campos corretos

NOTA sobre mock do supabase: Usar padrao de chaining:
```typescript
const mockFrom = vi.fn().mockReturnValue({
  select: vi.fn().mockReturnValue({
    eq: vi.fn().mockResolvedValue({ data: [...], error: null }),
  }),
  insert: vi.fn().mockResolvedValue({ error: null }),
})
vi.mock('@/lib/supabase/admin', () => ({
  supabaseAdmin: { from: mockFrom }
}))
```
  </action>
  <verify>
    <automated>npx vitest run src/features/fardos/utils/__tests__/reservation-engine.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/features/fardos/utils/reservation-engine.ts contains "export async function executeReservation"
    - src/features/fardos/utils/reservation-engine.ts contains "import { supabaseAdmin }"
    - src/features/fardos/utils/reservation-engine.ts contains "import { fetchStock }"
    - src/features/fardos/utils/reservation-engine.ts contains "import { findOptimalCombination }"
    - src/features/fardos/utils/reservation-engine.ts contains ".eq('status', 'reservado')"
    - src/features/fardos/utils/reservation-engine.ts contains "indisponivel: true"
    - src/features/fardos/utils/reservation-engine.ts contains ".eq('importacao_numero', importacao_numero)"
    - npx vitest run src/features/fardos/utils/__tests__/reservation-engine.test.ts exits 0
  </acceptance_criteria>
  <done>Reservation engine orquestra fluxo completo: estoque -> demanda -> subset sum -> persistencia. Testes verificam classificacao fardo/prateleira, visao global, cobertura parcial e unicidade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Sheets -> servidor | Dados de estoque nao confiaveis (automacao externa alimenta) |
| servidor -> Supabase | Inserts de reservas devem respeitar constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | stock-parser.ts | mitigate | Validar tipos (quantidade como Number, strings com trim), ignorar rows invalidas |
| T-04-04 | Denial of Service | stock-parser.ts | mitigate | Cache de 2min limita chamadas repetidas a Google Sheets; retry limitado a 3x |
| T-04-05 | Elevation of Privilege | reservation-engine.ts | mitigate | Partial unique index no banco impede reserva duplicada por race condition |
| T-04-06 | Information Disclosure | stock-parser.ts | accept | Dados de estoque nao sao PII; logs de erro sao server-side only |
</threat_model>

<verification>
- `npx vitest run src/features/fardos/utils/__tests__/ --reporter=verbose` -- todos os testes passam
- stock-parser usa getSheetData existente com retry wrapper
- reservation-engine usa supabaseAdmin para operacoes privilegiadas
- Cache invalidavel para re-reserva (D-02)
</verification>

<success_criteria>
- fetchStock le planilha Estoque com retry 3x, cache 2min, parse robusto
- executeReservation orquestra fluxo completo e retorna ReservationResult
- Fardo vs prateleira decidido por disponibilidade de fardo (D-06), nao por quantidade >= 50
- Fardos ja reservados globalmente sao excluidos (D-04)
- Testes unitarios passam para parser e engine
</success_criteria>

<output>
After completion, create `.planning/phases/04-estoque-e-reserva-de-fardos/04-02-SUMMARY.md`
</output>
