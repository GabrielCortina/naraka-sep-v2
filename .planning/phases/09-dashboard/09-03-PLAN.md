---
phase: 09-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/features/dashboard/components/dashboard-client.tsx
  - src/features/dashboard/components/dashboard-block.tsx
  - src/features/dashboard/components/stat-card.tsx
  - src/features/dashboard/components/resumo-geral.tsx
  - src/features/dashboard/components/progressao-metodo.tsx
  - src/features/dashboard/components/top-separadores.tsx
  - src/features/dashboard/components/top-fardistas.tsx
  - src/features/dashboard/components/status-fardos.tsx
  - src/features/dashboard/components/por-separador.tsx
  - src/features/dashboard/components/period-filter.tsx
  - app/(authenticated)/dashboard/page.tsx
autonomous: false

requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07

user_setup:
  - service: supabase
    why: "Schema push for historico_diario table and realtime publication"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens"

must_haves:
  truths:
    - "Lider/admin sees 6 dashboard blocks with live data on the Dashboard tab"
    - "Desktop shows 60/40 two-column layout; mobile shows single-column stack"
    - "Stat cards show large numbers with correct labels and em atraso in red"
    - "Rankings show medal emojis for top 3 and support period filter"
    - "Status de fardos shows 3 colored counters"
    - "All blocks update in real time without page refresh"
  artifacts:
    - path: "src/features/dashboard/components/dashboard-client.tsx"
      provides: "Main client entry point composing all blocks"
      min_lines: 40
    - path: "app/(authenticated)/dashboard/page.tsx"
      provides: "Dashboard page importing client component"
      contains: "DashboardClient"
  key_links:
    - from: "app/(authenticated)/dashboard/page.tsx"
      to: "src/features/dashboard/components/dashboard-client.tsx"
      via: "import DashboardClient"
      pattern: "import.*DashboardClient"
    - from: "src/features/dashboard/components/dashboard-client.tsx"
      to: "src/features/dashboard/hooks/use-dashboard-data.ts"
      via: "useDashboardData hook"
      pattern: "useDashboardData"
---

<objective>
Build all dashboard UI components and wire them into the existing Dashboard page, then push the database schema. This delivers the complete working dashboard with 6 blocks, responsive layout, period filter, and real-time updates.

Purpose: Delivers the fully functional dashboard visible to lider and admin roles, completing Phase 9.

Output: 10 components + updated page + schema pushed to Supabase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboard/09-CONTEXT.md
@.planning/phases/09-dashboard/09-RESEARCH.md
@.planning/phases/09-dashboard/09-UI-SPEC.md
@.planning/phases/09-dashboard/09-01-SUMMARY.md
@.planning/phases/09-dashboard/09-02-SUMMARY.md

@src/features/cards/components/progress-bar.tsx
@src/features/cards/components/urgency-badge.tsx
@src/features/cards/components/marketplace-badge.tsx
@src/components/ui/card.tsx
@src/components/ui/collapsible.tsx
@src/components/ui/select.tsx

<interfaces>
<!-- Types and hooks from Plans 01-02 that this plan consumes -->

From src/features/dashboard/types.ts:
```typescript
export interface DashboardData {
  resumo: { pecas_separadas: number; listas_pendentes: number; listas_concluidas: number; listas_em_atraso: number }
  progressao: ProgressaoMetodo[]
  topSeparadores: RankingEntry[]
  topFardistas: RankingEntry[]
  statusFardos: { pendentes: number; encontrados: number; baixados: number }
  porSeparador: SeparadorProgress[]
}
export interface ProgressaoMetodo { grupo_envio: string; total_pecas: number; pecas_separadas: number; percent: number; urgency: UrgencyTier; deadline_ms: number }
export interface RankingEntry { position: number; user_id: string; nome: string; pecas_separadas: number; cards_concluidos: number; fardos_confirmados: number }
export interface SeparadorProgress { user_id: string; nome: string; total_pecas: number; pecas_separadas: number; percent: number; num_cards: number }
export type PeriodFilter = 'hoje' | '15d' | '30d' | 'mes_atual' | 'ultimo_mes' | '3m' | 'personalizado'
```

From src/features/dashboard/hooks/use-dashboard-data.ts:
```typescript
export function useDashboardData(periodFilter: { isHistorical: boolean; dateRange: { start: string; end: string } | null }): { data: DashboardData | null; loading: boolean; error: string | null; refetch: () => Promise<void> }
```

From src/features/dashboard/hooks/use-period-filter.ts:
```typescript
export function usePeriodFilter(): { period: PeriodFilter; setPeriod; customStart; setCustomStart; customEnd; setCustomEnd; dateRange; isHistorical: boolean }
```

From src/features/cards/components (existing, reusable):
```typescript
// progress-bar.tsx
export function ProgressBar({ value, urgency }: { value: number; urgency: UrgencyTier })
// urgency-badge.tsx
export function UrgencyBadge({ urgency, grupoEnvio }: { urgency: UrgencyTier; grupoEnvio: string })
// marketplace-badge.tsx
export function MarketplaceBadge({ grupoEnvio }: { grupoEnvio: string })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: All dashboard UI components and page wiring</name>
  <files>
    src/features/dashboard/components/dashboard-client.tsx,
    src/features/dashboard/components/dashboard-block.tsx,
    src/features/dashboard/components/stat-card.tsx,
    src/features/dashboard/components/resumo-geral.tsx,
    src/features/dashboard/components/progressao-metodo.tsx,
    src/features/dashboard/components/top-separadores.tsx,
    src/features/dashboard/components/top-fardistas.tsx,
    src/features/dashboard/components/status-fardos.tsx,
    src/features/dashboard/components/por-separador.tsx,
    src/features/dashboard/components/period-filter.tsx,
    app/(authenticated)/dashboard/page.tsx
  </files>
  <read_first>
    src/features/dashboard/types.ts,
    src/features/dashboard/hooks/use-dashboard-data.ts,
    src/features/dashboard/hooks/use-period-filter.ts,
    src/features/dashboard/hooks/use-dashboard-realtime.ts,
    src/features/cards/components/progress-bar.tsx,
    src/features/cards/components/urgency-badge.tsx,
    src/features/cards/components/marketplace-badge.tsx,
    src/features/cards/lib/deadline-config.ts,
    src/components/ui/card.tsx,
    src/components/ui/collapsible.tsx,
    src/components/ui/select.tsx,
    src/components/ui/input.tsx,
    app/(authenticated)/dashboard/page.tsx,
    .planning/phases/09-dashboard/09-UI-SPEC.md
  </read_first>
  <action>
    Build all 10 components following the UI-SPEC exactly. All components use Tailwind classes. All text in Portuguese per project constraint.

    **1. stat-card.tsx** (per D-05, UI-SPEC Component 1):
    - Props: `{ value: number; label: string; variant?: 'default' | 'destructive' }`
    - shadcn Card with padding 16px (`p-4`)
    - Number: `text-4xl font-semibold` (36px). If variant=destructive AND value>0: `text-destructive` number, `bg-destructive/5` card background
    - Label: `text-xs font-semibold uppercase text-muted-foreground`
    - Add `aria-label={label + ': ' + value}` on the card

    **2. dashboard-block.tsx** (per D-03, UI-SPEC Component 7):
    - Props: `{ title: string; children: React.ReactNode; defaultOpen?: boolean; headerRight?: React.ReactNode }`
    - shadcn Card background secondary (`bg-secondary`), border, rounded, p-4
    - Header: title in `text-xs font-semibold uppercase` + headerRight slot + Collapsible trigger with ChevronDown/ChevronUp from lucide-react (16px)
    - Children wrapped in CollapsibleContent
    - Default state: `defaultOpen ?? false` (collapsed shows summary, expanded shows details)
    - Use `@radix-ui/react-collapsible` via shadcn Collapsible

    **3. resumo-geral.tsx** (DASH-01, D-05):
    - Props: `{ resumo: DashboardData['resumo'] }`
    - DashboardBlock with title "RESUMO GERAL", defaultOpen=true (stat cards always visible)
    - Inside: 4 StatCards in a grid: `grid grid-cols-2 lg:grid-cols-4 gap-2 lg:gap-4`
    - Cards: "Pecas Separadas" (default), "Pendentes" (default), "Concluidas" (default), "Em Atraso" (destructive)

    **4. progressao-metodo.tsx** (DASH-02, D-06):
    - Props: `{ progressao: ProgressaoMetodo[] }`
    - DashboardBlock with title "PROGRESSAO POR METODO"
    - Each row: `flex items-center gap-2 py-2 border-b last:border-b-0`
    - Desktop row: MarketplaceBadge + method name (text-sm) + ProgressBar (flex-1) + "X/Y pecas" (text-sm) + countdown via `formatCountdown(entry.deadline_ms)` (text-sm) + UrgencyBadge
    - Mobile row: stack 3 lines. Line 1: MarketplaceBadge + name. Line 2: ProgressBar full width. Line 3: counts + countdown
    - Use responsive classes: `hidden lg:flex` for desktop, `flex lg:hidden flex-col` for mobile

    **5. top-separadores.tsx** (DASH-03, D-07, D-08):
    - Props: `{ entries: RankingEntry[]; periodFilter: ReturnType<typeof usePeriodFilter> }`
    - DashboardBlock with title "TOP SEPARADORES", headerRight = PeriodFilter component
    - Rows: position (text-sm font-semibold) + medal emoji for top 3 (position 1: gold medal, 2: silver medal, 3: bronze medal -- use actual Unicode emoji characters) + name (text-sm) + pecas count (text-sm font-semibold) + " pecas" + cards count (text-xs text-muted-foreground) + " cards"
    - Rows separated by border-b
    - Medal emojis: use string literals. No medals for position > 3. Gap of 4px (gap-1) between medal and name

    **6. top-fardistas.tsx** (DASH-04, D-09):
    - Props: `{ entries: RankingEntry[] }`
    - DashboardBlock with title "TOP FARDISTAS" (no separate filter -- controlled by Top Separadores per D-08)
    - Same row style as top-separadores: position + medal + name + fardos count (text-sm font-semibold) + " fardos"

    **7. status-fardos.tsx** (DASH-05, D-10):
    - Props: `{ statusFardos: DashboardData['statusFardos'] }`
    - DashboardBlock with title "STATUS DE FARDOS"
    - 3 counters stacked with gap-2:
      - PENDENTES: `border-l-4 border-yellow-500 bg-yellow-500/5 py-2 px-4` + label "PENDENTES" (text-xs font-semibold uppercase) + count (text-xl font-semibold)
      - ENCONTRADOS: `border-l-4 border-blue-500 bg-blue-500/5 py-2 px-4` + label "ENCONTRADOS" + count
      - BAIXADOS: `border-l-4 border-green-600 bg-green-600/5 py-2 px-4` + label "BAIXADOS" + count

    **8. por-separador.tsx** (DASH-06, D-11):
    - Props: `{ entries: SeparadorProgress[] }`
    - DashboardBlock with title "POR SEPARADOR"
    - Rows: name (text-sm, min-w-[80px]) + ProgressBar (flex-1, urgency based on percent: ok if >50%, warning if 20-50%, overdue if <20%) + percent (text-sm font-semibold) + "X/Y pecas" (text-xs text-muted-foreground) + "N cards" (text-xs text-muted-foreground)
    - Sorted by percent descending (already sorted by hook)

    **9. period-filter.tsx** (D-08):
    - Props: `{ periodFilter: ReturnType<typeof usePeriodFilter> }`
    - shadcn Select with options: "Hoje", "Ultimos 15 dias", "Ultimos 30 dias", "Mes atual", "Ultimo mes", "Ultimos 3 meses", "Periodo personalizado"
    - When "Periodo personalizado" selected, show two Input[type=date] fields below: "De" and "Ate"
    - Select compact size to fit in block header

    **10. dashboard-client.tsx** (main entry, D-01, D-02, D-04):
    - 'use client' component
    - Uses `useDashboardData` and `usePeriodFilter` hooks
    - Loading state: skeleton placeholders (bg-muted animate-pulse divs matching block dimensions)
    - Error state: centered error message per UI-SPEC copywriting
    - Empty state: "Nenhum dado disponivel" + "Importe uma planilha para iniciar a operacao do dia."
    - Desktop layout (per D-01): `grid grid-cols-1 lg:grid-cols-[3fr_2fr] gap-8`
      - Left column: ResumoGeral + ProgressaoMetodo + PorSeparador with gap-6
      - Right column: TopSeparadores + TopFardistas + StatusFardos with gap-6
    - Mobile layout (per D-04): single column, blocks stacked: Resumo -> Progressao -> PorSeparador -> TopSeparadores -> TopFardistas -> StatusFardos. Use the same grid but lg breakpoint collapses to single column naturally
    - Padding: `p-6 lg:p-8`

    **11. Update page.tsx** (D-02):
    - Replace placeholder content with `<DashboardClient />`
    - Keep metadata export
    - Page is a server component that renders the client component (same pattern as other pages)

    All empty states show "Nenhum dado disponivel" message. All text in Portuguese.
  </action>
  <verify>
    <automated>cd /Users/gabrielcortina/Desktop/naraka-sep-v2 && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - src/features/dashboard/components/dashboard-client.tsx contains "'use client'"
    - src/features/dashboard/components/dashboard-client.tsx contains "useDashboardData"
    - src/features/dashboard/components/dashboard-client.tsx contains "usePeriodFilter"
    - src/features/dashboard/components/dashboard-client.tsx contains "lg:grid-cols-[3fr_2fr]" (60/40 layout per D-01)
    - src/features/dashboard/components/resumo-geral.tsx contains "RESUMO GERAL"
    - src/features/dashboard/components/resumo-geral.tsx contains "Em Atraso"
    - src/features/dashboard/components/progressao-metodo.tsx contains "PROGRESSAO POR METODO"
    - src/features/dashboard/components/progressao-metodo.tsx contains "MarketplaceBadge"
    - src/features/dashboard/components/progressao-metodo.tsx contains "ProgressBar"
    - src/features/dashboard/components/top-separadores.tsx contains "TOP SEPARADORES"
    - src/features/dashboard/components/top-fardistas.tsx contains "TOP FARDISTAS"
    - src/features/dashboard/components/status-fardos.tsx contains "STATUS DE FARDOS"
    - src/features/dashboard/components/status-fardos.tsx contains "PENDENTES"
    - src/features/dashboard/components/status-fardos.tsx contains "ENCONTRADOS"
    - src/features/dashboard/components/status-fardos.tsx contains "BAIXADOS"
    - src/features/dashboard/components/status-fardos.tsx contains "border-l-4 border-yellow-500" (PENDENTES color per D-10)
    - src/features/dashboard/components/status-fardos.tsx contains "border-l-4 border-blue-500" (ENCONTRADOS color per D-10)
    - src/features/dashboard/components/status-fardos.tsx contains "border-l-4 border-green-600" (BAIXADOS color per D-10)
    - src/features/dashboard/components/por-separador.tsx contains "POR SEPARADOR"
    - src/features/dashboard/components/period-filter.tsx contains "Periodo personalizado"
    - app/(authenticated)/dashboard/page.tsx contains "DashboardClient"
    - tsc --noEmit passes without errors related to dashboard
  </acceptance_criteria>
  <done>All 10 dashboard components render correctly with proper layout, colors, typography, and responsive behavior. Dashboard page shows live data.</done>
</task>

<task type="auto">
  <name>Task 2: Schema push and full verification</name>
  <files></files>
  <read_first>
    supabase/migrations/00011_historico_diario.sql,
    supabase/migrations/00012_realtime_baixados_pedidos.sql
  </read_first>
  <action>
    **Step 1: Push schema to Supabase**

    Run `supabase db push` to apply the two new migrations (00011_historico_diario.sql and 00012_realtime_baixados_pedidos.sql).

    If SUPABASE_ACCESS_TOKEN is set as env var, run:
    ```bash
    npx supabase db push
    ```

    If it requires interactive confirmation, try:
    ```bash
    echo "y" | npx supabase db push
    ```

    If push fails due to auth, flag for manual intervention.

    **Step 2: Run full test suite**
    ```bash
    npx vitest run --reporter=verbose
    ```

    **Step 3: Run type check**
    ```bash
    npx tsc --noEmit
    ```

    **Step 4: Build check**
    ```bash
    npx next build
    ```

    All must pass.
  </action>
  <verify>
    <automated>cd /Users/gabrielcortina/Desktop/naraka-sep-v2 && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - supabase db push completes successfully (historico_diario table created, realtime publication updated)
    - npx vitest run exits with code 0 (all tests pass)
    - npx tsc --noEmit exits with code 0 (no type errors)
  </acceptance_criteria>
  <done>Schema pushed. Full test suite green. Type check clean. Dashboard fully operational.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of complete dashboard</name>
  <action>Human verifies the complete dashboard works visually and functionally. Built: complete real-time dashboard with 6 blocks (Resumo Geral, Progressao por Metodo, Top Separadores, Top Fardistas, Status de Fardos, Por Separador). Desktop 60/40 layout. Mobile single-column stack. Period filter controls rankings. Real-time updates via Supabase subscriptions.</action>
  <verify>
    1. Open the app and log in as admin or lider
    2. Navigate to the Dashboard tab
    3. Verify 6 blocks are visible in 60/40 two-column layout on desktop
    4. Verify stat cards show numbers (may be 0 if no data imported today)
    5. If data exists: verify Progressao shows marketplace badges with progress bars and countdowns
    6. If data exists: verify Top Separadores shows ranked list with medal emojis for top 3
    7. Change period filter in Top Separadores to "Ultimos 15 dias" -- rankings should update
    8. Resize browser to mobile width -- verify blocks stack vertically in single column
    9. Verify Status de Fardos shows 3 counters with yellow/blue/green left borders
    10. In another browser tab, make a change (e.g., confirm a progresso item) -- verify dashboard updates within ~1 second without page refresh
  </verify>
  <done>Dashboard visually correct and functionally working with real-time updates confirmed by human.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Supabase | All dashboard data fetched via authenticated RLS-protected queries |
| browser rendering | Dashboard is read-only; no write operations from UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Elevation of Privilege | Dashboard page access | mitigate | Middleware role check restricts to admin/lider (existing auth); page.tsx is server component that renders client component only after auth |
| T-09-07 | Information Disclosure | Worker performance data | accept | Rankings show worker names and metrics; this is intended behavior for admin/lider roles; not sensitive PII |
</threat_model>

<verification>
1. npx vitest run (all tests pass)
2. npx tsc --noEmit (no type errors)
3. npx next build (build succeeds)
4. Manual: dashboard loads with data, updates in real time, responsive layout works
</verification>

<success_criteria>
- Dashboard page shows 6 blocks with correct data
- Desktop: 60/40 two-column layout
- Mobile: single-column stacked layout
- Period filter switches rankings between live and historical data
- Real-time updates work without page refresh
- Schema push successful
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard/09-03-SUMMARY.md`
</output>
