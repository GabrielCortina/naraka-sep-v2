---
phase: 06-lista-de-fardos
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/app/api/fardos/ok/route.ts
  - src/app/api/fardos/ne/route.ts
  - src/features/fardos/utils/__tests__/fardo-ok.test.ts
  - src/features/fardos/utils/__tests__/fardo-ne.test.ts
  - src/features/fardos/utils/fardo-ok-handler.ts
  - src/features/fardos/utils/fardo-ne-handler.ts
autonomous: true
requirements: [FARD-02, FARD-03]
must_haves:
  truths:
    - "Fardista marca fardo OK e ele aparece no trafego com todos os campos da planilha"
    - "Fardista marca fardo N/E e sistema busca alternativo ou libera para prateleira"
    - "Dupla verificacao impede apagar dados errados da planilha"
    - "Race condition prevenida: trafego_fardos verificado antes de operar na planilha"
  artifacts:
    - path: "src/app/api/fardos/ok/route.ts"
      provides: "API route POST para fluxo OK"
      exports: ["POST"]
    - path: "src/app/api/fardos/ne/route.ts"
      provides: "API route POST para fluxo N/E"
      exports: ["POST"]
    - path: "src/features/fardos/utils/fardo-ok-handler.ts"
      provides: "Logica pura do fluxo OK testavel"
      exports: ["handleFardoOk"]
    - path: "src/features/fardos/utils/fardo-ne-handler.ts"
      provides: "Logica pura do fluxo N/E testavel"
      exports: ["handleFardoNe"]
  key_links:
    - from: "src/app/api/fardos/ok/route.ts"
      to: "src/lib/google-sheets.ts"
      via: "getSheetData + clearSheetRange"
      pattern: "getSheetData.*clearSheetRange"
    - from: "src/app/api/fardos/ne/route.ts"
      to: "src/features/fardos/utils/subset-sum.ts"
      via: "findOptimalCombination para alternativo"
      pattern: "findOptimalCombination"
---

<objective>
API Routes para os fluxos OK (fardo encontrado) e N/E (nao encontrado) com logica pura extraida em handlers testaveis. Fluxo OK busca na planilha, insere no trafego_fardos com todos os campos, e apaga colunas F+ com dupla verificacao. Fluxo N/E busca alternativo via subset sum ou libera para prateleira.

Purpose: Backend completo dos dois fluxos criticos da tela de fardos — o coração operacional da Phase 6.
Output: 2 API routes + 2 handlers testaveis + testes unitarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-lista-de-fardos/06-CONTEXT.md
@.planning/phases/06-lista-de-fardos/06-RESEARCH.md
@.planning/phases/06-lista-de-fardos/06-01-SUMMARY.md

<interfaces>
<!-- Contratos existentes que os handlers precisam -->

From src/lib/google-sheets.ts:
```typescript
export async function getSheetData(range: string): Promise<string[][] | null | undefined>
export async function clearSheetRange(range: string): Promise<any>
```

From src/features/fardos/utils/stock-parser.ts:
```typescript
export async function fetchStock(forceRefresh?: boolean): Promise<StockItem[]>
function normalizeHeader(key: string): string  // NFD normalize
```

From src/features/fardos/utils/subset-sum.ts:
```typescript
export function findOptimalCombination(items: StockItem[], target: number): SubsetResult
```

From src/lib/supabase/admin.ts:
```typescript
export const supabaseAdmin: SupabaseClient
```

From src/features/fardos/types.ts (apos Plan 01):
```typescript
export type FardoStatus = 'pendente' | 'encontrado' | 'nao_encontrado'
export interface FardoItem { ... }
```

API auth pattern (Phase 5 05-06-SUMMARY):
```typescript
import { createClient } from '@/lib/supabase/server'
import { supabaseAdmin } from '@/lib/supabase/admin'
// const supabase = await createClient()
// const { data: { user } } = await supabase.auth.getUser()
// const { data: userData } = await supabaseAdmin.from('users')...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fluxo OK — handler testavel + API route (D-14 a D-19)</name>
  <files>
    src/features/fardos/utils/fardo-ok-handler.ts,
    src/features/fardos/utils/__tests__/fardo-ok.test.ts,
    src/app/api/fardos/ok/route.ts
  </files>
  <read_first>
    src/lib/google-sheets.ts,
    src/features/fardos/utils/stock-parser.ts,
    src/features/fardos/types.ts,
    src/lib/supabase/admin.ts,
    src/app/api/cards/progress/route.ts,
    supabase/migrations/00001_initial_schema.sql
  </read_first>
  <behavior>
    - Test 1: findBaleInSheet retorna rowIndex e rowData quando codigo_in encontrado com match exato (trim+lowercase)
    - Test 2: findBaleInSheet retorna null quando codigo_in nao encontrado
    - Test 3: findBaleInSheet ignora diferencas de case e espacos
    - Test 4: mapRowToTrafegoFields mapeia colunas A-N para campos corretos do trafego_fardos
    - Test 5: mapRowToTrafegoFields preserva colunas A-E como dados lidos (nao apaga)
    - Test 6: validateRowMatch retorna true quando codigo_in da re-leitura bate com original
    - Test 7: validateRowMatch retorna false quando linha mudou (race condition)
  </behavior>
  <action>
    **1. Criar `src/features/fardos/utils/fardo-ok-handler.ts`** com funcoes puras testaveis:

    ```typescript
    /** Headers da planilha normalizados (padrao NFD do stock-parser.ts) */
    function normalizeHeader(key: string): string {
      return key.trim().toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '')
    }

    /** Busca fardo na planilha por codigo IN com match exato (D-15) */
    export function findBaleInSheet(
      rows: string[][],
      codigoIn: string
    ): { rowIndex: number; rowData: string[]; headers: string[] } | null {
      const headers = rows[0].map(h => normalizeHeader(h?.toString() ?? ''))
      const codigoCol = headers.indexOf('codigo upseller')
      if (codigoCol === -1) return null

      const normalizedTarget = codigoIn.trim().toLowerCase()
      for (let i = 1; i < rows.length; i++) {
        const cellValue = rows[i][codigoCol]?.toString().trim().toLowerCase()
        if (cellValue === normalizedTarget) {
          return { rowIndex: i, rowData: rows[i], headers }
        }
      }
      return null
    }

    /** Mapeia linha da planilha para campos do trafego_fardos (D-16) */
    export function mapRowToTrafegoFields(rowData: string[], headers: string[]) {
      const get = (name: string) => {
        const idx = headers.indexOf(normalizeHeader(name))
        return idx !== -1 ? (rowData[idx]?.toString().trim() || null) : null
      }
      return {
        prioridade: get('PRIORIDADE'),
        prateleira: get('PRATELEIRA'),
        posicao: get('POSICAO'),       // ou POSIÇÃO — NFD normalize trata
        altura: get('ALTURA'),
        endereco: get('ENDERECO'),     // ou ENDEREÇO
        sku: get('SKU'),
        quantidade: Number(get('QUANTIDADE')) || 0,
        codigo_upseller: get('CODIGO UPSELLER'),
        data_entrada: get('DATA ENTRADA'),
        hora_entrada: get('HORA ENTRADA'),
        operador: get('OPERADOR'),
        transferencia: get('TRANFERENCIA'),  // planilha real tem TRANFERENCIA (sem S)
        data_transferencia: get('DATA TRANFERENCIA'),
        operador_transferencia: get('OPERADOR'),  // col N = segundo OPERADOR
      }
    }

    /** Dupla verificacao: re-le a linha e confere se codigo IN bate (D-17) */
    export function validateRowMatch(
      originalCodigoIn: string,
      recheckRow: string[],
      headers: string[]
    ): boolean {
      const codigoCol = headers.indexOf('codigo upseller')
      if (codigoCol === -1) return false
      return recheckRow[codigoCol]?.toString().trim().toLowerCase() ===
             originalCodigoIn.trim().toLowerCase()
    }
    ```

    **2. Criar testes** em `src/features/fardos/utils/__tests__/fardo-ok.test.ts` com:
    - Mock de dados da planilha (headers reais: PRIORIDADE, PRATELEIRA, POSICAO, ALTURA, ENDERECO, SKU, QUANTIDADE, CODIGO UPSELLER, DATA ENTRADA, HORA ENTRADA, OPERADOR, TRANFERENCIA, DATA TRANFERENCIA, OPERADOR)
    - Testes das 3 funcoes puras: findBaleInSheet, mapRowToTrafegoFields, validateRowMatch
    - Importar de vitest explicitamente (padrao do projeto)

    **3. Criar `src/app/api/fardos/ok/route.ts`** (API Route POST):

    Request body: `{ reserva_id: string, codigo_in: string }`

    Fluxo completo:
    1. Auth: `createClient() -> getUser() -> supabaseAdmin.from('users')` role check (fardista/admin/lider)
    2. Prevencao de duplicata (Pitfall 1): verificar se `trafego_fardos` ja tem registro com este `codigo_in` e status='encontrado'. Se sim, retornar 409 "Fardo ja processado"
    3. Passo 1 (D-15): `getSheetData('Estoque')` e buscar com `findBaleInSheet(rows, codigo_in)`. Se nao encontrar, retornar 404
    4. Passo 2 (D-16): Mapear com `mapRowToTrafegoFields` e inserir no `trafego_fardos` via `supabaseAdmin`:
       ```typescript
       const mapped = mapRowToTrafegoFields(result.rowData, result.headers)
       await supabaseAdmin.from('trafego_fardos').insert({
         reserva_id: reserva_id,
         codigo_in: codigo_in,
         sku: mapped.sku,
         quantidade: mapped.quantidade,
         endereco: mapped.endereco,
         status: 'encontrado',
         fardista_id: user.id,
         fardista_nome: userData.nome,
         clicked_at: new Date().toISOString(),
         prioridade: mapped.prioridade,
         prateleira: mapped.prateleira,
         posicao: mapped.posicao,
         altura: mapped.altura,
         data_entrada: mapped.data_entrada,
         hora_entrada: mapped.hora_entrada,
         operador: mapped.operador,
         transferencia: mapped.transferencia,
         data_transferencia: mapped.data_transferencia,
         operador_transferencia: mapped.operador_transferencia,
       })
       ```
    5. Se insert falhar, retornar 500 SEM tocar na planilha (D-18)
    6. Passo 3 (D-17): Dupla verificacao — re-ler linha `getSheetData('Estoque!A${row+1}:N${row+1}')` e confirmar com `validateRowMatch`. Se nao bater, retornar 409 "Conflito na planilha" (mas manter registro no trafego — D-18)
    7. Apagar colunas F+ (index 5 em diante): `clearSheetRange('Estoque!F${row+1}:N${row+1}')`. Se falhar, logar erro mas NAO desfazer o insert no trafego (D-18)
    8. Atualizar status da reserva para 'encontrado': `supabaseAdmin.from('reservas').update({ status: 'encontrado' }).eq('id', reserva_id)`
    9. Retornar 200 `{ success: true }`

    Wrap `request.json()` em try/catch retornando 400 para body invalido (padrao Phase 5).
  </action>
  <verify>
    <automated>npx vitest run src/features/fardos/utils/__tests__/fardo-ok.test.ts -x && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/features/fardos/utils/fardo-ok-handler.ts contem "export function findBaleInSheet"
    - src/features/fardos/utils/fardo-ok-handler.ts contem "export function mapRowToTrafegoFields"
    - src/features/fardos/utils/fardo-ok-handler.ts contem "export function validateRowMatch"
    - src/features/fardos/utils/fardo-ok-handler.ts contem "normalizeHeader" com NFD
    - src/app/api/fardos/ok/route.ts contem "export async function POST"
    - src/app/api/fardos/ok/route.ts contem "getSheetData" (leitura planilha)
    - src/app/api/fardos/ok/route.ts contem "clearSheetRange" (apagar colunas F+)
    - src/app/api/fardos/ok/route.ts contem "trafego_fardos" (insert)
    - src/app/api/fardos/ok/route.ts contem "validateRowMatch" (dupla verificacao D-17)
    - src/app/api/fardos/ok/route.ts contem "status: 'encontrado'" (atualizar reserva)
    - npx vitest run exits 0
    - tsc --noEmit sem erros nos arquivos novos
  </acceptance_criteria>
  <done>Handler testavel com 3 funcoes puras, API route POST /api/fardos/ok com fluxo completo OK (buscar planilha, inserir trafego, dupla verificacao, apagar colunas F+), testes passam, compila</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fluxo N/E — handler testavel + API route (D-20 a D-22)</name>
  <files>
    src/features/fardos/utils/fardo-ne-handler.ts,
    src/features/fardos/utils/__tests__/fardo-ne.test.ts,
    src/app/api/fardos/ne/route.ts
  </files>
  <read_first>
    src/features/fardos/utils/subset-sum.ts,
    src/features/fardos/utils/stock-parser.ts,
    src/features/fardos/utils/reservation-engine.ts,
    src/features/fardos/types.ts,
    src/app/api/fardos/ok/route.ts,
    supabase/migrations/00001_initial_schema.sql
  </read_first>
  <behavior>
    - Test 1: findAlternativeBale retorna StockItem quando fardo alternativo disponivel dentro de 20% margem
    - Test 2: findAlternativeBale retorna null quando nenhum fardo alternativo disponivel
    - Test 3: findAlternativeBale filtra fardos ja reservados (Set de codigos_in)
    - Test 4: findAlternativeBale usa findOptimalCombination para importacao normal (margem 20%)
  </behavior>
  <action>
    **1. Criar `src/features/fardos/utils/fardo-ne-handler.ts`** com funcao pura testavel:

    ```typescript
    import { findOptimalCombination } from './subset-sum'
    import type { StockItem } from '../types'

    /**
     * Busca fardo alternativo para o mesmo SKU (D-20).
     * - Importacao normal: usa regra dos 20% (subset sum)
     * - Cascata (Phase 7): aceita qualquer fardo
     * Na Phase 6, is_cascata e sempre false — implementar ambos os caminhos
     */
    export function findAlternativeBale(
      stock: StockItem[],
      sku: string,
      quantidade: number,
      reservedCodigosIn: Set<string>,
      isCascata: boolean
    ): StockItem | null {
      const disponiveis = stock.filter(
        item => item.sku === sku && !reservedCodigosIn.has(item.codigo_in)
      )

      if (disponiveis.length === 0) return null

      if (isCascata) {
        // Phase 7: qualquer fardo serve
        return disponiveis[0]
      }

      // Importacao normal: subset sum com 20% margem
      const resultado = findOptimalCombination(disponiveis, quantidade)
      if (resultado.fardos.length === 0) return null
      // Retornar o primeiro fardo da combinacao otima
      return resultado.fardos[0]
    }
    ```

    **2. Criar testes** em `src/features/fardos/utils/__tests__/fardo-ne.test.ts`:
    - Mock StockItem[] com fardos de mesmo SKU e quantidades variadas
    - Testar findAlternativeBale com fardos disponiveis e sem disponiveis
    - Testar exclusao de fardos ja reservados
    - Importar de vitest explicitamente

    **3. Criar `src/app/api/fardos/ne/route.ts`** (API Route POST):

    Request body: `{ reserva_id: string, codigo_in: string }`

    Fluxo completo:
    1. Auth: mesmo padrao (createClient -> getUser -> role check fardista/admin/lider)
    2. Buscar reserva original: `supabaseAdmin.from('reservas').select('*').eq('id', reserva_id).single()`. Se nao encontrar, 404
    3. Buscar estoque atualizado: `fetchStock(true)` (forceRefresh para dados frescos)
    4. Buscar todas as reservas ativas: `supabaseAdmin.from('reservas').select('codigo_in').eq('status', 'reservado')` para montar Set de ja reservados
    5. Chamar `findAlternativeBale(stock, reserva.sku, reserva.quantidade, reservedSet, false)` — is_cascata=false nesta phase
    6. **Se encontrou alternativo (D-21):**
       a. Inserir nova reserva: `supabaseAdmin.from('reservas').insert({ codigo_in: alt.codigo_in, sku: alt.sku, quantidade: alt.quantidade, endereco: alt.endereco, status: 'reservado', importacao_numero: reserva.importacao_numero })`
       b. Cancelar reserva original: `supabaseAdmin.from('reservas').update({ status: 'cancelado' }).eq('id', reserva_id)`
       c. Retornar 200 `{ found_alternative: true, novo_codigo_in: alt.codigo_in }`
    7. **Se NAO encontrou alternativo (D-22):**
       a. Registrar em `fardos_nao_encontrados` com campos completos (migration 00005 tornou trafego_id nullable e adicionou colunas):
         ```typescript
         await supabaseAdmin.from('fardos_nao_encontrados').insert({
           codigo_in: codigo_in,
           trafego_id: null,
           reportado_por: user.id,
           sku: reserva.sku,
           quantidade: reserva.quantidade,
           endereco: reserva.endereco,
           fardista_nome: userData.nome,
           fardista_id: user.id,
         })
         ```
       b. Cancelar reserva: `supabaseAdmin.from('reservas').update({ status: 'cancelado' }).eq('id', reserva_id)`
       c. Liberar para prateleira: atualizar `progresso` dos pedidos que usam este SKU e estao com status='aguardar_fardista' para status='pendente' (desbloqueia no modal do separador — D-22)
         ```typescript
         // Buscar pedidos que usam este SKU na mesma importacao
         const { data: pedidos } = await supabaseAdmin
           .from('pedidos')
           .select('id')
           .eq('sku', reserva.sku)
           .eq('importacao_numero', reserva.importacao_numero)

         if (pedidos && pedidos.length > 0) {
           const pedidoIds = pedidos.map(p => p.id)
           await supabaseAdmin
             .from('progresso')
             .update({ status: 'pendente' })
             .in('pedido_id', pedidoIds)
             .eq('status', 'aguardar_fardista')
         }
         ```
       d. Retornar 200 `{ found_alternative: false }`

    NAO apagar nada da planilha externa no fluxo N/E (D-22).
    Wrap `request.json()` em try/catch (padrao).
  </action>
  <verify>
    <automated>npx vitest run src/features/fardos/utils/__tests__/fardo-ne.test.ts -x && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/features/fardos/utils/fardo-ne-handler.ts contem "export function findAlternativeBale"
    - src/features/fardos/utils/fardo-ne-handler.ts contem "findOptimalCombination"
    - src/features/fardos/utils/fardo-ne-handler.ts contem "isCascata"
    - src/app/api/fardos/ne/route.ts contem "export async function POST"
    - src/app/api/fardos/ne/route.ts contem "fetchStock(true)" (forceRefresh)
    - src/app/api/fardos/ne/route.ts contem "findAlternativeBale"
    - src/app/api/fardos/ne/route.ts contem "fardos_nao_encontrados" (registro)
    - src/app/api/fardos/ne/route.ts contem "status: 'cancelado'" (cancelar reserva)
    - src/app/api/fardos/ne/route.ts contem "aguardar_fardista" (liberar prateleira)
    - npx vitest run exits 0
    - tsc --noEmit sem erros nos arquivos novos
  </acceptance_criteria>
  <done>Handler testavel findAlternativeBale, API route POST /api/fardos/ne com fluxo completo (buscar alternativo ou liberar prateleira, cancelar reserva, registrar N/E), testes passam, compila</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->API routes | Request body com reserva_id e codigo_in — input nao confiavel |
| API->Google Sheets | Leitura/escrita em planilha externa — pode mudar entre leituras |
| API->Supabase | Escrita em tabelas com dados criticos (trafego, reservas) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | /api/fardos/ok | mitigate | Verificar reserva_id existe e pertence ao usuario (ou admin/lider). Trim+lowercase no codigo_in antes de busca |
| T-06-04 | Tampering | /api/fardos/ok | mitigate | Dupla verificacao (D-17): re-ler linha antes de apagar. Prevencao de duplicata: check trafego_fardos antes de processar |
| T-06-05 | Elevation | /api/fardos/ok | mitigate | Role check: fardista so pode OK em fardos atribuidos ao seu card. Admin/lider podem operar qualquer fardo |
| T-06-06 | Denial of Service | Google Sheets API | accept | Rate limit implicito do Google. Cache de estoque 2min (stock-parser) mitiga chamadas repetidas |
| T-06-07 | Tampering | /api/fardos/ne | mitigate | Validar reserva_id existe com status='reservado' antes de operar. Nao aceitar reserva ja cancelada |
| T-06-08 | Information Disclosure | /api/fardos/ne | mitigate | Nao retornar dados internos no erro. Usar mensagens genericas |
</threat_model>

<verification>
- `npx vitest run` passa (todos os testes existentes + novos)
- `npx tsc --noEmit` compila sem erros
- API routes seguem padrao de auth da Phase 5 (createClient -> getUser -> role check)
</verification>

<success_criteria>
- POST /api/fardos/ok: busca planilha, insere trafego, dupla verificacao, apaga colunas F+
- POST /api/fardos/ne: busca alternativo ou libera prateleira, cancela reserva, registra N/E
- Handlers puros testados unitariamente
- Tratamento transacional conforme D-18 (insert antes de apagar, logar erros sem rollback)
</success_criteria>

<output>
After completion, create `.planning/phases/06-lista-de-fardos/06-02-SUMMARY.md`
</output>
