---
phase: 06-lista-de-fardos
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - app/(authenticated)/fardos/fardos-client.tsx
  - src/features/fardos/components/fardo-list.tsx
  - src/features/fardos/components/fardo-item.tsx
  - src/features/fardos/components/fardo-filters.tsx
  - src/features/fardos/components/fardo-counters.tsx
  - src/features/fardos/components/selection-bar.tsx
  - src/features/fardos/lib/fardo-pdf-generator.ts
  - src/app/api/fardos/assign/route.ts
  - src/app/api/fardos/sync/route.ts
autonomous: false
requirements: [FARD-01, FARD-04, FARD-05, FARD-06]
user_setup: []
must_haves:
  truths:
    - "Fardista ve lista plana de fardos com endereco, codigo IN, SKU, quantidade"
    - "Fardista pode clicar OK e N/E e ver feedback visual imediato"
    - "Lider ve todos os fardos com checkboxes e pode atribuir fardistas em lote"
    - "Botao Imprimir Fardos gera PDF com codigo IN, SKU, endereco, quantidade, para quem entregar"
    - "Lista atualiza em tempo real sem refresh"
    - "Contadores no header refletem estado atual"
  artifacts:
    - path: "app/(authenticated)/fardos/fardos-client.tsx"
      provides: "Client component principal da pagina de fardos"
      contains: "FardoList"
    - path: "src/features/fardos/components/fardo-list.tsx"
      provides: "Componente lista com filtros, contadores e itens"
      exports: ["FardoList"]
    - path: "src/features/fardos/components/fardo-item.tsx"
      provides: "Card individual do fardo"
      exports: ["FardoItem"]
    - path: "src/features/fardos/lib/fardo-pdf-generator.ts"
      provides: "Gerador de PDF para fardos"
      exports: ["generateFardosPdf"]
  key_links:
    - from: "app/(authenticated)/fardos/fardos-client.tsx"
      to: "src/features/fardos/hooks/use-fardos-data.ts"
      via: "useFardosData hook"
      pattern: "useFardosData"
    - from: "src/features/fardos/components/fardo-item.tsx"
      to: "src/app/api/fardos/ok/route.ts"
      via: "fetch POST /api/fardos/ok"
      pattern: "fetch.*api/fardos/ok"
---

<objective>
UI completa da tela de fardos: lista plana com filtros, contadores, botoes OK/N/E com feedback visual, selecao multipla com barra flutuante, atribuicao em lote, PDF, sincronizar estoque, e schema push. Substitui completamente o fardos-client.tsx atual.

Purpose: Entregar a interface operacional do fardista com todas as interacoes e feedback visual conforme UI-SPEC e CONTEXT.md.
Output: 7 componentes novos, 2 API routes auxiliares, PDF generator, schema push aplicado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-lista-de-fardos/06-CONTEXT.md
@.planning/phases/06-lista-de-fardos/06-RESEARCH.md
@.planning/phases/06-lista-de-fardos/06-UI-SPEC.md
@.planning/phases/06-lista-de-fardos/06-01-SUMMARY.md
@.planning/phases/06-lista-de-fardos/06-02-SUMMARY.md

<interfaces>
<!-- Contratos criados nos planos anteriores -->

From src/features/fardos/types.ts (Plan 01):
```typescript
export type FardoStatus = 'pendente' | 'encontrado' | 'nao_encontrado'
export interface FardoItem {
  reserva_id: string
  codigo_in: string
  sku: string
  quantidade: number
  endereco: string
  status: FardoStatus
  fardista_id: string | null
  fardista_nome: string | null
  card_key: string | null
  separador_nome: string | null
  importacao_numero: number
  is_cascata: boolean
}
export interface FardoFilters {
  search: string
  statusFilter: 'pendentes' | 'encontrados' | 'nao_encontrados' | 'todos'
  assignFilter: 'todos' | 'atribuidos' | 'nao_atribuidos'
  sortBy: 'endereco' | 'sku' | 'importacao'
}
export interface FardoCounters { pendentes: number; encontrados: number; nao_encontrados: number }
```

From src/features/fardos/hooks/use-fardos-data.ts (Plan 01):
```typescript
export function useFardosData(userId: string, userRole: string): {
  fardos: FardoItem[]; counters: FardoCounters; loading: boolean; error: string | null; refetch: () => Promise<void>
}
```

From src/features/cards/components/assign-modal.tsx:
```typescript
interface AssignModalProps {
  open: boolean; onOpenChange: (open: boolean) => void
  cardKey: string; filterRole: 'separador' | 'fardista'
  users: { id: string; nome: string }[]; currentUserId: string | null
  onAssign: (cardKey: string, userId: string) => void
}
```

From src/features/cards/lib/pdf-generator.ts (padrao):
```typescript
// jsPDF + autoTable pattern
import jsPDF from 'jspdf'
import autoTable from 'jspdf-autotable'
```

API routes OK/NE (Plan 02):
- POST /api/fardos/ok body: { reserva_id: string, codigo_in: string }
- POST /api/fardos/ne body: { reserva_id: string, codigo_in: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Componentes UI da lista plana + API routes auxiliares (D-01 a D-13, D-19, D-22, D-29, D-30)</name>
  <files>
    app/(authenticated)/fardos/fardos-client.tsx,
    src/features/fardos/components/fardo-list.tsx,
    src/features/fardos/components/fardo-item.tsx,
    src/features/fardos/components/fardo-filters.tsx,
    src/features/fardos/components/fardo-counters.tsx,
    src/features/fardos/components/selection-bar.tsx,
    src/app/api/fardos/assign/route.ts,
    src/app/api/fardos/sync/route.ts
  </files>
  <read_first>
    app/(authenticated)/fardos/fardos-client.tsx,
    app/(authenticated)/fardos/page.tsx,
    src/features/fardos/types.ts,
    src/features/fardos/hooks/use-fardos-data.ts,
    src/features/cards/components/assign-modal.tsx,
    src/features/cards/components/order-card.tsx,
    src/features/fardos/utils/reservation-engine.ts,
    src/app/api/cards/assign/route.ts,
    src/app/api/reservas/route.ts,
    .planning/phases/06-lista-de-fardos/06-UI-SPEC.md
  </read_first>
  <action>
    **SUBSTITUIR COMPLETAMENTE `app/(authenticated)/fardos/fardos-client.tsx`** — remover todo o conteudo Kanban atual. Per D-01: NAO usar KanbanBoard.

    **1. Criar `src/features/fardos/components/fardo-counters.tsx`** (D-09):
    - Props: `counters: FardoCounters`
    - Layout inline horizontal: "{n} pendentes | {n} encontrados | {n} N/E"
    - Numeros em font-semibold, labels em font-normal
    - Separados por "|" em text-muted-foreground
    - Usar classes do UI-SPEC: numeros 16px semibold, labels 12px regular

    **2. Criar `src/features/fardos/components/fardo-filters.tsx`** (D-06 a D-08, D-11):
    - Props: `filters: FardoFilters, onFiltersChange: (filters: FardoFilters) => void, userRole: string`
    - Busca por codigo IN (D-06): shadcn Input com Search icon (lucide), placeholder "Buscar codigo IN...", debounce 300ms via setTimeout
    - Filtro por status (D-07): Badge components como tab chips — Pendentes / Encontrados / N/E / Todos. variant=outline para nao selecionado, variant=default para selecionado
    - Filtro por atribuicao (D-08): mesma abordagem de chips — Todos / Atribuidos / Nao Atribuidos. So visivel para admin/lider
    - Ordenacao (D-11): Select dropdown com 3 opcoes — "Endereco (A-Z)" default, "SKU (A-Z)", "Importacao"
    - Mobile: filtros empilham verticalmente. Desktop: linha unica com gap-3

    **3. Criar `src/features/fardos/components/fardo-item.tsx`** (D-02, D-03, D-19, D-22, D-29, D-30):
    - Props: `fardo: FardoItem, userRole: string, selected: boolean, onSelect: (id: string) => void, onOk: (fardo: FardoItem) => void, onNe: (fardo: FardoItem) => void`
    - Card branco, rounded-lg, shadow-sm, border-l-[3px] border-blue-500 (D-02)
    - Fundo geral: condicional por status — bg-white (pendente), bg-green-50 (encontrado D-19), bg-red-50 (nao_encontrado D-22)
    - Layout horizontal flexbox, items-center, p-4 (md padding), min-h-[80px]:
      - Esquerda: Checkbox (se userRole admin/lider, D-04/D-05). aria-label="Selecionar fardo {codigo_in}". Visual 20px, tap zone 44x44px
      - Centro-esquerda (flex-1 flex flex-col gap-1):
        - SKU: text-[16px] font-semibold (D-03 "SKU em bold grande")
        - "ID: {codigo_in}" em text-[14px] text-muted-foreground (D-03 "texto menor cinza")
        - Endereco com MapPin icon (lucide) verde: text-[14px], icon h-4 w-4 text-green-500 (D-03 "icone pin verde")
        - Badge condicional: se encontrado -> Badge "Encontrado" bg-green-500 text-white. Se nao_encontrado -> Badge "Nao Encontrado" bg-red-500 text-white. aria-live="polite"
      - Centro-direita (flex flex-col items-end):
        - "CONTEM" em text-[12px] text-muted-foreground uppercase (D-03)
        - Quantidade em text-[24px] font-semibold (D-03 "numero grande bold")
      - Direita (flex flex-col gap-2):
        - Botao OK: bg-green-500 text-white, hover bg-green-600, min-h-[44px] para touch target (UIUX-02). aria-label="Marcar fardo {codigo_in} como encontrado"
        - Botao N/E: bg-red-500 text-white, hover bg-red-600, min-h-[44px]. aria-label="Marcar fardo {codigo_in} como nao encontrado"
        - Se status != 'pendente': ambos os botoes disabled, opacity-40 (D-19, D-22)
        - State local `processing: 'ok' | 'ne' | null`:
          - Ao clicar OK: set processing='ok', mostrar Loader2 spinner no botao OK, ambos disabled (D-30)
          - Ao clicar N/E: set processing='ne', spinner no N/E, ambos disabled
          - Chamar onOk/onNe. Se erro, set processing=null (revert)
    - Transicao suave de fundo: transition-colors duration-300 (D-29)
    - Separador sutil entre itens: via gap na lista pai ou Separator component

    **4. Criar `src/features/fardos/components/selection-bar.tsx`** (D-23, D-25):
    - Props: `selectedCount: number, onAssign: () => void, onPrint: () => void`
    - Barra fixa no rodape: fixed bottom-0 left-0 right-0 z-50
    - bg-primary text-primary-foreground (preto/branco no theme neutral)
    - Aparece com translate-y animacao quando selectedCount > 0, esconde quando 0
    - Layout: texto "{n} fardos selecionados" a esquerda, botoes "Imprimir" e "Atribuir" a direita
    - Botoes ghost variant em fundo escuro (text-white hover:bg-white/10)
    - Padding safe area para mobile (pb-safe ou pb-6)

    **5. Criar `src/features/fardos/components/fardo-list.tsx`** (componente orquestrador):
    - Props: `fardos: FardoItem[], counters: FardoCounters, userRole: string, userId: string, userName: string, onRefetch: () => void`
    - State interno: filters (FardoFilters), selectedIds (Set<string>)
    - Logica de filtragem:
      - search: filtrar por codigo_in.toLowerCase().includes(search.toLowerCase())
      - statusFilter: filtrar por status match (pendentes->pendente, encontrados->encontrado, nao_encontrados->nao_encontrado, todos->sem filtro)
      - assignFilter: atribuidos->fardista_id != null, nao_atribuidos->fardista_id == null, todos->sem filtro
    - Logica de ordenacao (D-11, D-13):
      - endereco: sort por endereco localeCompare (A-Z) — PADRAO (D-13)
      - sku: sort por sku localeCompare (A-Z)
      - importacao: sort por importacao_numero
    - Handler handleOk(fardo):
      - fetch POST /api/fardos/ok com { reserva_id: fardo.reserva_id, codigo_in: fardo.codigo_in }
      - Se sucesso: toast.success("Fardo {codigo_in} encontrado") — sonner (D-29)
      - Se erro: toast.error com mensagem do response
    - Handler handleNe(fardo):
      - fetch POST /api/fardos/ne com { reserva_id: fardo.reserva_id, codigo_in: fardo.codigo_in }
      - Se sucesso com found_alternative=true: toast.success("Fardo {codigo_in} substituido — novo fardo reservado")
      - Se sucesso com found_alternative=false: toast.info("Fardo {codigo_in} nao encontrado — liberado para prateleira")
      - Se erro: toast.error
    - Handler handleSelectAll (D-10): toggle todos os fardos filtrados
    - AssignModal state: reutilizar AssignModal existente com filterRole='fardista' (D-24)
    - Handler handleAssign: fetch POST /api/fardos/assign com { card_keys: [...uniqueCardKeys], user_id }
    - Handler handlePrint: chamar generateFardosPdf (D-26) — se selectedIds.size > 0, imprimir selecionados; senao imprimir todos
    - Empty state (Pitfall 6):
      - Fardista sem fardos: "Nenhum fardo atribuido. Aguarde o lider atribuir fardos para voce."
      - Lider sem dados: "Nenhum fardo reservado. Faca upload de uma planilha para gerar reservas."
    - Render: FardoCounters + FardoFilters + header buttons (Sincronizar Estoque, Selecionar Todos, Imprimir — so admin/lider per D-10/D-12) + lista de FardoItem + SelectionBar (se admin/lider)

    **6. Reescrever `app/(authenticated)/fardos/fardos-client.tsx`:**
    - Importar useFardosData, FardoList
    - Props: userId, userRole, userName (mesmas do server component)
    - Usar useFardosData(userId, userRole) para dados
    - Render FardoList com todos os props
    - Loading state: spinner centralizado "Carregando fardos..."
    - Error state: mensagem de erro

    **7. Criar `src/app/api/fardos/assign/route.ts`** (D-23, D-24):
    - POST com body: `{ card_keys: string[], user_id: string }`
    - Auth: createClient -> getUser -> role check (admin/lider only)
    - Validar target user existe e e fardista ativo
    - Para cada card_key: upsert em atribuicoes (card_key + tipo='fardista') — mesma logica do /api/cards/assign mas aceitando array de card_keys
    - Retornar 200 `{ success: true, count: N }`

    **8. Criar `src/app/api/fardos/sync/route.ts`** (D-12):
    - POST sem body
    - Auth: role check (admin/lider only)
    - Buscar importacao_numero mais recente: `supabaseAdmin.from('pedidos').select('importacao_numero').order('importacao_numero', { ascending: false }).limit(1).single()`
    - Chamar `executeReservation(importacao_numero, true)` com forceRefresh=true (reutiliza Phase 4)
    - Retornar 200 `{ success: true, result: ReservationResult }`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/(authenticated)/fardos/fardos-client.tsx NAO contem "KanbanBoard" (D-01 — substituido)
    - app/(authenticated)/fardos/fardos-client.tsx contem "useFardosData"
    - app/(authenticated)/fardos/fardos-client.tsx contem "FardoList"
    - src/features/fardos/components/fardo-item.tsx contem "border-l-[3px]" e "border-blue-500" (D-02)
    - src/features/fardos/components/fardo-item.tsx contem "CONTEM" (D-03)
    - src/features/fardos/components/fardo-item.tsx contem "MapPin" (D-03)
    - src/features/fardos/components/fardo-item.tsx contem "opacity-40" ou "opacity-[0.4]" (D-19/D-22 disabled)
    - src/features/fardos/components/fardo-item.tsx contem "Loader2" (D-30 spinner)
    - src/features/fardos/components/fardo-filters.tsx contem "Buscar codigo IN" (D-06)
    - src/features/fardos/components/fardo-counters.tsx contem "pendentes" e "encontrados" (D-09)
    - src/features/fardos/components/selection-bar.tsx contem "fardos selecionados" (D-23)
    - src/features/fardos/components/fardo-list.tsx contem "api/fardos/ok" (fetch OK)
    - src/features/fardos/components/fardo-list.tsx contem "api/fardos/ne" (fetch N/E)
    - src/features/fardos/components/fardo-list.tsx contem "toast" (feedback D-29)
    - src/features/fardos/components/fardo-list.tsx contem "Nenhum fardo" (empty state)
    - src/app/api/fardos/assign/route.ts contem "export async function POST"
    - src/app/api/fardos/assign/route.ts contem "card_keys"
    - src/app/api/fardos/sync/route.ts contem "executeReservation"
    - tsc --noEmit sem erros
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>Lista plana de fardos funcional com filtros, contadores, botoes OK/N/E com spinner, selecao multipla, barra flutuante, assign route, sync route. Compilacao limpa, testes passam.</done>
</task>

<task type="auto">
  <name>Task 2: PDF generator + schema push + regenerar tipos</name>
  <files>
    src/features/fardos/lib/fardo-pdf-generator.ts,
    src/types/database.types.ts
  </files>
  <read_first>
    src/features/fardos/types.ts,
    src/features/cards/lib/pdf-generator.ts,
    .planning/phases/06-lista-de-fardos/06-UI-SPEC.md,
    supabase/migrations/00005_trafego_fardos_campos.sql
  </read_first>
  <action>
    **1. Criar `src/features/fardos/lib/fardo-pdf-generator.ts`** (D-26, D-27, D-28):

    ```typescript
    import jsPDF from 'jspdf'
    import autoTable from 'jspdf-autotable'
    import type { FardoItem } from '../types'

    /**
     * Gera PDF com lista de fardos (D-26, D-27).
     * Se fardos selecionados, imprime selecionados. Se vazio, imprime todos.
     * Conteudo: Codigo IN, SKU, Endereco, Qtd, Entregar para (nome separador ou "---")
     */
    export function generateFardosPdf(fardos: FardoItem[]): void {
      const doc = new jsPDF()

      // Titulo (UI-SPEC: 14pt, top-left, y=20mm)
      doc.setFontSize(14)
      doc.text('Lista de Fardos', 14, 20)

      // Tabela (UI-SPEC: y=30mm, colunas com proporções definidas)
      autoTable(doc, {
        startY: 30,
        head: [['Codigo IN', 'SKU', 'Endereco', 'Qtd', 'Entregar para']],
        body: fardos.map(f => [
          f.codigo_in,
          f.sku,
          f.endereco ?? '',
          String(f.quantidade),
          f.separador_nome ?? '---',  // D-27: nome do separador ou traco
        ]),
        styles: { fontSize: 10 },
        columnStyles: {
          0: { cellWidth: 45 },   // Codigo IN ~25%
          1: { cellWidth: 45 },   // SKU ~25%
          2: { cellWidth: 35 },   // Endereco ~20%
          3: { cellWidth: 18 },   // Qtd ~10%
          4: { cellWidth: 37 },   // Entregar para ~20%
        },
      })

      doc.save('fardos.pdf')
    }
    ```

    **2. Aplicar schema push** (BLOCKING):
    - Executar `npx supabase db push` para aplicar migration 00005_trafego_fardos_campos.sql
    - Se requer confirmacao interativa, usar `echo "y" | npx supabase db push` ou `SUPABASE_ACCESS_TOKEN` env var
    - Verificar sucesso: sem erros no output

    **3. Regenerar database.types.ts**:
    - Executar `npx supabase gen types typescript --linked > src/types/database.types.ts`
    - Verificar que trafego_fardos agora tem colunas novas (prioridade, prateleira, posicao, etc.)

    **4. Verificar compilacao apos regenerar tipos**:
    - `npx tsc --noEmit` deve passar
    - Se houver erros de tipo por colunas novas, ajustar imports/usos conforme necessario
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/features/fardos/lib/fardo-pdf-generator.ts contem "export function generateFardosPdf"
    - src/features/fardos/lib/fardo-pdf-generator.ts contem "Lista de Fardos" (titulo PDF)
    - src/features/fardos/lib/fardo-pdf-generator.ts contem "Entregar para" (coluna D-27)
    - src/features/fardos/lib/fardo-pdf-generator.ts contem "separador_nome" (D-27)
    - src/features/fardos/lib/fardo-pdf-generator.ts contem "---" (fallback quando sem separador)
    - src/types/database.types.ts contem "prioridade" (campo novo da migration)
    - src/types/database.types.ts contem "clicked_at" (campo novo da migration)
    - tsc --noEmit sem erros
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>PDF generator funcional, schema push aplicado, tipos regenerados com colunas novas, compilacao limpa</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao visual da tela de fardos</name>
  <what-built>
    Tela completa de Lista de Fardos substituindo o KanbanBoard anterior:
    - Lista plana de fardos com card branco, borda azul, SKU bold, endereco com pin verde, quantidade "CONTEM"
    - Botoes OK (verde) e N/E (vermelho) com spinner durante processamento
    - Filtros: busca por codigo IN, status (Pendentes/Encontrados/N/E/Todos), atribuicao, ordenacao
    - Contadores no header: X pendentes | Y encontrados | Z N/E
    - Checkboxes de selecao (lider/admin), barra flutuante com Atribuir e Imprimir
    - Botao Sincronizar Estoque e Selecionar Todos (lider/admin)
    - PDF com codigo IN, SKU, endereco, quantidade, "para quem entregar"
    - Fardo OK: fundo verde claro, badge "Encontrado", botoes desabilitados
    - Fardo N/E: fundo vermelho claro, badge "Nao Encontrado", botoes desabilitados
    - Realtime: mudancas refletem automaticamente
  </what-built>
  <how-to-verify>
    1. Acesse /fardos como lider — deve ver lista plana de fardos (NAO kanban)
    2. Verifique layout: cards brancos com borda azul esquerda, SKU grande, ID cinza, pin verde no endereco, "CONTEM" com numero grande
    3. Verifique filtros: busca, tabs de status, dropdown de ordenacao
    4. Verifique contadores no header: pendentes, encontrados, N/E
    5. Clique OK em um fardo: spinner aparece, fundo fica verde claro, badge "Encontrado", botoes desabilitados
    6. Clique N/E em outro fardo: spinner, fundo vermelho claro, badge "Nao Encontrado"
    7. Selecione checkboxes: barra flutuante aparece no rodape com "X fardos selecionados"
    8. Teste Imprimir: PDF abre com tabela de fardos
    9. Acesse /fardos como fardista: deve ver apenas fardos atribuidos, sem checkboxes
    10. Verifique responsividade mobile: filtros empilhados, botoes com touch target adequado
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <action>Verificacao visual humana da tela de fardos completa</action>
  <verify>Aprovacao do usuario</verify>
  <done>Tela de fardos aprovada visualmente pelo usuario</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->API fardos/assign | Lider/admin atribui fardistas — precisa validar role e target user |
| client->API fardos/sync | Lider/admin sincroniza estoque — executa operacao pesada (Google Sheets + subset sum) |
| client->PDF | PDF gerado client-side — dados ja filtrados por role no hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Elevation | /api/fardos/assign | mitigate | Role check: apenas admin/lider podem atribuir. Validar que target user e fardista ativo |
| T-06-10 | Denial of Service | /api/fardos/sync | mitigate | Rate limit implicito: estoque tem cache 2min. Sync so disponivel para admin/lider |
| T-06-11 | Information Disclosure | fardo-list.tsx | mitigate | Filtro por role no hook: fardista so ve dados de seus cards. PDF usa mesmos dados filtrados |
| T-06-12 | Spoofing | /api/fardos/assign | mitigate | Auth via getUser() + DB lookup. Nao confiar em role do body — buscar do banco |
</threat_model>

<verification>
- `npx tsc --noEmit` compila sem erros
- `npx vitest run` todos os testes passam
- Schema push aplicado com sucesso
- database.types.ts regenerado com colunas novas
- Verificacao visual humana da tela de fardos
</verification>

<success_criteria>
- Tela de fardos exibe lista plana (NAO kanban) com todos os dados por fardo
- OK e N/E funcionam com feedback visual (spinner, background, badge, toast)
- Filtros e contadores atualizam corretamente
- Selecao multipla + barra flutuante + atribuicao em lote funcionam para lider/admin
- PDF gera com 5 colunas corretas incluindo "Entregar para"
- Sincronizar Estoque executa re-reserva
- Fardista ve apenas seus fardos, sem checkboxes
- Schema push aplicado, tipos regenerados
</success_criteria>

<output>
After completion, create `.planning/phases/06-lista-de-fardos/06-03-SUMMARY.md`
</output>
