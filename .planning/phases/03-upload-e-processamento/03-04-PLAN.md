---
phase: 03-upload-e-processamento
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - app/(authenticated)/upload/page.tsx
  - src/features/upload/components/upload-client.tsx
autonomous: false
requirements: [UPLD-01, UPLD-09]

must_haves:
  truths:
    - "Pagina /upload exibe DropZone funcional com drag-and-drop"
    - "Fluxo completo funciona: arrastar .xlsx -> preview -> confirmar -> pedidos no banco"
    - "Lista de importacoes do dia aparece abaixo do drop zone"
    - "Desfazer ultima importacao remove pedidos do banco"
    - "Virada de dia mostra toast informativo e limpa pedidos anteriores"
  artifacts:
    - path: "app/(authenticated)/upload/page.tsx"
      provides: "Pagina de upload completa com todos os componentes"
      min_lines: 30
    - path: "src/features/upload/components/upload-client.tsx"
      provides: "Client wrapper conectando todos os componentes via useUpload"
      min_lines: 20
  key_links:
    - from: "app/(authenticated)/upload/page.tsx"
      to: "src/features/upload/components/upload-client.tsx"
      via: "import { UploadClient }"
      pattern: "UploadClient"
    - from: "src/features/upload/components/upload-client.tsx"
      to: "app/api/upload/route.ts"
      via: "useUpload -> fetch POST /api/upload"
      pattern: "api/upload"
---

<objective>
Substituir a pagina placeholder de upload pela implementacao real, conectando todos os componentes (DropZone, ImportPreview, ImportList) com o hook useUpload e os Route Handlers. Verificar fluxo completo end-to-end.

Purpose: Pagina funcional pronta para uso pelo lider/admin.
Output: Pagina /upload substituida e checkpoint de verificacao visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-upload-e-processamento/03-CONTEXT.md
@.planning/phases/03-upload-e-processamento/03-UI-SPEC.md

<interfaces>
<!-- Componentes criados nos Plans 02 e 03 -->

From src/features/upload/hooks/use-upload.ts:
```typescript
export function useUpload(initialImports?: ImportRecord[]): {
  step: UploadStep
  file: File | null
  parseResult: ParseResult | null
  summary: ImportSummary | null
  importacao_numero: number | null
  dayReset: boolean
  error: string | null
  imports: ImportRecord[]
  inputRef: React.RefObject<HTMLInputElement>
  handleDrop: (e: React.DragEvent) => void
  handleDragOver: (e: React.DragEvent) => void
  handleFileSelect: (e: React.ChangeEvent<HTMLInputElement>) => void
  handleProcess: () => void
  handleConfirm: () => void
  handleUndo: () => void
  handleReset: () => void
}
```

From src/features/upload/components/:
- DropZone: aceita props do useUpload
- ImportPreview: aceita parseResult, summary, importacao_numero, step, onConfirm, onReset
- ImportList: aceita imports, onUndo, isProcessing
- ProcessingSpinner: aceita text, visible
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir pagina placeholder por implementacao real</name>
  <files>app/(authenticated)/upload/page.tsx, src/features/upload/components/upload-client.tsx</files>
  <read_first>
    - app/(authenticated)/upload/page.tsx (placeholder atual a ser substituido)
    - app/(authenticated)/layout.tsx (entender como layout passa props)
    - src/features/upload/hooks/use-upload.ts (API do hook)
    - src/features/upload/components/drop-zone.tsx (props esperadas)
    - src/features/upload/components/import-preview.tsx (props esperadas)
    - src/features/upload/components/import-list.tsx (props esperadas)
    - src/features/upload/components/processing-spinner.tsx (props esperadas)
    - .planning/phases/03-upload-e-processamento/03-UI-SPEC.md (layout desktop/mobile, breakpoints)
  </read_first>
  <action>
Substituir app/(authenticated)/upload/page.tsx completamente:

1. **Estrutura da pagina:**
   A pagina precisa de um client component wrapper porque usa hooks (useUpload). Usar padrao server component que carrega dados iniciais (importacoes do dia) e passa como prop para um UploadClient component.

   ```typescript
   // app/(authenticated)/upload/page.tsx
   import type { Metadata } from 'next'
   import { createClient } from '@/lib/supabase/server'
   import { UploadClient } from '@/features/upload/components/upload-client'

   export const metadata: Metadata = {
     title: 'Upload | NARAKA SEP v2',
   }

   export default async function UploadPage() {
     // Carregar importacoes do dia para estado inicial
     const supabase = await createClient()
     const today = new Date().toLocaleDateString('en-CA', { timeZone: 'America/Sao_Paulo' })

     // Buscar pedidos agrupados por importacao_numero para montar lista de importacoes
     const { data: pedidos } = await supabase
       .from('pedidos')
       .select('importacao_numero, tipo, grupo_envio, created_at')
       .eq('importacao_data', today)
       .order('importacao_numero', { ascending: false })

     // Agrupar por importacao_numero para montar ImportRecord[]
     const importMap = new Map<number, { tipos: Record<string, number>, grupos: Record<string, number>, horario: string, total: number }>()

     for (const p of pedidos ?? []) {
       const existing = importMap.get(p.importacao_numero) ?? { tipos: {}, grupos: {}, horario: p.created_at, total: 0 }
       existing.tipos[p.tipo] = (existing.tipos[p.tipo] ?? 0) + 1
       existing.grupos[p.grupo_envio] = (existing.grupos[p.grupo_envio] ?? 0) + 1
       existing.total++
       importMap.set(p.importacao_numero, existing)
     }

     const initialImports = Array.from(importMap.entries()).map(([num, data]) => ({
       importacao_numero: num,
       horario: data.horario,
       total_pedidos: data.total,
       por_tipo: data.tipos as Record<'unitario' | 'kit' | 'combo', number>,
       por_grupo: data.grupos,
     }))

     return <UploadClient initialImports={initialImports} />
   }
   ```

2. **Criar src/features/upload/components/upload-client.tsx:**
   - 'use client'
   - Importa useUpload, DropZone, ImportPreview, ImportList, ProcessingSpinner, Separator
   - Layout: heading "Upload" (h1, text-xl semibold) + DropZone + ImportPreview (condicional: step === 'preview' ou step === 'success') + Separator + ImportList
   - Espacamento: gap-6 (lg da spacing scale) entre secoes
   - max-w-2xl mx-auto como wrapper geral
   - ProcessingSpinner como overlay quando step === 'confirming'
   - Toasts via sonner sao disparados pelo hook useUpload internamente (nao pelo upload-client)
   - Props: initialImports: ImportRecord[]
  </action>
  <verify>
    <automated>cd /Users/gabrielcortina/Desktop/naraka-sep-v2 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - app/(authenticated)/upload/page.tsx contem "UploadClient" (importa componente client)
    - app/(authenticated)/upload/page.tsx contem "createClient" (busca dados server-side)
    - app/(authenticated)/upload/page.tsx contem "America/Sao_Paulo"
    - app/(authenticated)/upload/page.tsx NAO contem "Fase 3" ou "sera implementado" (placeholder removido)
    - src/features/upload/components/upload-client.tsx contem "'use client'"
    - src/features/upload/components/upload-client.tsx contem "useUpload"
    - src/features/upload/components/upload-client.tsx contem "DropZone"
    - src/features/upload/components/upload-client.tsx contem "ImportPreview"
    - src/features/upload/components/upload-client.tsx contem "ImportList"
    - npm run build passa sem erros
  </acceptance_criteria>
  <done>Pagina /upload substituida: server component carrega importacoes do dia, client component conecta DropZone + ImportPreview + ImportList via useUpload. Build passa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificacao visual e funcional do fluxo de upload</name>
  <files>app/(authenticated)/upload/page.tsx</files>
  <action>
Verificacao humana do fluxo completo de upload. Nenhuma alteracao de codigo necessaria — apenas validacao visual e funcional do que foi construido nos 4 planos desta fase.
  </action>
  <verify>
    <automated>cd /Users/gabrielcortina/Desktop/naraka-sep-v2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <what-built>
    Fluxo completo de upload de planilha .xlsx:
    1. Pagina /upload com drag-and-drop zone
    2. Parse de arquivo .xlsx com SheetJS no frontend
    3. Preview com resumo numerico antes de confirmar
    4. Processamento server-side com classificacao, deduplicacao e persistencia
    5. Lista de importacoes do dia com botao Desfazer
    6. Virada de dia automatica
  </what-built>
  <how-to-verify>
    **Pre-requisito:** Ter um arquivo .xlsx do ERP UpSeller com colunas corretas (ou criar um de teste com as 13 colunas).

    1. **Abrir http://localhost:3000/upload** (fazer login como Lider ou Admin)
    2. **Drop zone visivel:** Card com icone de upload, texto "Arraste o arquivo .xlsx aqui ou", botao "Selecionar arquivo"
    3. **Arrastar arquivo .xlsx:** Borda muda de cor ao passar sobre a drop zone. Ao soltar: mostra nome do arquivo + tamanho + botao "Processar planilha"
    4. **Clicar "Processar planilha":** Breve loading, depois aparece card de resumo com:
       - Total de pedidos validos
       - Filtrados por status
       - Filtrados por Full/Fulfillment
       - Botao "Confirmar Importacao"
    5. **Clicar "Confirmar Importacao":** Spinner "Importando pedidos...", depois toast de sucesso "Importacao #1 concluida — X pedidos importados"
    6. **Lista de importacoes:** Abaixo do drop zone, item "#1" aparece com horario e contagem
    7. **Segunda importacao:** Repetir upload do mesmo arquivo. Verificar que duplicados sao ignorados (toast mostra 0 pedidos ou mensagem vazia)
    8. **Desfazer:** Clicar "Desfazer" no item mais recente. Confirmacao aparece. Apos confirmar: toast "Importacao #N desfeita", item removido da lista
    9. **Mobile:** Abrir em viewport mobile (375px). Botoes devem ser full-width, layout vertical funcional
    10. **Verificar no Supabase Dashboard:** Tabela pedidos tem os registros corretos com card_key, grupo_envio, tipo, importacao_numero

    Se nao tiver arquivo .xlsx de teste, criar um no Excel/Google Sheets com as colunas:
    No de Pedido da Plataforma, No de Pedido, Plataformas, Nome da Loja no UpSeller, Estado do Pedido (usar "Em processo"), Prazo de Envio, SKU (Armazem), Quantidade de Produtos, Quantidade Mapeada, Variacao, Nome do Produto, Metodo de Envio, Etiqueta
  </how-to-verify>
  <resume-signal>Digitar "aprovado" se tudo funcionar, ou descrever problemas encontrados</resume-signal>
  <done>Fluxo completo verificado pelo usuario: drag-and-drop, parse, preview, confirmacao, persistencia, lista de importacoes, desfazer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server component -> Supabase | Query de importacoes do dia server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Information Disclosure | upload/page.tsx | mitigate | Server component usa createClient com cookies autenticados; dados so sao carregados para usuarios logados (layout verifica sessao) |
</threat_model>

<verification>
1. `npm run build` — build passa
2. Pagina /upload renderiza sem erros
3. Todos os componentes conectados: DropZone, ImportPreview, ImportList, ProcessingSpinner
4. Checkpoint humano verifica fluxo end-to-end
</verification>

<success_criteria>
- Pagina /upload funcional (nao mais placeholder)
- Fluxo drag-and-drop -> parse -> preview -> confirm -> persist funciona end-to-end
- Lista de importacoes do dia visivel e funcional
- Desfazer remove pedidos corretamente
- Copy 100% em portugues brasileiro
- Responsivo: funciona em desktop e mobile
</success_criteria>

<output>
After completion, create `.planning/phases/03-upload-e-processamento/03-04-SUMMARY.md`
</output>
