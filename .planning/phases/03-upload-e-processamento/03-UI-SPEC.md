---
phase: 3
slug: upload-e-processamento
status: draft
shadcn_initialized: true
preset: default (neutral base color)
created: 2026-04-04
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for the Upload e Processamento page. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (default style, RSC, CSS variables) |
| Preset | neutral base color, 0.5rem radius |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react |
| Font | System font stack (sans-serif) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, gap between import list items |
| md | 16px | Default element spacing, card internal padding |
| lg | 24px | Section padding, gap between drop zone and import list |
| xl | 32px | Layout gaps, page top/bottom padding |
| 2xl | 48px | Drop zone internal vertical padding |
| 3xl | 64px | Not used this phase |

Exceptions: Drop zone minimum height 200px to provide adequate drag target area. Touch target minimum 44x44px for mobile buttons.

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px (text-base) | 400 (normal) | 1.5 | Import list details, preview numbers |
| Label | 14px (text-sm) | 500 (medium) | 1.4 | Drop zone hint text, stat labels, timestamps |
| Heading | 20px (text-xl) | 600 (semibold) | 1.2 | Page title "Upload", section headings |
| Display | 28px (text-2xl) | 700 (bold) | 1.2 | Preview summary numbers (total pedidos) |

---

## Color

| Role | Token | HSL | Usage |
|------|-------|-----|-------|
| Dominant (60%) | --background | 0 0% 100% | Page background, main surface |
| Secondary (30%) | --card / --muted | 0 0% 100% / 0 0% 96.1% | Drop zone card, import list cards, preview card |
| Accent (10%) | --primary | 0 0% 9% | "Processar" button, "Confirmar Importacao" button |
| Destructive | --destructive | 0 84.2% 60.2% | "Desfazer ultima importacao" button |

Accent reserved for:
- "Processar" primary CTA button (solid fill)
- "Confirmar Importacao" confirmation button (solid fill)
- Drop zone border on drag-over state (dashed border turns primary)

Additional semantic colors for this phase:
- Drop zone idle border: --border (dashed, 2px)
- Drop zone active/drag-over: --primary (dashed, 2px)
- Drop zone accepted file indicator: --primary with 5% opacity background
- Preview stat badges: --muted background with --foreground text
- Success toast: handled by sonner defaults (green)
- Day-reset toast: handled by sonner info variant (blue)

---

## Component Inventory

### New Components (to be created in `src/features/upload/`)

| Component | Description | shadcn Base | States |
|-----------|-------------|-------------|--------|
| DropZone | Drag-and-drop area + file button | Card | idle, drag-over, file-selected, processing |
| ImportPreview | Numeric summary before confirming | Card | loading, ready, empty |
| ImportList | List of day's imports | Card | empty, populated |
| ImportListItem | Single import entry | — | default, undoable (most recent) |
| ProcessingSpinner | Overlay spinner with text | — | visible, hidden |

### Existing Components Used

| Component | From | Usage |
|-----------|------|-------|
| Button | shadcn/ui | Processar, Confirmar, Desfazer, Selecionar arquivo |
| Card, CardHeader, CardContent | shadcn/ui | Drop zone wrapper, preview, import list |
| Sonner (toast) | shadcn/ui | Success, day-reset, error, empty-result toasts |

### Components to Install (via `npx shadcn add`)

| Component | Reason |
|-----------|--------|
| progress | Preview bar showing parse progress (optional, if needed) |
| separator | Visual divider between drop zone and import list |
| badge | Type badges (Unitario, Kit, Combo) in preview and import list |
| alert | Empty-result and error inline messages |

---

## Page Layout

### Desktop (md+ / 768px+)

```
[AppShell sidebar 240px] | [Main content area p-6]
                          |
                          | [Page heading "Upload"]
                          |
                          | [DropZone card — full width, max-w-2xl, centered]
                          |   - Icon (Upload cloud) + "Arraste o arquivo .xlsx ou"
                          |   - [Selecionar arquivo] button (secondary variant)
                          |   --- after file selected ---
                          |   - File name + file size
                          |   - [Processar] button (primary)
                          |
                          | [ImportPreview card — full width, max-w-2xl, centered]
                          |   (appears after processing, before confirming)
                          |   - Summary grid: Total | Filtrados | Duplicados
                          |   - Breakdown: Unitario | Kit | Combo
                          |   - [Confirmar Importacao] button (primary)
                          |   - [Cancelar] button (ghost)
                          |
                          | [Separator]
                          |
                          | [Import list heading "Importacoes de hoje"]
                          | [ImportListItem #3 — 14:30 — 45 pedidos] [Desfazer]
                          | [ImportListItem #2 — 11:15 — 32 pedidos]
                          | [ImportListItem #1 — 09:00 — 28 pedidos]
```

### Mobile (<768px)

Same vertical stack, full width (p-4 from AppShell). Drop zone takes full viewport width minus padding. Buttons are full-width (`w-full`). Import list items stack vertically with 8px gap.

---

## Interaction States

### DropZone States

| State | Visual | Behavior |
|-------|--------|----------|
| Idle | Dashed border (--border), upload cloud icon (lucide `Upload`), hint text | Accepts drag or click |
| Drag-over | Dashed border (--primary), background primary/5, icon scales to 110% | Visual feedback for valid drag |
| Drag-invalid | Dashed border (--destructive), shake animation 300ms | Non-.xlsx file dragged |
| File selected | Solid border (--border), file icon (lucide `FileSpreadsheet`), filename + size displayed, [Processar] button appears | Ready to process |
| Processing | Spinner overlay with "Processando arquivo..." text, button disabled | SheetJS parsing in progress |

### ImportPreview States

| State | Visual | Behavior |
|-------|--------|----------|
| Loading | Skeleton placeholders for number values | Server processing |
| Ready | Summary numbers populated, buttons active | Awaiting confirmation |
| Empty result | Alert component: "Nenhum pedido novo encontrado" with details | No confirm button shown |

### Button States

| Button | Variant | Disabled When |
|--------|---------|---------------|
| Selecionar arquivo | secondary, outline | During processing |
| Processar | primary (default) | No file selected, during processing |
| Confirmar Importacao | primary (default) | During server persist |
| Cancelar | ghost | During server persist |
| Desfazer ultima importacao | destructive, outline | During any processing, not most recent |

### Loading States

| Trigger | Duration | Visual |
|---------|----------|--------|
| SheetJS parse (frontend) | <1s typically | Button shows spinner + "Processando..." |
| Server persist (Route Handler) | 1-5s | Full card overlay spinner + "Importando pedidos..." |
| Desfazer (undo) | <1s | Button shows spinner, disabled |

---

## Copywriting Contract

All copy in Brazilian Portuguese as per CLAUDE.md.

| Element | Copy |
|---------|------|
| Page title | Upload |
| Drop zone heading | Importar planilha do ERP |
| Drop zone hint (idle) | Arraste o arquivo .xlsx aqui ou |
| Drop zone file button | Selecionar arquivo |
| Drop zone file selected | {filename} ({size}) |
| Primary CTA (parse) | Processar |
| Primary CTA (confirm) | Confirmar Importacao |
| Cancel button | Cancelar |
| Processing spinner (parse) | Processando arquivo... |
| Processing spinner (persist) | Importando pedidos... |
| Preview heading | Resumo da importacao |
| Preview label: total | Pedidos validos |
| Preview label: filtered | Filtrados (Full/Fulfillment) |
| Preview label: duplicates | Ja importados |
| Preview label: unitario | Unitarios |
| Preview label: kit | Kits |
| Preview label: combo | Combos |
| Success toast | Importacao #{N} concluida — {X} pedidos importados |
| Empty state heading | Nenhum pedido novo encontrado |
| Empty state body | {X} filtrados por status, {Y} ja importados anteriormente. Verifique se o arquivo esta correto. |
| Error state (parse) | Erro ao processar arquivo — Verifique se o arquivo e um .xlsx valido do ERP UpSeller. |
| Error state (server) | Erro ao salvar importacao — Tente novamente. Se o problema persistir, contate o administrador. |
| Error state (invalid format) | Formato invalido — Selecione um arquivo .xlsx |
| Day reset toast | Virada de dia — pedidos anteriores removidos |
| Import list heading | Importacoes de hoje |
| Import list empty | Nenhuma importacao realizada hoje |
| Undo button | Desfazer ultima importacao |
| Undo confirmation | Desfazer importacao #{N}? Os {X} pedidos desta importacao serao removidos. |
| Undo success toast | Importacao #{N} desfeita |

---

## Animation & Transitions

| Element | Property | Duration | Easing | Notes |
|---------|----------|----------|--------|-------|
| Drop zone drag-over | border-color, background-color | 150ms | ease-in-out | Smooth transition on drag enter/leave |
| Drop zone icon scale | transform: scale | 200ms | ease-out | 1.0 to 1.1 on drag-over |
| Drop zone invalid shake | transform: translateX | 300ms | ease-in-out | -4px, 4px, -2px, 2px, 0 |
| Button loading spinner | opacity | 150ms | ease-in | Fade in spinner icon |
| Preview card appear | opacity, transform: translateY | 250ms | ease-out | Fade up from 8px below |
| Import list item appear | opacity, transform: translateY | 200ms | ease-out | Fade up, stagger 50ms per item |
| Toast (sonner) | default | default | default | Use sonner built-in animations |

All animations respect `prefers-reduced-motion: reduce` — disable transform animations, keep opacity only.

---

## Accessibility

| Requirement | Implementation |
|-------------|----------------|
| Drop zone keyboard access | `tabIndex={0}`, Enter/Space opens file picker, `role="button"` |
| Drop zone aria | `aria-label="Zona de upload de planilha .xlsx"` |
| File input | Hidden `<input type="file" accept=".xlsx">`, triggered by button and drop zone |
| Processing state | `aria-live="polite"` on status text, `aria-busy="true"` on form during processing |
| Button disabled states | Native `disabled` attribute (not aria-disabled), cursor-not-allowed |
| Toast announcements | Sonner handles `aria-live` regions automatically |
| Focus management | After file select, focus moves to "Processar" button. After confirm, focus returns to drop zone |
| Heading hierarchy | h1 "Upload" (page), h2 "Resumo da importacao" (preview), h2 "Importacoes de hoje" (list) |
| Contrast | All text meets 4.5:1 minimum against its background (verified by shadcn neutral palette) |
| Destructive confirmation | "Desfazer" uses `window.confirm()` or inline confirmation before executing |

---

## Responsive Breakpoints

| Breakpoint | Layout Change |
|------------|---------------|
| < 768px (mobile) | Full-width cards, p-4, buttons w-full, bottom tabs visible, no sidebar |
| >= 768px (tablet/desktop) | Sidebar 240px, p-6, cards max-w-2xl mx-auto, buttons auto-width |

This page is desktop-focused (Lider/Admin role) but must be functional on mobile for occasional use.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | badge, separator, alert, progress | not required |

No third-party registries declared for this phase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
