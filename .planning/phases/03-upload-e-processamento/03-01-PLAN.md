---
phase: 03-upload-e-processamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/features/upload/types.ts
  - src/features/upload/lib/parse-xlsx.ts
  - src/features/upload/lib/envio-groups.ts
  - src/features/upload/lib/classify.ts
  - src/features/upload/lib/__tests__/parse-xlsx.test.ts
  - src/features/upload/lib/__tests__/classify.test.ts
  - src/features/upload/lib/__tests__/envio-groups.test.ts
  - src/components/ui/badge.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/alert.tsx
autonomous: true
requirements: [UPLD-02, UPLD-03, UPLD-04, UPLD-05, UPLD-07, UPLD-10]

must_haves:
  truths:
    - "SheetJS 0.20.3 esta instalado e importavel"
    - "vitest roda e todos os testes passam"
    - "parseXlsx transforma ArrayBuffer de xlsx em ParsedRow[] filtrando status e envio"
    - "classifyOrders agrupa por numero_pedido e classifica Unitario/Kit/Combo corretamente"
    - "classifyEnvio mapeia metodos de envio para os 6 grupos com TikTok antes de Shopee Xpress"
  artifacts:
    - path: "src/features/upload/lib/parse-xlsx.ts"
      provides: "Parse xlsx com SheetJS, mapeamento 13 colunas, filtros status/envio"
      exports: ["parseXlsx", "ParsedRow", "ParseResult"]
    - path: "src/features/upload/lib/classify.ts"
      provides: "Classificacao Unitario/Kit/Combo e geracao card_key"
      exports: ["classifyOrders", "generateCardKey", "GroupedOrder"]
    - path: "src/features/upload/lib/envio-groups.ts"
      provides: "Mapeamento metodo envio para 6 grupos"
      exports: ["classifyEnvio"]
    - path: "src/features/upload/types.ts"
      provides: "Tipos locais do feature upload"
      exports: ["ImportSummary", "ImportRecord"]
    - path: "vitest.config.ts"
      provides: "Configuracao vitest com path aliases"
  key_links:
    - from: "src/features/upload/lib/parse-xlsx.ts"
      to: "xlsx"
      via: "import { read, utils } from 'xlsx'"
      pattern: "import.*from.*xlsx"
    - from: "src/features/upload/lib/classify.ts"
      to: "src/features/upload/lib/parse-xlsx.ts"
      via: "import { ParsedRow }"
      pattern: "import.*ParsedRow"
---

<objective>
Instalar dependencias (SheetJS 0.20.3, vitest, shadcn badge/separator/alert), criar infraestrutura de testes e implementar as 3 funcoes puras de logica de negocio com testes unitarios completos: parse de xlsx, classificacao de pedidos e classificacao de metodo de envio.

Purpose: Toda a logica de negocio testavel fica pronta neste plano, isolada de Supabase e UI. Os planos 02 e 03 consomem essas funcoes.
Output: 3 modulos de logica pura com testes verdes, tipos do feature, vitest configurado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-upload-e-processamento/03-CONTEXT.md
@.planning/phases/03-upload-e-processamento/03-RESEARCH.md
@.planning/phases/03-upload-e-processamento/03-VALIDATION.md

@src/types/index.ts
@src/types/database.types.ts

<interfaces>
<!-- Tipos existentes que este plano usa -->

From src/types/index.ts:
```typescript
export type TipoPedido = 'unitario' | 'kit' | 'combo'
```

From src/types/database.types.ts (tabela pedidos Insert):
```typescript
{
  card_key: string
  grupo_envio: string
  importacao_data?: string
  importacao_numero: number
  loja: string
  metodo_envio: string
  nome_produto?: string | null
  numero_pedido: string
  numero_pedido_plataforma?: string | null
  plataforma: string
  prazo_envio?: string | null
  quantidade: number
  sku: string
  tipo: string
  variacao?: string | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependencias e configurar vitest</name>
  <files>package.json, vitest.config.ts, src/components/ui/badge.tsx, src/components/ui/separator.tsx, src/components/ui/alert.tsx</files>
  <read_first>
    - package.json (ver dependencias atuais)
    - tsconfig.json (ver path aliases para vitest.config.ts)
    - src/components/ui/button.tsx (padrao shadcn existente)
  </read_first>
  <action>
1. Instalar SheetJS 0.20.3 via CDN (NAO npm registry):
   ```bash
   npm i --save https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz
   ```

2. Instalar vitest como devDependency:
   ```bash
   npm i -D vitest
   ```

3. Criar vitest.config.ts na raiz com resolve de path aliases:
   ```typescript
   import { defineConfig } from 'vitest/config'
   import path from 'path'

   export default defineConfig({
     test: {
       globals: true,
     },
     resolve: {
       alias: {
         '@': path.resolve(__dirname, './src'),
       },
     },
   })
   ```

4. Instalar componentes shadcn necessarios para a UI (Plans 03/04):
   ```bash
   npx shadcn@latest add badge separator alert
   ```

5. Adicionar script de teste no package.json:
   ```json
   "test": "vitest run",
   "test:watch": "vitest"
   ```
  </action>
  <verify>
    <automated>cd /Users/gabrielcortina/Desktop/naraka-sep-v2 && npx vitest run --reporter=verbose 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contem "xlsx" como dependencia (nao devDependency)
    - package.json contem "vitest" como devDependency
    - package.json contem script "test": "vitest run"
    - vitest.config.ts existe com alias '@' apontando para './src'
    - src/components/ui/badge.tsx existe
    - src/components/ui/separator.tsx existe
    - src/components/ui/alert.tsx existe
    - npm run build passa sem erros
  </acceptance_criteria>
  <done>SheetJS 0.20.3, vitest e shadcn badge/separator/alert instalados. vitest.config.ts criado com path aliases. Build passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar tipos do feature e funcao parseXlsx com testes</name>
  <files>src/features/upload/types.ts, src/features/upload/lib/parse-xlsx.ts, src/features/upload/lib/__tests__/parse-xlsx.test.ts</files>
  <read_first>
    - src/types/index.ts (tipos globais TipoPedido)
    - src/types/database.types.ts (schema pedidos Insert para alinhar campos)
  </read_first>
  <behavior>
    - parseXlsx recebe ArrayBuffer e retorna ParseResult com rows, filtered_status, filtered_envio, total_raw
    - Linhas com estado_pedido != "Em processo" sao filtradas (incrementa filtered_status)
    - Linhas com metodo_envio contendo "Full" ou "Fulfillment" (case-insensitive) sao filtradas (incrementa filtered_envio)
    - Colunas ERP mapeadas: 'No de Pedido da Plataforma' -> numero_pedido_plataforma, 'No de Pedido' -> numero_pedido, 'Plataformas' -> plataforma, 'Nome da Loja no UpSeller' -> loja, 'Estado do Pedido' -> estado_pedido (usado para filtro, nao no output), 'Prazo de Envio' -> prazo_envio, 'SKU (Armazem)' -> sku, 'Quantidade de Produtos' -> quantidade, 'Quantidade Mapeada' -> quantidade_mapeada (nao usado), 'Variacao' -> variacao, 'Nome do Produto' -> nome_produto, 'Metodo de Envio' -> metodo_envio, 'Etiqueta' -> etiqueta (nao usado)
    - quantidade e convertida para number (Number() || 0)
    - prazo_envio, variacao, nome_produto podem ser null
    - total_raw conta todas as linhas brutas da planilha
  </behavior>
  <action>
1. Criar src/features/upload/types.ts com tipos compartilhados do feature:
   ```typescript
   import type { TipoPedido } from '@/types'

   export interface ImportSummary {
     total_validos: number
     filtered_status: number
     filtered_envio: number
     duplicados: number
     por_tipo: Record<TipoPedido, number>
     por_grupo: Record<string, number>
   }

   export interface ImportRecord {
     importacao_numero: number
     horario: string
     total_pedidos: number
     por_tipo: Record<TipoPedido, number>
     por_grupo: Record<string, number>
   }
   ```

2. Criar src/features/upload/lib/parse-xlsx.ts com:
   - COLUMN_MAP: Record das 13 colunas ERP -> nomes internos (conforme mapeamento do RESEARCH.md Pattern 1)
   - Interface ParsedRow: { numero_pedido_plataforma, numero_pedido, plataforma, loja, prazo_envio, sku, quantidade, variacao, nome_produto, metodo_envio }
   - Interface ParseResult: { rows: ParsedRow[], filtered_status: number, filtered_envio: number, total_raw: number }
   - Funcao parseXlsx(buffer: ArrayBuffer): ParseResult
     - Usa xlsx.read(buffer, { type: 'array', cellDates: true }) para parse
     - Pega primeira sheet: workbook.Sheets[workbook.SheetNames[0]]
     - Converte com utils.sheet_to_json<Record<string, unknown>>(sheet)
     - Itera sobre linhas: mapeia colunas via COLUMN_MAP, filtra por estado_pedido === 'Em processo' (UPLD-03), filtra por /full|fulfillment/i no metodo_envio (UPLD-04)
     - Converte quantidade com Number() || 0
     - Campos opcionais (prazo_envio, variacao, nome_produto): string ou null
   - Exportar parseXlsx, ParsedRow, ParseResult

3. Criar src/features/upload/lib/__tests__/parse-xlsx.test.ts:
   - Para testar sem arquivo .xlsx real, mockar o modulo 'xlsx' com vi.mock()
   - Testes:
     a. "filtra linhas com estado diferente de Em processo" — 3 linhas, 1 com "Cancelado" -> rows.length === 2, filtered_status === 1
     b. "filtra linhas com Full ou Fulfillment no metodo de envio" — linhas com "Shopee Full", "Fulfillment Express" -> filtered_envio correto
     c. "mapeia todas as 13 colunas corretamente" — verifica que ParsedRow tem todos os campos esperados
     d. "converte quantidade para number, default 0" — linha sem quantidade -> quantidade === 0
     e. "campos opcionais sao null quando ausentes" — prazo_envio, variacao, nome_produto
     f. "total_raw conta todas as linhas brutas" — independente de filtros
  </action>
  <verify>
    <automated>cd /Users/gabrielcortina/Desktop/naraka-sep-v2 && npx vitest run src/features/upload/lib/__tests__/parse-xlsx.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/features/upload/types.ts contem "export interface ImportSummary"
    - src/features/upload/types.ts contem "export interface ImportRecord"
    - src/features/upload/lib/parse-xlsx.ts contem "export function parseXlsx"
    - src/features/upload/lib/parse-xlsx.ts contem "export interface ParsedRow"
    - src/features/upload/lib/parse-xlsx.ts contem "export interface ParseResult"
    - src/features/upload/lib/parse-xlsx.ts contem "Em processo" (filtro UPLD-03)
    - src/features/upload/lib/parse-xlsx.ts contem "/full|fulfillment/i" ou regex equivalente (filtro UPLD-04)
    - src/features/upload/lib/parse-xlsx.ts contem "cellDates: true" (prevenir datas como numeros)
    - parse-xlsx.test.ts tem pelo menos 5 testes
    - Todos os testes de parse-xlsx.test.ts passam
  </acceptance_criteria>
  <done>parseXlsx implementado com mapeamento das 13 colunas, filtros de status e envio, conversao de tipos. Todos os testes unitarios passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar classifyOrders, classifyEnvio e generateCardKey com testes</name>
  <files>src/features/upload/lib/classify.ts, src/features/upload/lib/envio-groups.ts, src/features/upload/lib/__tests__/classify.test.ts, src/features/upload/lib/__tests__/envio-groups.test.ts</files>
  <read_first>
    - src/features/upload/lib/parse-xlsx.ts (ParsedRow que sera input de classifyOrders)
    - src/types/index.ts (TipoPedido type)
  </read_first>
  <behavior>
    classifyOrders:
    - Agrupa ParsedRow[] por numero_pedido (UPLD-07)
    - 1 SKU unico + quantidade total == 1 -> 'unitario'
    - 1 SKU unico + quantidade total > 1 -> 'kit'
    - 2+ SKUs distintos -> 'combo'
    - Retorna GroupedOrder[] com { numero_pedido, items, tipo }

    classifyEnvio:
    - TikTok verificado ANTES de Shopee Xpress (D-08)
    - 6 grupos: TikTok, Shopee SPX, ML Flex, ML Coleta, Shein, Shopee Xpress
    - Fallback: 'Outro'
    - Case-insensitive

    generateCardKey:
    - Formato: "grupo|tipo|importacao_numero"
  </behavior>
  <action>
1. Criar src/features/upload/lib/classify.ts:
   ```typescript
   import type { ParsedRow } from './parse-xlsx'
   import type { TipoPedido } from '@/types'

   export interface GroupedOrder {
     numero_pedido: string
     items: ParsedRow[]
     tipo: TipoPedido
   }

   export function classifyOrders(rows: ParsedRow[]): GroupedOrder[] {
     const groups = new Map<string, ParsedRow[]>()
     for (const row of rows) {
       const existing = groups.get(row.numero_pedido) ?? []
       existing.push(row)
       groups.set(row.numero_pedido, existing)
     }

     return Array.from(groups.entries()).map(([numero_pedido, items]) => {
       const uniqueSkus = new Set(items.map(i => i.sku))
       const totalQty = items.reduce((sum, i) => sum + i.quantidade, 0)

       let tipo: TipoPedido
       if (uniqueSkus.size >= 2) {
         tipo = 'combo'
       } else if (totalQty > 1) {
         tipo = 'kit'
       } else {
         tipo = 'unitario'
       }

       return { numero_pedido, items, tipo }
     })
   }

   export function generateCardKey(grupo_envio: string, tipo: TipoPedido, importacao_numero: number): string {
     return `${grupo_envio}|${tipo}|${importacao_numero}`
   }
   ```

2. Criar src/features/upload/lib/envio-groups.ts:
   ```typescript
   const ENVIO_RULES: { pattern: RegExp; grupo: string }[] = [
     // ORDEM IMPORTA: TikTok antes de Shopee Xpress (D-08)
     { pattern: /tiktok/i, grupo: 'TikTok' },
     { pattern: /shopee.*spx|spx/i, grupo: 'Shopee SPX' },
     { pattern: /flex/i, grupo: 'ML Flex' },
     { pattern: /coleta/i, grupo: 'ML Coleta' },
     { pattern: /shein/i, grupo: 'Shein' },
     { pattern: /shopee.*xpress|xpress/i, grupo: 'Shopee Xpress' },
   ]

   export function classifyEnvio(metodoEnvio: string): string {
     for (const rule of ENVIO_RULES) {
       if (rule.pattern.test(metodoEnvio)) {
         return rule.grupo
       }
     }
     return 'Outro'
   }
   ```

3. Criar src/features/upload/lib/__tests__/classify.test.ts:
   Testes para classifyOrders:
   a. "classifica Unitario: 1 SKU, quantidade 1" — 1 linha, SKU-A, qtd 1 -> 'unitario'
   b. "classifica Kit: 1 SKU, quantidade > 1" — 1 linha, SKU-A, qtd 3 -> 'kit'
   c. "classifica Kit: multiplas linhas mesmo SKU" — 2 linhas SKU-A qtd 1 cada -> 'kit' (total > 1)
   d. "classifica Combo: 2+ SKUs diferentes" — 2 linhas, SKU-A e SKU-B -> 'combo'
   e. "agrupa linhas com mesmo numero_pedido (UPLD-07)" — 3 linhas, 2 com PED-1 e 1 com PED-2 -> 2 GroupedOrders
   f. "nao confunde Kit com Combo quando linhas duplicadas tem mesmo SKU" — 2 linhas mesmo SKU -> 'kit' nao 'combo'

   Testes para generateCardKey:
   g. "gera card_key no formato grupo|tipo|importacao_numero" — generateCardKey('TikTok', 'unitario', 1) === 'TikTok|unitario|1'

4. Criar src/features/upload/lib/__tests__/envio-groups.test.ts:
   Testes para classifyEnvio:
   a. "classifica TikTok" — "TikTok - Envio Normal" -> 'TikTok'
   b. "classifica Shopee SPX" — "Shopee SPX Express" -> 'Shopee SPX'
   c. "classifica ML Flex" — "Mercado Livre Flex" -> 'ML Flex'
   d. "classifica ML Coleta" — "Mercado Envios Coleta" -> 'ML Coleta'
   e. "classifica Shein" — "Shein Express" -> 'Shein'
   f. "classifica Shopee Xpress" — "Shopee Xpress" -> 'Shopee Xpress'
   g. "TikTok verificado ANTES de Shopee Xpress (D-08)" — "TikTok Xpress Envio" -> 'TikTok' (nao 'Shopee Xpress')
   h. "retorna Outro para metodo desconhecido" — "Correios SEDEX" -> 'Outro'
   i. "case-insensitive" — "TIKTOK envio" -> 'TikTok'
  </action>
  <verify>
    <automated>cd /Users/gabrielcortina/Desktop/naraka-sep-v2 && npx vitest run src/features/upload/lib/__tests__/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/features/upload/lib/classify.ts contem "export function classifyOrders"
    - src/features/upload/lib/classify.ts contem "export function generateCardKey"
    - src/features/upload/lib/classify.ts contem "export interface GroupedOrder"
    - src/features/upload/lib/classify.ts contem "new Set(items.map" (conta SKUs distintos, nao length)
    - src/features/upload/lib/envio-groups.ts contem "export function classifyEnvio"
    - src/features/upload/lib/envio-groups.ts tem /tiktok/i ANTES de /xpress/i no array ENVIO_RULES
    - classify.test.ts tem pelo menos 6 testes
    - envio-groups.test.ts tem pelo menos 8 testes
    - Todos os testes em src/features/upload/lib/__tests__/ passam
  </acceptance_criteria>
  <done>classifyOrders, generateCardKey e classifyEnvio implementados. Classificacao Unitario/Kit/Combo correta. TikTok verificado antes de Shopee Xpress. Todos os testes passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| xlsx file -> parseXlsx | Arquivo externo nao confiavel e parseado pelo SheetJS no frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | parse-xlsx.ts | mitigate | SheetJS parseia xlsx sem executar macros; dados sao extraidos como strings/numbers apenas. Sanitizacao de strings (trim, cast to expected types) antes de retornar ParsedRow |
| T-03-02 | Denial of Service | parse-xlsx.ts | accept | Arquivo xlsx muito grande pode travar browser; risco baixo pois volumes tipicos de armazem sao <1000 linhas. D-02 limita a 1 arquivo por vez |
</threat_model>

<verification>
1. `npx vitest run --reporter=verbose` — todos os testes passam (parse-xlsx, classify, envio-groups)
2. `npm run build` — build passa sem erros
3. SheetJS importavel: `import { read, utils } from 'xlsx'` compila
</verification>

<success_criteria>
- 3 modulos de logica pura criados e exportando funcoes corretas
- Pelo menos 19 testes unitarios passando
- vitest configurado e funcional
- SheetJS 0.20.3 instalado (nao 0.18.5)
- shadcn badge, separator e alert instalados
- Build continua passando
</success_criteria>

<output>
After completion, create `.planning/phases/03-upload-e-processamento/03-01-SUMMARY.md`
</output>
