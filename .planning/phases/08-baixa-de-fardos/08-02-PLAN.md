---
phase: 08-baixa-de-fardos
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/features/baixa/hooks/use-baixa.ts
  - src/features/baixa/components/baixa-input.tsx
  - src/features/baixa/components/baixa-modal.tsx
  - src/features/baixa/components/baixados-hoje.tsx
autonomous: true
requirements:
  - BAIX-01
  - BAIX-02
  - BAIX-03

must_haves:
  truths:
    - "Input field has auto-focus on mount, Enter triggers search, error flash on not found"
    - "Modal displays fardo details (codigo IN, SKU, quantidade with CONTEM label, endereco with pin icon) and Entregar Para list"
    - "Modal has marketplace-colored top border and Confirmar/Cancelar buttons with loading state"
    - "BAIXADOS HOJE section shows discharged fardos with collapsible behavior"
    - "Duplicate scan shows yellow toast without opening modal"
  artifacts:
    - path: "src/features/baixa/hooks/use-baixa.ts"
      provides: "Main hook managing search, confirm, state, and baixados list"
      exports: ["useBaixa"]
    - path: "src/features/baixa/components/baixa-input.tsx"
      provides: "Input field with auto-focus and Enter handler"
      exports: ["BaixaInput"]
    - path: "src/features/baixa/components/baixa-modal.tsx"
      provides: "Confirmation modal with fardo details and Entregar Para"
      exports: ["BaixaModal"]
    - path: "src/features/baixa/components/baixados-hoje.tsx"
      provides: "Collapsible section showing today's discharges"
      exports: ["BaixadosHoje"]
  key_links:
    - from: "src/features/baixa/hooks/use-baixa.ts"
      to: "/api/baixa/buscar and /api/baixa/confirmar"
      via: "fetch calls"
      pattern: "fetch.*api/baixa"
    - from: "src/features/baixa/components/baixa-input.tsx"
      to: "src/features/baixa/hooks/use-baixa.ts"
      via: "onSearch callback"
      pattern: "onSearch"
---

<objective>
Client-side components and hook for the baixa flow: input field with auto-focus and scanner support, confirmation modal with marketplace colors and Entregar Para section, BAIXADOS HOJE collapsible section, and the main useBaixa hook orchestrating the flow.

Purpose: Complete UI layer that enables the continuous scanning workflow -- scan/type code, see confirmation, confirm baixa, field clears, ready for next scan.

Output: 4 client-side files (hook + 3 components).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-baixa-de-fardos/08-CONTEXT.md
@.planning/phases/08-baixa-de-fardos/08-RESEARCH.md
@.planning/phases/08-baixa-de-fardos/08-UI-SPEC.md
@.planning/phases/08-baixa-de-fardos/08-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan consumes -->

From src/features/baixa/lib/baixa-utils.ts (created in Plan 01):
```typescript
export interface BaixaFardoResult {
  trafego_id: string
  codigo_in: string
  sku: string
  quantidade: number
  endereco: string | null
  marketplace_color: string
  entregas: EntregaInfo[]
}

export interface EntregaInfo {
  card_key: string
  grupo_envio: string
  separador_nome: string | null
}

export function getMarketplaceColor(grupoEnvio: string): string
```

From src/types/index.ts:
```typescript
export type StatusTrafego = 'pendente' | 'encontrado' | 'nao_encontrado' | 'baixado'
```

Existing shadcn components (already installed):
- `@/components/ui/input` (Input)
- `@/components/ui/dialog` (Dialog, DialogContent, DialogHeader, DialogTitle)
- `@/components/ui/button` (Button)
- `@/components/ui/collapsible` (Collapsible, CollapsibleContent, CollapsibleTrigger)
- sonner: `toast` function from 'sonner'
- lucide-react: MapPin, Camera, Loader2, ChevronDown, ChevronUp
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useBaixa hook and BaixaInput component</name>
  <files>src/features/baixa/hooks/use-baixa.ts, src/features/baixa/components/baixa-input.tsx</files>
  <read_first>
    - src/features/baixa/lib/baixa-utils.ts (BaixaFardoResult type, getMarketplaceColor)
    - src/features/cards/hooks/use-cards-realtime.ts (realtime subscription pattern)
    - src/components/ui/input.tsx (Input component API)
    - .planning/phases/08-baixa-de-fardos/08-UI-SPEC.md (BaixaInput spec: width, height, font, states)
    - .planning/phases/08-baixa-de-fardos/08-CONTEXT.md (D-01 through D-04, D-08, D-13)
  </read_first>
  <action>
    1. Create `src/features/baixa/hooks/use-baixa.ts`:
       - 'use client' at top
       - Import: useState, useCallback from 'react'
       - Import: toast from 'sonner'
       - Import: BaixaFardoResult from '../lib/baixa-utils'
       - Import: createClient from '@/lib/supabase/client'
       - State: `fardo: BaixaFardoResult | null`, `isSearching: boolean`, `isConfirming: boolean`, `modalOpen: boolean`, `baixadosHoje: BaixadoItem[]`, `hasError: boolean`
       - Define type `BaixadoItem`:
         ```typescript
         export interface BaixadoItem {
           codigo_in: string
           sku: string
           quantidade: number
           entregas: { separador_nome: string | null; card_key: string }[]
           baixado_em: string  // ISO timestamp
         }
         ```
       - `search(codigoIn: string)`:
         - Set isSearching = true, hasError = false
         - Fetch `GET /api/baixa/buscar?codigo_in=${encodeURIComponent(codigoIn)}`
         - If response.status === 409 (duplicado): toast.warning(`Fardo ${codigoIn} ja teve baixa`) per D-08. Set isSearching = false. Return { clear: true }
         - If response.status === 404: toast.error('Fardo nao encontrado no trafego') per D-04. Set hasError = true. setTimeout(() => setHasError(false), 2000). Set isSearching = false. Return { clear: true }
         - If response.ok: set fardo = data, set modalOpen = true. Set isSearching = false. Return { clear: false } (don't clear input yet)
         - On network error: toast.error('Erro de conexao'). Set isSearching = false. Return { clear: true }
       - `confirm()`:
         - Guard: if !fardo, return
         - Set isConfirming = true
         - Fetch `POST /api/baixa/confirmar` with body `{ codigo_in: fardo.codigo_in, trafego_id: fardo.trafego_id }`
         - If response.ok: toast.success(`Baixa confirmada -- ${fardo.codigo_in}`) per D-13. Add to baixadosHoje (prepend to array). Close modal. Set isConfirming = false. Return { success: true }
         - If response.status === 409: toast.warning(`Fardo ${fardo.codigo_in} ja teve baixa`). Close modal. Set isConfirming = false. Return { success: false }
         - On error: toast.error('Erro ao confirmar baixa'). Set isConfirming = false. Return { success: false }
       - `cancel()`: Set modalOpen = false, fardo = null
       - `loadBaixadosHoje()`:
         - Called on mount. Query supabase client: `from('baixados').select('codigo_in, baixado_em, trafego_fardos(sku, quantidade, endereco)').order('baixado_em', { ascending: false })`
         - Note: baixados has trafego_id FK -> trafego_fardos. But we need entregas info too. For simplicity, store minimal data in baixadosHoje from the confirm response and supplement with the initial load. For initial load, just show codigo_in, sku, quantidade, baixado_em. Entregas can be empty for historical items (they were already delivered)
       - Realtime subscription for baixados table: listen for INSERT events to refresh the list (when another fardista does a baixa). Use same pattern as useCardsRealtime but on a 'baixa-realtime' channel listening to baixados and trafego_fardos tables. On change, call loadBaixadosHoje()
       - Export hook returning: `{ fardo, isSearching, isConfirming, modalOpen, hasError, baixadosHoje, search, confirm, cancel }`

    2. Create `src/features/baixa/components/baixa-input.tsx`:
       - 'use client' at top
       - Props: `{ onSearch: (codigo: string) => void; hasError: boolean; disabled: boolean; inputRef: React.RefObject<HTMLInputElement> }`
       - Internal state: `value: string`
       - useEffect: auto-focus inputRef on mount (D-01)
       - onKeyDown handler: if Enter and value.trim() is non-empty, call onSearch(value.trim()), then clear value
       - Layout per UI-SPEC:
         - Outer div: `flex items-center gap-2 w-[80%] md:w-[50%]`
         - Input: ref={inputRef}, value, onChange, onKeyDown, placeholder="Escanear ou digitar codigo IN", className includes `text-xl h-14`, plus `border-red-500` when hasError (D-04)
         - Camera icon button: 40px square, Camera icon from lucide-react, aria-label="Escanear com camera". onClick prop: `onCameraClick` (passed from parent, optional)
         - disabled prop: both input and button disabled when disabled=true (during modal open or search)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/features/baixa/hooks/use-baixa.ts contains "export function useBaixa" or "export const useBaixa"
    - src/features/baixa/hooks/use-baixa.ts contains "api/baixa/buscar"
    - src/features/baixa/hooks/use-baixa.ts contains "api/baixa/confirmar"
    - src/features/baixa/hooks/use-baixa.ts contains "toast.warning" (D-08 duplicate)
    - src/features/baixa/hooks/use-baixa.ts contains "toast.error" (D-04 not found)
    - src/features/baixa/hooks/use-baixa.ts contains "toast.success" (D-13 confirmed)
    - src/features/baixa/hooks/use-baixa.ts contains "baixa-realtime" (channel name)
    - src/features/baixa/components/baixa-input.tsx contains "Escanear ou digitar codigo IN"
    - src/features/baixa/components/baixa-input.tsx contains "text-xl h-14"
    - src/features/baixa/components/baixa-input.tsx contains "border-red-500"
    - src/features/baixa/components/baixa-input.tsx contains "aria-label"
    - tsc --noEmit exits 0 or only pre-existing errors
  </acceptance_criteria>
  <done>useBaixa hook manages full search/confirm/cancel flow with toast notifications per D-04/D-08/D-13. BaixaInput renders centered input with auto-focus, Enter trigger, error flash, and camera button.</done>
</task>

<task type="auto">
  <name>Task 2: BaixaModal confirmation component</name>
  <files>src/features/baixa/components/baixa-modal.tsx</files>
  <read_first>
    - src/components/ui/dialog.tsx (Dialog API -- DialogContent, DialogHeader, DialogTitle, DialogDescription)
    - src/components/ui/button.tsx (Button API, variants)
    - src/features/baixa/lib/baixa-utils.ts (BaixaFardoResult, EntregaInfo types)
    - .planning/phases/08-baixa-de-fardos/08-UI-SPEC.md (BaixaModal spec, Entregar Para spec, Modal Action Buttons spec)
    - .planning/phases/08-baixa-de-fardos/08-CONTEXT.md (D-05 through D-12)
  </read_first>
  <action>
    Create `src/features/baixa/components/baixa-modal.tsx`:
    - 'use client' at top
    - Props: `{ fardo: BaixaFardoResult | null; open: boolean; isConfirming: boolean; onConfirm: () => void; onCancel: () => void }`
    - Use shadcn Dialog with open/onOpenChange props. onOpenChange calls onCancel when closing
    - DialogContent: add inline style for border-top: `style={{ borderTop: \`4px solid ${fardo?.marketplace_color || '#000'}\` }}` per D-06. Max width 420px: className `max-w-[420px] sm:max-w-[420px]`
    - Content layout (top to bottom per UI-SPEC Modal Structure):
      1. **Codigo IN**: `<p className="text-xl font-semibold">{fardo.codigo_in}</p>`
      2. **SKU**: `<p className="text-sm text-muted-foreground">{fardo.sku}</p>`
      3. **Endereco**: `<div className="flex items-center gap-1 text-sm"><MapPin className="h-4 w-4 text-green-500" />{fardo.endereco || 'Sem endereco'}</div>` per D-05 (pin verde)
      4. **Quantidade**: `<div className="mt-4"><p className="text-xs font-semibold uppercase text-muted-foreground">CONTEM</p><p className="text-2xl font-semibold">{fardo.quantidade}</p></div>` per D-05 (CONTEM label + numero grande 28px)
      5. **Entregar Para section**: `<div className="mt-4"><p className="text-xs font-semibold text-muted-foreground">Entregar para:</p><div className="mt-2 max-h-[200px] overflow-y-auto space-y-0">` per D-09, D-12
         - Map over fardo.entregas. For each entrega:
           ```tsx
           <div
             key={entrega.card_key}
             className="flex flex-col py-2 pl-4 border-b"
             style={{ borderLeft: `3px solid ${getMarketplaceColor(entrega.grupo_envio)}` }}
           >
             <span className="text-sm font-semibold">
               {entrega.separador_nome || <span className="italic">Nao atribuido</span>}
             </span>
             <span className="text-xs text-muted-foreground">{entrega.card_key}</span>
           </div>
           ```
         - Import getMarketplaceColor from '../lib/baixa-utils' for per-line marketplace colors per D-09
         - If entregas is empty: show `<p className="text-sm text-muted-foreground italic py-2">Nenhum separador vinculado</p>`
      6. **Action buttons** (stacked per UI-SPEC):
         - Confirmar Baixa: `<Button onClick={onConfirm} disabled={isConfirming} className="w-full h-12 bg-green-500 hover:bg-green-600 text-white font-semibold">{isConfirming ? <><Loader2 className="mr-2 h-4 w-4 animate-spin" />Confirmando...</> : 'Confirmar Baixa'}</Button>`
         - Cancelar: `<Button variant="outline" onClick={onCancel} disabled={isConfirming} className="w-full h-11 opacity-50-when-confirming">{Cancelar}</Button>` per D-07
         - Gap between buttons: 8px (gap-2)
    - Use DialogDescription (visually hidden) with sr-only text to satisfy accessibility
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/features/baixa/components/baixa-modal.tsx contains "export function BaixaModal" or "export default function BaixaModal"
    - src/features/baixa/components/baixa-modal.tsx contains "borderTop" (marketplace color border)
    - src/features/baixa/components/baixa-modal.tsx contains "CONTEM" (quantidade label)
    - src/features/baixa/components/baixa-modal.tsx contains "Entregar para" (section header)
    - src/features/baixa/components/baixa-modal.tsx contains "Nao atribuido" (D-11 fallback)
    - src/features/baixa/components/baixa-modal.tsx contains "Confirmar Baixa" (primary CTA)
    - src/features/baixa/components/baixa-modal.tsx contains "Cancelar" (secondary action)
    - src/features/baixa/components/baixa-modal.tsx contains "Loader2" (spinner during confirm)
    - src/features/baixa/components/baixa-modal.tsx contains "MapPin" (endereco icon)
    - src/features/baixa/components/baixa-modal.tsx contains "getMarketplaceColor" (per-line colors)
    - src/features/baixa/components/baixa-modal.tsx contains "max-h-[200px]" (scroll for many entries)
    - tsc --noEmit exits 0 or only pre-existing errors
  </acceptance_criteria>
  <done>BaixaModal renders confirmation popup with marketplace-colored top border, fardo details (codigo IN, SKU, endereco with green pin, CONTEM + quantidade), Entregar Para list with per-line marketplace borders, and Confirmar/Cancelar buttons with loading state.</done>
</task>

<task type="auto">
  <name>Task 3: BaixadosHoje collapsible section</name>
  <files>src/features/baixa/components/baixados-hoje.tsx</files>
  <read_first>
    - src/components/ui/collapsible.tsx (Collapsible API)
    - src/features/baixa/hooks/use-baixa.ts (BaixadoItem type -- created in Task 1)
    - .planning/phases/08-baixa-de-fardos/08-UI-SPEC.md (BaixadosHoje Section spec, BaixadosHoje List Items spec)
    - .planning/phases/08-baixa-de-fardos/08-CONTEXT.md (D-14, D-15)
  </read_first>
  <action>
    Create `src/features/baixa/components/baixados-hoje.tsx`:
    - 'use client' at top
    - Props: `{ items: BaixadoItem[] }` where BaixadoItem is imported from '../hooks/use-baixa'
    - Use shadcn Collapsible (Collapsible, CollapsibleTrigger, CollapsibleContent)
    - State: `isOpen: boolean`, initialized to false
    - useEffect: when items.length goes from 0 to >= 1, set isOpen = true (D-15 auto-expand on first baixa)
    - Header (CollapsibleTrigger): `<button className="flex items-center justify-between w-full px-4 py-3 text-xs font-semibold uppercase">BAIXADOS HOJE ({items.length}){isOpen ? <ChevronUp className="h-4 w-4" /> : <ChevronDown className="h-4 w-4" />}</button>`
    - Wrapper: `<div className="w-[80%] md:w-[50%] bg-secondary rounded-lg overflow-hidden">` per UI-SPEC (same width as input, secondary background, 8px radius)
    - CollapsibleContent contains list of items:
      - Map over items. For each item:
        - Mobile layout (md:hidden): two lines
          - Line 1: `{item.codigo_in} | {item.sku} | {item.quantidade}`
          - Line 2: `Para: {entregas names or 'N/A'} | {formatted time HH:MM}`
        - Desktop layout (hidden md:block): single row with columns
          - codigo_in | sku | quantidade | separador(es) | horario
        - Font: `text-sm` for data, `text-xs text-muted-foreground` for timestamps
        - Border bottom between items: `border-b last:border-b-0`
        - Padding: `py-2 px-4`
    - Format time: extract HH:MM from baixado_em ISO string. Use `new Date(item.baixado_em).toLocaleTimeString('pt-BR', { hour: '2-digit', minute: '2-digit' })`
    - Entregas display: join separador_nome values with ', '. If separador_nome is null, show 'N/A'
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/features/baixa/components/baixados-hoje.tsx contains "export function BaixadosHoje"
    - src/features/baixa/components/baixados-hoje.tsx contains "BAIXADOS HOJE"
    - src/features/baixa/components/baixados-hoje.tsx contains "Collapsible"
    - src/features/baixa/components/baixados-hoje.tsx contains "ChevronUp" or "ChevronDown"
    - src/features/baixa/components/baixados-hoje.tsx contains "bg-secondary"
    - src/features/baixa/components/baixados-hoje.tsx contains "w-[80%] md:w-[50%]"
    - src/features/baixa/components/baixados-hoje.tsx contains "toLocaleTimeString"
    - tsc --noEmit exits 0 or only pre-existing errors
  </acceptance_criteria>
  <done>BaixadosHoje renders collapsible section with counter in header, auto-expands on first baixa, shows list of discharged fardos with responsive mobile/desktop layout, timestamps in HH:MM format.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> fetch calls | codigo_in from input field passed to API |
| API response -> component rendering | Data from API rendered in modal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | BaixaInput | mitigate | Input value trimmed before sending to API. No HTML rendering of user input (React escapes by default) |
| T-08-07 | Information Disclosure | useBaixa | accept | Baixados list shows all today's discharges (appropriate for warehouse context -- all fardistas see all baixas) |
| T-08-08 | Denial of Service | BaixaModal entregas list | mitigate | max-h-[200px] overflow-y-auto prevents DOM explosion from many entregas entries |
</threat_model>

<verification>
1. `npx tsc --noEmit` -- no new type errors
2. All components render without crashes when fardo is null (guard in hook)
3. Toast messages match CONTEXT.md decision copy exactly
</verification>

<success_criteria>
- useBaixa hook manages full search/confirm/cancel lifecycle with correct toast messages
- BaixaInput has auto-focus, Enter trigger, error flash animation, camera button
- BaixaModal shows marketplace-colored border, CONTEM label, Entregar Para with per-line colors
- BaixadosHoje auto-expands on first baixa, shows formatted list with timestamps
</success_criteria>

<output>
After completion, create `.planning/phases/08-baixa-de-fardos/08-02-SUMMARY.md`
</output>
