---
phase: 01-infraestrutura
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tailwind.config.ts
  - next.config.mjs
  - components.json
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - src/lib/utils.ts
  - src/components/ui/.gitkeep
  - src/features/.gitkeep
  - src/types/index.ts
  - .env.example
  - .gitignore
  - .eslintrc.json
autonomous: true
requirements:
  - SETUP-01

must_haves:
  truths:
    - "Projeto Next.js 14 roda localmente com npm run dev"
    - "Tailwind CSS funciona (classes utilitarias aplicam estilos)"
    - "shadcn/ui esta inicializado e cn() utility funciona"
    - "Estrutura de pastas segue D-01/D-02/D-03 (app/ na raiz, src/ para codigo)"
  artifacts:
    - path: "package.json"
      provides: "Next.js 14 com dependencias core"
      contains: "next"
    - path: "tailwind.config.ts"
      provides: "Configuracao Tailwind v3"
      contains: "tailwindcss"
    - path: "components.json"
      provides: "Configuracao shadcn/ui"
      contains: "shadcn"
    - path: "src/lib/utils.ts"
      provides: "cn() utility do shadcn"
      contains: "cn"
    - path: ".env.example"
      provides: "Template de variaveis de ambiente"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
    - path: "src/types/index.ts"
      provides: "Arquivo base de tipos globais"
  key_links:
    - from: "app/globals.css"
      to: "tailwind.config.ts"
      via: "Tailwind directives (@tailwind base/components/utilities)"
      pattern: "@tailwind"
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Criar o projeto Next.js 14 com Tailwind CSS v3 e shadcn/ui, estabelecendo a estrutura de pastas feature-based conforme decisoes D-01, D-02, D-03 e o template .env.example conforme D-10.

Purpose: Fundacao do projeto -- todas as fases seguintes dependem desta estrutura.
Output: Projeto Next.js funcional rodando localmente com Tailwind + shadcn configurados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infraestrutura/01-CONTEXT.md
@.planning/phases/01-infraestrutura/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar projeto Next.js 14 com Tailwind v3, shadcn/ui e estrutura de pastas</name>
  <files>
    package.json, tsconfig.json, tailwind.config.ts, next.config.mjs, components.json,
    app/layout.tsx, app/page.tsx, app/globals.css, .eslintrc.json,
    src/lib/utils.ts, src/components/ui/.gitkeep, src/features/.gitkeep, src/types/index.ts
  </files>
  <read_first>
    .planning/phases/01-infraestrutura/01-RESEARCH.md (secoes Standard Stack, Architecture Patterns, Pitfall 2 e 5)
  </read_first>
  <action>
1. Criar projeto Next.js 14 SEM --src-dir (para app/ ficar na raiz, per D-02 e Pitfall 5):
   ```bash
   npx create-next-app@14 . --typescript --tailwind --eslint --app --import-alias "@/*" --use-npm
   ```
   NOTA: Se o diretorio nao estiver vazio (CLAUDE.md existe), pode ser necessario mover CLAUDE.md temporariamente ou usar --yes. Verificar o comportamento e adaptar.

2. Criar diretorio src/ manualmente e mover/criar a estrutura (per D-01, D-02, D-03):
   ```
   src/
   ├── components/
   │   └── ui/           # shadcn/ui components (D-03)
   ├── features/         # Feature-based modules (D-01)
   │   ├── auth/
   │   ├── upload/
   │   ├── fardos/
   │   ├── prateleira/
   │   ├── baixa/
   │   ├── dashboard/
   │   └── users/
   ├── lib/
   │   └── utils.ts      # cn() utility (D-03)
   └── types/
       └── index.ts      # Tipos globais (D-03)
   ```

3. Atualizar tsconfig.json para que o alias `@/*` aponte para `./src/*`:
   ```json
   {
     "compilerOptions": {
       "paths": {
         "@/*": ["./src/*"]
       }
     }
   }
   ```

4. Inicializar shadcn/ui com versao 2.3.0 (para Tailwind v3 per RESEARCH.md):
   ```bash
   npx shadcn@2.3.0 init
   ```
   Configurar durante init:
   - Style: Default
   - Base color: Neutral
   - CSS variables: yes
   - tailwind.css path: app/globals.css
   - tailwind.config path: tailwind.config.ts
   - Components alias: @/components
   - Utils alias: @/lib/utils

   IMPORTANTE: Se shadcn init perguntar interativamente, usar flags CLI para evitar prompts. Consultar `npx shadcn@2.3.0 init --help` para flags disponiveis. Se nao houver modo non-interactive, criar components.json manualmente com o conteudo correto.

5. Verificar que globals.css contem as CSS variables do shadcn (--background, --foreground, etc.) e as diretivas Tailwind (@tailwind base; @tailwind components; @tailwind utilities).

6. Verificar que tailwind.config.ts usa Tailwind v3 e inclui os content paths corretos:
   ```ts
   content: [
     "./app/**/*.{js,ts,jsx,tsx,mdx}",
     "./src/**/*.{js,ts,jsx,tsx,mdx}",
   ]
   ```

7. Criar src/lib/utils.ts com a funcao cn() do shadcn:
   ```typescript
   import { type ClassValue, clsx } from "clsx"
   import { twMerge } from "tailwind-merge"

   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs))
   }
   ```

8. Criar src/types/index.ts vazio com comentario:
   ```typescript
   // Tipos globais do NARAKA SEP v2
   // database.types.ts sera gerado automaticamente pelo Supabase CLI (D-06)
   ```

9. Criar diretorios de features com .gitkeep em cada subdiretorio:
   ```
   src/features/auth/.gitkeep
   src/features/upload/.gitkeep
   src/features/fardos/.gitkeep
   src/features/prateleira/.gitkeep
   src/features/baixa/.gitkeep
   src/features/dashboard/.gitkeep
   src/features/users/.gitkeep
   ```

10. Atualizar app/page.tsx para uma pagina simples de verificacao:
    ```tsx
    export default function Home() {
      return (
        <main className="flex min-h-screen flex-col items-center justify-center p-24">
          <h1 className="text-4xl font-bold">NARAKA | SEP v2</h1>
          <p className="mt-4 text-muted-foreground">Sistema de Separacao</p>
        </main>
      )
    }
    ```

11. Atualizar app/layout.tsx para incluir fonte Inter e metadata em portugues:
    ```tsx
    import type { Metadata } from "next"
    import { Inter } from "next/font/google"
    import "./globals.css"

    const inter = Inter({ subsets: ["latin"] })

    export const metadata: Metadata = {
      title: "NARAKA | SEP v2",
      description: "Sistema de Separacao e Gestao de Pedidos",
    }

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      return (
        <html lang="pt-BR">
          <body className={inter.className}>{children}</body>
        </html>
      )
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/gabrielcortina/Desktop/naraka-sep-v2 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next": "14 (any 14.x version)
    - package.json contains "tailwindcss"
    - tsconfig.json contains "@/*": ["./src/*"]
    - app/globals.css contains "@tailwind base"
    - app/globals.css contains "--background"
    - app/layout.tsx contains 'lang="pt-BR"'
    - app/layout.tsx contains "import.*globals.css"
    - app/page.tsx contains "NARAKA | SEP v2"
    - src/lib/utils.ts contains "export function cn"
    - src/types/index.ts exists
    - src/components/ui/ directory exists
    - src/features/auth/ directory exists
    - src/features/upload/ directory exists
    - src/features/fardos/ directory exists
    - src/features/prateleira/ directory exists
    - src/features/baixa/ directory exists
    - src/features/dashboard/ directory exists
    - src/features/users/ directory exists
    - components.json exists
    - tailwind.config.ts contains "src/**/*"
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Projeto Next.js 14 roda localmente, Tailwind v3 aplica estilos, shadcn/ui inicializado com cn() utility, estrutura de pastas feature-based completa per D-01/D-02/D-03</done>
</task>

<task type="auto">
  <name>Task 2: Criar .env.example e configurar .gitignore</name>
  <files>.env.example, .gitignore</files>
  <read_first>
    .gitignore (criado pelo create-next-app na Task 1)
    .planning/phases/01-infraestrutura/01-RESEARCH.md (secao .env.example)
  </read_first>
  <action>
1. Criar .env.example com todas as variaveis necessarias per D-10:
   ```bash
   # Supabase
   NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
   SUPABASE_SERVICE_ROLE_KEY=eyJ...

   # Google Sheets
   GOOGLE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n","client_email":"...@...iam.gserviceaccount.com"}'
   GOOGLE_SHEET_ID=1tL5as2Q0QEZCj_6Kc4xGGqPqU4T5uduHmaGmntC8W58
   ```

2. Verificar que .gitignore (criado pelo create-next-app) ja inclui .env.local. Se nao incluir, adicionar:
   ```
   # env files
   .env
   .env.local
   .env*.local
   ```

3. Garantir que .gitignore NAO ignora .env.example (template deve ser commitado).
  </action>
  <verify>
    <automated>cd /Users/gabrielcortina/Desktop/naraka-sep-v2 && cat .env.example | grep -c "NEXT_PUBLIC_SUPABASE_URL\|SUPABASE_SERVICE_ROLE_KEY\|GOOGLE_SERVICE_ACCOUNT_KEY\|GOOGLE_SHEET_ID\|NEXT_PUBLIC_SUPABASE_ANON_KEY" | grep -q "5" && echo "OK: all 5 env vars present" && grep -q ".env.local" .gitignore && echo "OK: .env.local in gitignore"</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contains "NEXT_PUBLIC_SUPABASE_URL="
    - .env.example contains "NEXT_PUBLIC_SUPABASE_ANON_KEY="
    - .env.example contains "SUPABASE_SERVICE_ROLE_KEY="
    - .env.example contains "GOOGLE_SERVICE_ACCOUNT_KEY="
    - .env.example contains "GOOGLE_SHEET_ID="
    - .gitignore contains ".env.local"
    - .env.example is NOT in .gitignore
  </acceptance_criteria>
  <done>Template .env.example com todas as 5 variaveis de ambiente per D-10, .gitignore protege .env.local</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.local -> repositorio | Credenciais nao podem ser commitadas |
| client bundle -> env vars | Apenas NEXT_PUBLIC_* vao para o client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.local | mitigate | .gitignore inclui .env.local e .env*.local; .env.example tem apenas placeholders, nunca valores reais |
| T-01-02 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Variavel NAO tem prefixo NEXT_PUBLIC_, garantindo que nunca entra no client bundle do Next.js |
</threat_model>

<verification>
- `npm run build` completa sem erros
- `npm run dev` inicia servidor em localhost:3000
- Pagina inicial exibe "NARAKA | SEP v2" com estilos Tailwind aplicados
- Estrutura de pastas corresponde a D-01/D-02/D-03
- .env.example lista todas as 5 variaveis
</verification>

<success_criteria>
Projeto Next.js 14 criado e funcional com Tailwind v3, shadcn/ui, estrutura feature-based e template de variaveis de ambiente -- pronto para receber Supabase e Google Sheets na proxima wave.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infraestrutura/01-01-SUMMARY.md`
</output>
