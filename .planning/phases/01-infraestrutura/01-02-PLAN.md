---
phase: 01-infraestrutura
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - supabase/config.toml
  - supabase/migrations/00001_initial_schema.sql
  - supabase/seed.sql
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/admin.ts
  - src/types/database.types.ts
  - src/lib/google-sheets.ts
  - app/api/sheets/route.ts
  - middleware.ts
autonomous: true
requirements:
  - SETUP-02
  - SETUP-04

must_haves:
  truths:
    - "Supabase client conecta ao projeto remoto"
    - "Todas as 9 tabelas existem no banco com RLS ativado"
    - "Tipos TypeScript refletem o schema do banco"
    - "Google Sheets API responde com dados da planilha de estoque"
    - "Credenciais Google ficam apenas no servidor (Route Handler)"
  artifacts:
    - path: "supabase/migrations/00001_initial_schema.sql"
      provides: "Schema completo com 9 tabelas, indices e RLS"
      contains: "CREATE TABLE users"
    - path: "src/lib/supabase/client.ts"
      provides: "Cliente Supabase para browser"
      contains: "createBrowserClient"
    - path: "src/lib/supabase/server.ts"
      provides: "Cliente Supabase para server components"
      contains: "createServerClient"
    - path: "src/lib/supabase/admin.ts"
      provides: "Cliente Supabase com service role"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: "src/types/database.types.ts"
      provides: "Tipos auto-gerados do schema"
      contains: "Database"
    - path: "src/lib/google-sheets.ts"
      provides: "Cliente Google Sheets API"
      contains: "getSheetData"
    - path: "app/api/sheets/route.ts"
      provides: "Route Handler para Google Sheets"
      contains: "export async function GET"
    - path: "middleware.ts"
      provides: "Middleware Supabase para refresh de tokens"
      contains: "middleware"
  key_links:
    - from: "src/lib/supabase/client.ts"
      to: "src/types/database.types.ts"
      via: "Generic type parameter"
      pattern: "Database"
    - from: "src/lib/supabase/server.ts"
      to: "src/types/database.types.ts"
      via: "Generic type parameter"
      pattern: "Database"
    - from: "app/api/sheets/route.ts"
      to: "src/lib/google-sheets.ts"
      via: "import getSheetData"
      pattern: "import.*getSheetData.*google-sheets"
    - from: "src/lib/google-sheets.ts"
      to: "GOOGLE_SERVICE_ACCOUNT_KEY env var"
      via: "JSON.parse(process.env.GOOGLE_SERVICE_ACCOUNT_KEY)"
      pattern: "GOOGLE_SERVICE_ACCOUNT_KEY"

user_setup:
  - service: supabase
    why: "Banco de dados e autenticacao"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role secret key"
    dashboard_config:
      - task: "Criar projeto Supabase (dev)"
        location: "supabase.com/dashboard -> New Project"
  - service: google-sheets
    why: "Leitura do estoque externo"
    env_vars:
      - name: GOOGLE_SERVICE_ACCOUNT_KEY
        source: "Google Cloud Console -> IAM -> Service Accounts -> Keys -> JSON"
      - name: GOOGLE_SHEET_ID
        source: "URL da planilha de estoque (ID entre /d/ e /edit)"
    dashboard_config:
      - task: "Compartilhar planilha com o email da service account"
        location: "Google Sheets -> Compartilhar -> adicionar email da service account como Editor"
---

<objective>
Configurar Supabase com schema completo (9 tabelas, RLS, indices), clientes TypeScript type-safe, e integracao Google Sheets API via Route Handler com conta de servico.

Purpose: Banco de dados e integracao com estoque externo sao pre-requisitos para todas as funcionalidades do sistema.
Output: Supabase conectado com tabelas acessiveis + Google Sheets API respondendo com dados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infraestrutura/01-CONTEXT.md
@.planning/phases/01-infraestrutura/01-RESEARCH.md
@.planning/phases/01-infraestrutura/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - paths and utilities available -->
From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From tsconfig.json:
```json
{ "compilerOptions": { "paths": { "@/*": ["./src/*"] } } }
```

From .env.example:
```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
GOOGLE_SERVICE_ACCOUNT_KEY=
GOOGLE_SHEET_ID=
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configurar Supabase — migration SQL, clientes TypeScript e middleware</name>
  <files>
    package.json, supabase/config.toml, supabase/migrations/00001_initial_schema.sql,
    supabase/seed.sql, src/lib/supabase/client.ts, src/lib/supabase/server.ts,
    src/lib/supabase/admin.ts, src/types/database.types.ts, middleware.ts
  </files>
  <read_first>
    package.json (estado atual apos Plan 01)
    .planning/phases/01-infraestrutura/01-RESEARCH.md (secoes Pattern 1, Pattern 3, Code Examples - Migration SQL)
    .planning/PROJECT.md (schema do banco)
  </read_first>
  <action>
1. Instalar dependencias Supabase:
   ```bash
   npm install @supabase/supabase-js @supabase/ssr
   ```

2. Inicializar Supabase CLI no projeto:
   ```bash
   npx supabase init
   ```
   Isto cria supabase/config.toml e supabase/migrations/.

3. Criar migration SQL completa em supabase/migrations/00001_initial_schema.sql com EXATAMENTE o seguinte conteudo (per RESEARCH.md Code Examples):

   ```sql
   -- 9 tabelas: users, config, pedidos, progresso, reservas, atribuicoes, trafego_fardos, baixados, fardos_nao_encontrados

   CREATE TABLE users (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     nome TEXT NOT NULL,
     pin_hash TEXT NOT NULL,
     role TEXT NOT NULL CHECK (role IN ('admin', 'lider', 'separador', 'fardista')),
     ativo BOOLEAN NOT NULL DEFAULT true,
     created_at TIMESTAMPTZ NOT NULL DEFAULT now()
   );

   CREATE TABLE config (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     chave TEXT NOT NULL UNIQUE,
     valor TEXT NOT NULL,
     updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
   );

   CREATE TABLE pedidos (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     numero_pedido TEXT NOT NULL,
     numero_pedido_plataforma TEXT,
     plataforma TEXT NOT NULL,
     loja TEXT NOT NULL,
     sku TEXT NOT NULL,
     quantidade INTEGER NOT NULL,
     variacao TEXT,
     nome_produto TEXT,
     metodo_envio TEXT NOT NULL,
     grupo_envio TEXT NOT NULL,
     tipo TEXT NOT NULL CHECK (tipo IN ('unitario', 'kit', 'combo')),
     importacao_numero INTEGER NOT NULL,
     importacao_data DATE NOT NULL DEFAULT CURRENT_DATE,
     card_key TEXT NOT NULL,
     prazo_envio TIMESTAMPTZ,
     created_at TIMESTAMPTZ NOT NULL DEFAULT now()
   );

   CREATE TABLE progresso (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     pedido_id UUID NOT NULL REFERENCES pedidos(id) ON DELETE CASCADE,
     quantidade_separada INTEGER NOT NULL DEFAULT 0,
     status TEXT NOT NULL DEFAULT 'pendente' CHECK (status IN ('pendente', 'parcial', 'completo', 'nao_encontrado', 'aguardar_fardista', 'transformacao')),
     updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
   );

   CREATE TABLE reservas (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     pedido_id UUID NOT NULL REFERENCES pedidos(id) ON DELETE CASCADE,
     codigo_in TEXT NOT NULL,
     sku TEXT NOT NULL,
     quantidade INTEGER NOT NULL,
     endereco TEXT,
     status TEXT NOT NULL DEFAULT 'reservado' CHECK (status IN ('reservado', 'cancelado')),
     created_at TIMESTAMPTZ NOT NULL DEFAULT now()
   );

   CREATE TABLE atribuicoes (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     card_key TEXT NOT NULL,
     user_id UUID NOT NULL REFERENCES users(id),
     tipo TEXT NOT NULL CHECK (tipo IN ('separador', 'fardista')),
     created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
     UNIQUE(card_key, tipo)
   );

   CREATE TABLE trafego_fardos (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     reserva_id UUID NOT NULL REFERENCES reservas(id) ON DELETE CASCADE,
     codigo_in TEXT NOT NULL,
     sku TEXT NOT NULL,
     quantidade INTEGER NOT NULL,
     endereco TEXT,
     status TEXT NOT NULL DEFAULT 'pendente' CHECK (status IN ('pendente', 'encontrado', 'nao_encontrado')),
     fardista_id UUID REFERENCES users(id),
     created_at TIMESTAMPTZ NOT NULL DEFAULT now()
   );

   CREATE TABLE baixados (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     trafego_id UUID NOT NULL REFERENCES trafego_fardos(id),
     codigo_in TEXT NOT NULL,
     baixado_por UUID NOT NULL REFERENCES users(id),
     baixado_em TIMESTAMPTZ NOT NULL DEFAULT now()
   );

   CREATE TABLE fardos_nao_encontrados (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     trafego_id UUID NOT NULL REFERENCES trafego_fardos(id),
     codigo_in TEXT NOT NULL,
     reportado_por UUID NOT NULL REFERENCES users(id),
     reportado_em TIMESTAMPTZ NOT NULL DEFAULT now()
   );

   -- Indices para queries frequentes
   CREATE INDEX idx_pedidos_card_key ON pedidos(card_key);
   CREATE INDEX idx_pedidos_importacao ON pedidos(importacao_data, importacao_numero);
   CREATE INDEX idx_pedidos_numero ON pedidos(numero_pedido);
   CREATE INDEX idx_progresso_pedido ON progresso(pedido_id);
   CREATE INDEX idx_reservas_codigo_in ON reservas(codigo_in);
   CREATE INDEX idx_reservas_pedido ON reservas(pedido_id);
   CREATE INDEX idx_trafego_codigo_in ON trafego_fardos(codigo_in);
   CREATE INDEX idx_atribuicoes_card ON atribuicoes(card_key);

   -- Habilitar RLS em todas as tabelas (D-05)
   ALTER TABLE users ENABLE ROW LEVEL SECURITY;
   ALTER TABLE config ENABLE ROW LEVEL SECURITY;
   ALTER TABLE pedidos ENABLE ROW LEVEL SECURITY;
   ALTER TABLE progresso ENABLE ROW LEVEL SECURITY;
   ALTER TABLE reservas ENABLE ROW LEVEL SECURITY;
   ALTER TABLE atribuicoes ENABLE ROW LEVEL SECURITY;
   ALTER TABLE trafego_fardos ENABLE ROW LEVEL SECURITY;
   ALTER TABLE baixados ENABLE ROW LEVEL SECURITY;
   ALTER TABLE fardos_nao_encontrados ENABLE ROW LEVEL SECURITY;

   -- Policies basicas: leitura autenticada (D-05)
   CREATE POLICY "Leitura autenticada" ON users FOR SELECT TO authenticated USING (true);
   CREATE POLICY "Leitura autenticada" ON config FOR SELECT TO authenticated USING (true);
   CREATE POLICY "Leitura autenticada" ON pedidos FOR SELECT TO authenticated USING (true);
   CREATE POLICY "Leitura autenticada" ON progresso FOR SELECT TO authenticated USING (true);
   CREATE POLICY "Leitura autenticada" ON reservas FOR SELECT TO authenticated USING (true);
   CREATE POLICY "Leitura autenticada" ON atribuicoes FOR SELECT TO authenticated USING (true);
   CREATE POLICY "Leitura autenticada" ON trafego_fardos FOR SELECT TO authenticated USING (true);
   CREATE POLICY "Leitura autenticada" ON baixados FOR SELECT TO authenticated USING (true);
   CREATE POLICY "Leitura autenticada" ON fardos_nao_encontrados FOR SELECT TO authenticated USING (true);
   ```

4. Criar supabase/seed.sql com usuario admin inicial:
   ```sql
   -- Seed: usuario admin para desenvolvimento
   -- PIN: 1234 -> SHA-256: 03ac674216f3e15c761ee1a5e255f067953623c8b388b4459e13f978d7c846f4
   INSERT INTO users (nome, pin_hash, role) VALUES
     ('Admin Dev', '03ac674216f3e15c761ee1a5e255f067953623c8b388b4459e13f978d7c846f4', 'admin');
   ```

5. Criar src/lib/supabase/client.ts — cliente browser (per RESEARCH.md Pattern 1):
   ```typescript
   import { createBrowserClient } from '@supabase/ssr'
   import type { Database } from '@/types/database.types'

   export function createClient() {
     return createBrowserClient<Database>(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
     )
   }
   ```

6. Criar src/lib/supabase/server.ts — cliente server (per RESEARCH.md Pattern 1):
   ```typescript
   import { createServerClient } from '@supabase/ssr'
   import { cookies } from 'next/headers'
   import type { Database } from '@/types/database.types'

   export async function createClient() {
     const cookieStore = await cookies()
     return createServerClient<Database>(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
       {
         cookies: {
           getAll() {
             return cookieStore.getAll()
           },
           setAll(cookiesToSet) {
             try {
               cookiesToSet.forEach(({ name, value, options }) =>
                 cookieStore.set(name, value, options)
               )
             } catch {
               // Server Component read-only: ignore
             }
           },
         },
       }
     )
   }
   ```

7. Criar src/lib/supabase/admin.ts — cliente service role (per RESEARCH.md Pattern 1):
   ```typescript
   import { createClient } from '@supabase/supabase-js'
   import type { Database } from '@/types/database.types'

   export const supabaseAdmin = createClient<Database>(
     process.env.NEXT_PUBLIC_SUPABASE_URL!,
     process.env.SUPABASE_SERVICE_ROLE_KEY!
   )
   ```

8. Criar src/types/database.types.ts — tipo placeholder que sera substituido por `supabase gen types`:
   ```typescript
   // Este arquivo sera substituido pelo output de:
   // npx supabase gen types typescript --project-id <PROJECT_ID> > src/types/database.types.ts
   // Por enquanto, tipo minimo para compilacao

   export type Json =
     | string
     | number
     | boolean
     | null
     | { [key: string]: Json | undefined }
     | Json[]

   export interface Database {
     public: {
       Tables: {
         users: {
           Row: {
             id: string
             nome: string
             pin_hash: string
             role: 'admin' | 'lider' | 'separador' | 'fardista'
             ativo: boolean
             created_at: string
           }
           Insert: {
             id?: string
             nome: string
             pin_hash: string
             role: 'admin' | 'lider' | 'separador' | 'fardista'
             ativo?: boolean
             created_at?: string
           }
           Update: {
             id?: string
             nome?: string
             pin_hash?: string
             role?: 'admin' | 'lider' | 'separador' | 'fardista'
             ativo?: boolean
             created_at?: string
           }
         }
         config: {
           Row: {
             id: string
             chave: string
             valor: string
             updated_at: string
           }
           Insert: {
             id?: string
             chave: string
             valor: string
             updated_at?: string
           }
           Update: {
             id?: string
             chave?: string
             valor?: string
             updated_at?: string
           }
         }
         pedidos: {
           Row: {
             id: string
             numero_pedido: string
             numero_pedido_plataforma: string | null
             plataforma: string
             loja: string
             sku: string
             quantidade: number
             variacao: string | null
             nome_produto: string | null
             metodo_envio: string
             grupo_envio: string
             tipo: 'unitario' | 'kit' | 'combo'
             importacao_numero: number
             importacao_data: string
             card_key: string
             prazo_envio: string | null
             created_at: string
           }
           Insert: {
             id?: string
             numero_pedido: string
             numero_pedido_plataforma?: string | null
             plataforma: string
             loja: string
             sku: string
             quantidade: number
             variacao?: string | null
             nome_produto?: string | null
             metodo_envio: string
             grupo_envio: string
             tipo: 'unitario' | 'kit' | 'combo'
             importacao_numero: number
             importacao_data?: string
             card_key: string
             prazo_envio?: string | null
             created_at?: string
           }
           Update: {
             id?: string
             numero_pedido?: string
             numero_pedido_plataforma?: string | null
             plataforma?: string
             loja?: string
             sku?: string
             quantidade?: number
             variacao?: string | null
             nome_produto?: string | null
             metodo_envio?: string
             grupo_envio?: string
             tipo?: 'unitario' | 'kit' | 'combo'
             importacao_numero?: number
             importacao_data?: string
             card_key?: string
             prazo_envio?: string | null
             created_at?: string
           }
         }
         progresso: {
           Row: {
             id: string
             pedido_id: string
             quantidade_separada: number
             status: 'pendente' | 'parcial' | 'completo' | 'nao_encontrado' | 'aguardar_fardista' | 'transformacao'
             updated_at: string
           }
           Insert: {
             id?: string
             pedido_id: string
             quantidade_separada?: number
             status?: 'pendente' | 'parcial' | 'completo' | 'nao_encontrado' | 'aguardar_fardista' | 'transformacao'
             updated_at?: string
           }
           Update: {
             id?: string
             pedido_id?: string
             quantidade_separada?: number
             status?: 'pendente' | 'parcial' | 'completo' | 'nao_encontrado' | 'aguardar_fardista' | 'transformacao'
             updated_at?: string
           }
         }
         reservas: {
           Row: {
             id: string
             pedido_id: string
             codigo_in: string
             sku: string
             quantidade: number
             endereco: string | null
             status: 'reservado' | 'cancelado'
             created_at: string
           }
           Insert: {
             id?: string
             pedido_id: string
             codigo_in: string
             sku: string
             quantidade: number
             endereco?: string | null
             status?: 'reservado' | 'cancelado'
             created_at?: string
           }
           Update: {
             id?: string
             pedido_id?: string
             codigo_in?: string
             sku?: string
             quantidade?: number
             endereco?: string | null
             status?: 'reservado' | 'cancelado'
             created_at?: string
           }
         }
         atribuicoes: {
           Row: {
             id: string
             card_key: string
             user_id: string
             tipo: 'separador' | 'fardista'
             created_at: string
           }
           Insert: {
             id?: string
             card_key: string
             user_id: string
             tipo: 'separador' | 'fardista'
             created_at?: string
           }
           Update: {
             id?: string
             card_key?: string
             user_id?: string
             tipo?: 'separador' | 'fardista'
             created_at?: string
           }
         }
         trafego_fardos: {
           Row: {
             id: string
             reserva_id: string
             codigo_in: string
             sku: string
             quantidade: number
             endereco: string | null
             status: 'pendente' | 'encontrado' | 'nao_encontrado'
             fardista_id: string | null
             created_at: string
           }
           Insert: {
             id?: string
             reserva_id: string
             codigo_in: string
             sku: string
             quantidade: number
             endereco?: string | null
             status?: 'pendente' | 'encontrado' | 'nao_encontrado'
             fardista_id?: string | null
             created_at?: string
           }
           Update: {
             id?: string
             reserva_id?: string
             codigo_in?: string
             sku?: string
             quantidade?: number
             endereco?: string | null
             status?: 'pendente' | 'encontrado' | 'nao_encontrado'
             fardista_id?: string | null
             created_at?: string
           }
         }
         baixados: {
           Row: {
             id: string
             trafego_id: string
             codigo_in: string
             baixado_por: string
             baixado_em: string
           }
           Insert: {
             id?: string
             trafego_id: string
             codigo_in: string
             baixado_por: string
             baixado_em?: string
           }
           Update: {
             id?: string
             trafego_id?: string
             codigo_in?: string
             baixado_por?: string
             baixado_em?: string
           }
         }
         fardos_nao_encontrados: {
           Row: {
             id: string
             trafego_id: string
             codigo_in: string
             reportado_por: string
             reportado_em: string
           }
           Insert: {
             id?: string
             trafego_id: string
             codigo_in: string
             reportado_por: string
             reportado_em?: string
           }
           Update: {
             id?: string
             trafego_id?: string
             codigo_in?: string
             reportado_por?: string
             reportado_em?: string
           }
         }
       }
       Views: {
         [_ in never]: never
       }
       Functions: {
         [_ in never]: never
       }
       Enums: {
         [_ in never]: never
       }
     }
   }
   ```

9. Criar middleware.ts na raiz do projeto (per RESEARCH.md Pattern 3):
   ```typescript
   import { createServerClient } from '@supabase/ssr'
   import { NextResponse, type NextRequest } from 'next/server'

   export async function middleware(request: NextRequest) {
     let supabaseResponse = NextResponse.next({ request })

     const supabase = createServerClient(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
       {
         cookies: {
           getAll() {
             return request.cookies.getAll()
           },
           setAll(cookiesToSet) {
             cookiesToSet.forEach(({ name, value }) =>
               request.cookies.set(name, value)
             )
             supabaseResponse = NextResponse.next({ request })
             cookiesToSet.forEach(({ name, value, options }) =>
               supabaseResponse.cookies.set(name, value, options)
             )
           },
         },
       }
     )

     await supabase.auth.getUser()
     return supabaseResponse
   }

   export const config = {
     matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
   }
   ```

10. Atualizar src/types/index.ts para re-exportar tipos do database:
    ```typescript
    // Tipos globais do NARAKA SEP v2
    export type { Database } from './database.types'

    // Roles do sistema
    export type UserRole = 'admin' | 'lider' | 'separador' | 'fardista'

    // Tipos de pedido
    export type TipoPedido = 'unitario' | 'kit' | 'combo'

    // Status de progresso
    export type StatusProgresso = 'pendente' | 'parcial' | 'completo' | 'nao_encontrado' | 'aguardar_fardista' | 'transformacao'

    // Status de fardo no trafego
    export type StatusTrafego = 'pendente' | 'encontrado' | 'nao_encontrado'
    ```
  </action>
  <verify>
    <automated>cd /Users/gabrielcortina/Desktop/naraka-sep-v2 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "@supabase/supabase-js"
    - package.json contains "@supabase/ssr"
    - supabase/config.toml exists
    - supabase/migrations/00001_initial_schema.sql contains "CREATE TABLE users"
    - supabase/migrations/00001_initial_schema.sql contains "CREATE TABLE pedidos"
    - supabase/migrations/00001_initial_schema.sql contains "CREATE TABLE progresso"
    - supabase/migrations/00001_initial_schema.sql contains "CREATE TABLE reservas"
    - supabase/migrations/00001_initial_schema.sql contains "CREATE TABLE atribuicoes"
    - supabase/migrations/00001_initial_schema.sql contains "CREATE TABLE trafego_fardos"
    - supabase/migrations/00001_initial_schema.sql contains "CREATE TABLE baixados"
    - supabase/migrations/00001_initial_schema.sql contains "CREATE TABLE fardos_nao_encontrados"
    - supabase/migrations/00001_initial_schema.sql contains "CREATE TABLE config"
    - supabase/migrations/00001_initial_schema.sql contains "ENABLE ROW LEVEL SECURITY"
    - supabase/migrations/00001_initial_schema.sql contains "CREATE POLICY"
    - supabase/seed.sql contains "INSERT INTO users"
    - src/lib/supabase/client.ts contains "createBrowserClient"
    - src/lib/supabase/client.ts contains "Database"
    - src/lib/supabase/server.ts contains "createServerClient"
    - src/lib/supabase/server.ts contains "cookies"
    - src/lib/supabase/admin.ts contains "SUPABASE_SERVICE_ROLE_KEY"
    - src/types/database.types.ts contains "export interface Database"
    - src/types/database.types.ts contains "users"
    - src/types/database.types.ts contains "pedidos"
    - middleware.ts contains "export async function middleware"
    - middleware.ts contains "getUser"
    - src/types/index.ts contains "export type { Database }"
    - src/types/index.ts contains "UserRole"
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Supabase configurado com migration SQL completa (9 tabelas, RLS, indices, policies), 3 clientes TypeScript type-safe (browser, server, admin), middleware de auth, e tipos auto-gerados</done>
</task>

<task type="auto">
  <name>Task 2: Configurar Google Sheets API com Route Handler</name>
  <files>
    package.json, src/lib/google-sheets.ts, app/api/sheets/route.ts
  </files>
  <read_first>
    package.json (estado apos Task 1)
    .planning/phases/01-infraestrutura/01-RESEARCH.md (secao Pattern 2, Pitfall 1)
  </read_first>
  <action>
1. Instalar googleapis:
   ```bash
   npm install googleapis
   ```

2. Criar src/lib/google-sheets.ts (per D-07, D-08 e RESEARCH.md Pattern 2):
   ```typescript
   import { google } from 'googleapis'

   function getAuthClient() {
     const credentials = JSON.parse(
       process.env.GOOGLE_SERVICE_ACCOUNT_KEY!
     )

     return new google.auth.JWT(
       credentials.client_email,
       undefined,
       credentials.private_key,
       ['https://www.googleapis.com/auth/spreadsheets']
     )
   }

   export async function getSheetData(range: string) {
     const auth = getAuthClient()
     const sheets = google.sheets({ version: 'v4', auth })

     const response = await sheets.spreadsheets.values.get({
       spreadsheetId: process.env.GOOGLE_SHEET_ID!,
       range,
     })

     return response.data.values
   }

   export async function updateSheetData(
     range: string,
     values: (string | number | null)[][]
   ) {
     const auth = getAuthClient()
     const sheets = google.sheets({ version: 'v4', auth })

     const response = await sheets.spreadsheets.values.update({
       spreadsheetId: process.env.GOOGLE_SHEET_ID!,
       range,
       valueInputOption: 'RAW',
       requestBody: { values },
     })

     return response.data
   }

   export async function clearSheetRange(range: string) {
     const auth = getAuthClient()
     const sheets = google.sheets({ version: 'v4', auth })

     const response = await sheets.spreadsheets.values.clear({
       spreadsheetId: process.env.GOOGLE_SHEET_ID!,
       range,
     })

     return response.data
   }
   ```

3. Criar app/api/sheets/route.ts (per D-07):
   ```typescript
   import { getSheetData } from '@/lib/google-sheets'
   import { NextResponse } from 'next/server'

   export async function GET() {
     try {
       const data = await getSheetData('Estoque!A:F')
       return NextResponse.json({
         success: true,
         rows: data?.length ?? 0,
         data,
       })
     } catch (error) {
       console.error('Erro ao acessar planilha:', error)
       return NextResponse.json(
         { success: false, error: 'Falha ao acessar planilha de estoque' },
         { status: 500 }
       )
     }
   }
   ```

   IMPORTANTE: Este Route Handler roda apenas no servidor. Credenciais Google NUNCA sao enviadas ao cliente (per D-07).
  </action>
  <verify>
    <automated>cd /Users/gabrielcortina/Desktop/naraka-sep-v2 && npm run build && grep -q "googleapis" package.json && echo "OK: googleapis installed" && grep -q "getSheetData" src/lib/google-sheets.ts && echo "OK: getSheetData exists" && grep -q "export async function GET" app/api/sheets/route.ts && echo "OK: Route handler exists"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "googleapis"
    - src/lib/google-sheets.ts contains "export async function getSheetData"
    - src/lib/google-sheets.ts contains "export async function updateSheetData"
    - src/lib/google-sheets.ts contains "export async function clearSheetRange"
    - src/lib/google-sheets.ts contains "GOOGLE_SERVICE_ACCOUNT_KEY"
    - src/lib/google-sheets.ts contains "GOOGLE_SHEET_ID"
    - src/lib/google-sheets.ts contains "google.auth.JWT"
    - app/api/sheets/route.ts contains "export async function GET"
    - app/api/sheets/route.ts contains "import { getSheetData }"
    - app/api/sheets/route.ts contains "Estoque!A:F"
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Google Sheets API configurada com cliente JWT via conta de servico, Route Handler GET /api/sheets retorna dados da planilha de estoque, funcoes de leitura/escrita/limpeza disponiveis</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server | Credenciais Supabase service role e Google service account ficam no servidor |
| Route Handler -> Google API | Autenticacao via JWT de conta de servico |
| browser -> Supabase | Apenas anon key (NEXT_PUBLIC_*) exposta ao client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | src/lib/supabase/admin.ts | mitigate | SUPABASE_SERVICE_ROLE_KEY sem prefixo NEXT_PUBLIC_; arquivo importado apenas em server-side code |
| T-01-04 | Information Disclosure | src/lib/google-sheets.ts | mitigate | GOOGLE_SERVICE_ACCOUNT_KEY acessada apenas em server-side; Route Handler em app/api/ garante server-only execution (per D-07) |
| T-01-05 | Elevation of Privilege | Supabase RLS | mitigate | RLS habilitado em TODAS as 9 tabelas na migration; policies basicas de leitura autenticada criadas (per D-05) |
| T-01-06 | Spoofing | middleware.ts | mitigate | Middleware valida JWT em toda request via supabase.auth.getUser(); tokens expirados sao recusados |
| T-01-07 | Information Disclosure | supabase/seed.sql | accept | Seed com PIN hash (nao PIN raw) para dev only; hash SHA-256 de "1234" e publicamente derivavel mas aceitavel para dev |
</threat_model>

<verification>
- `npm run build` completa sem erros
- supabase/migrations/00001_initial_schema.sql contem 9 tabelas com RLS
- src/lib/supabase/ contem 3 clientes (client.ts, server.ts, admin.ts)
- src/types/database.types.ts contem interface Database com 9 tabelas
- app/api/sheets/route.ts exporta GET handler
- middleware.ts intercepta requests para refresh de tokens
</verification>

<success_criteria>
Supabase totalmente configurado com schema SQL versionado, clientes type-safe e middleware; Google Sheets API integrada via Route Handler com funcoes de leitura, escrita e limpeza -- pronto para deploy e teste de conexoes reais.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infraestrutura/01-02-SUMMARY.md`
</output>
