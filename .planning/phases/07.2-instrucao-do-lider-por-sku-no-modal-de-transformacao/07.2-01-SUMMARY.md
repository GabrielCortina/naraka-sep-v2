---
phase: 07.2-instrucao-do-lider-por-sku-no-modal-de-transformacao
plan: 01
subsystem: transformacao
tags: [backend, api, database, instrucao-lider]
dependency_graph:
  requires: [07.1-aba-de-transformacao]
  provides: [PATCH /api/transformacao/instrucao, InstrucaoLider type, instrucao_lider column]
  affects: [transformacao-modal, use-transformacao-data]
tech_stack:
  added: []
  patterns: [PATCH endpoint, enum validation, nullable column]
key_files:
  created:
    - app/api/transformacao/instrucao/route.ts
    - supabase/migrations/00008_instrucao_lider.sql
  modified:
    - src/features/transformacao/types.ts
decisions:
  - "Migration 00008 adds nullable text column instrucao_lider (no default, no constraint)"
  - "VALID_INSTRUCOES const array used as allowlist instead of DB-level CHECK constraint"
  - "eslint-disable for supabaseAdmin as any follows existing route.ts pattern"
metrics:
  duration: 3min
  completed: 2026-04-08
  tasks: 2
  files: 3
---

# Phase 07.2 Plan 01: Backend instrucao do lider Summary

PATCH endpoint with role-gated enum validation for leader instructions per SKU on transformacoes table.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Adicionar coluna instrucao_lider e atualizar tipo TypeScript | ecb2db4 | types.ts, 00008_instrucao_lider.sql |
| 2 | Criar endpoint PATCH /api/transformacao/instrucao | 2562336 | instrucao/route.ts |

## What Was Built

1. **Database migration** (`supabase/migrations/00008_instrucao_lider.sql`): ALTER TABLE adds nullable `instrucao_lider` text column to `transformacoes`.

2. **TypeScript types** (`src/features/transformacao/types.ts`): Exported `InstrucaoLider` union type with 4 values (TRANSFORMACAO_LIBERADA, SKU_VAI_CHEGAR, PEGAR_NA_VALERIA, PEGAR_NA_LOJA). Added `instrucao_lider: InstrucaoLider | null` to `TransformacaoItem`. `TransformacaoCardData` unchanged per D-11.

3. **PATCH endpoint** (`app/api/transformacao/instrucao/route.ts`): Full request lifecycle -- auth (401), role check admin/lider only (403), body parsing (400), enum validation (400), existence check (404), update via supabaseAdmin (500 on failure). Accepts `instrucao: null` to clear instruction.

## Deviations from Plan

None -- plan executed exactly as written.

## Verification Results

- `grep instrucao_lider types.ts` -- field present in TransformacaoItem
- `grep InstrucaoLider types.ts` -- 2 occurrences (type def + field usage)
- `grep PATCH instrucao/route.ts` -- exported handler found
- `grep VALID_INSTRUCOES instrucao/route.ts` -- 4-value allowlist found
- `grep "admin.*lider" instrucao/route.ts` -- role check present (2 matches)
- `npx tsc --noEmit` -- zero transformacao-related type errors (pre-existing stock-parser test error is out of scope)

## Known Stubs

None -- all data flows are wired to the database.

## Self-Check: PASSED

- FOUND: app/api/transformacao/instrucao/route.ts
- FOUND: supabase/migrations/00008_instrucao_lider.sql
- FOUND: src/features/transformacao/types.ts
- FOUND: commit ecb2db4
- FOUND: commit 2562336
