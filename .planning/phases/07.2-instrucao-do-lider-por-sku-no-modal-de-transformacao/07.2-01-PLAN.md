---
phase: 07.2-instrucao-do-lider-por-sku-no-modal-de-transformacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/transformacao/types.ts
  - app/api/transformacao/instrucao/route.ts
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-03

must_haves:
  truths:
    - "Coluna instrucao_lider existe na tabela transformacoes no Supabase"
    - "PATCH /api/transformacao/instrucao aceita transformacao_id e instrucao, atualiza o campo no banco"
    - "Endpoint rejeita fardista e separador com 403"
    - "Endpoint rejeita valores de instrucao fora do enum com 400"
    - "TransformacaoItem type inclui campo instrucao_lider"
  artifacts:
    - path: "app/api/transformacao/instrucao/route.ts"
      provides: "PATCH endpoint para instrucao do lider"
      exports: ["PATCH"]
    - path: "src/features/transformacao/types.ts"
      provides: "TransformacaoItem com instrucao_lider"
      contains: "instrucao_lider"
  key_links:
    - from: "app/api/transformacao/instrucao/route.ts"
      to: "supabaseAdmin.from('transformacoes')"
      via: "update instrucao_lider"
      pattern: "instrucao_lider"
---

<objective>
Backend para instrucao do lider por SKU: adicionar coluna instrucao_lider na tabela transformacoes, atualizar tipo TypeScript, e criar endpoint PATCH /api/transformacao/instrucao com validacao de role e enum.

Purpose: Permitir que lider/admin persista instrucoes por SKU no banco para que o frontend (Plan 02) possa exibi-las como badges coloridos.
Output: Coluna no banco, tipo atualizado, API endpoint funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.2-instrucao-do-lider-por-sku-no-modal-de-transformacao/07.2-CONTEXT.md

<interfaces>
<!-- Existing patterns the executor needs -->

From src/features/transformacao/types.ts:
```typescript
export interface TransformacaoItem {
  id: string
  sku: string
  quantidade: number
  card_key: string
  numero_pedido: string | null
  separador_id: string | null
  separador_nome: string | null
  status: 'pendente' | 'atribuido' | 'concluido'
  numero_transformacao: number
  created_at: string
  concluido_at: string | null
}
```

From app/api/transformacao/route.ts (auth pattern):
```typescript
// 1. Autenticacao
const supabase = await createClient()
const { data: { user }, error: authError } = await supabase.auth.getUser()
if (authError || !user) {
  return NextResponse.json({ error: 'Nao autenticado' }, { status: 401 })
}

// 2. Role check
const { data: dbUser } = await supabaseAdmin
  .from('users')
  .select('role')
  .eq('id', user.id)
  .single()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar coluna instrucao_lider e atualizar tipo TypeScript</name>
  <read_first>
    - src/features/transformacao/types.ts
    - src/features/transformacao/hooks/use-transformacao-data.ts
  </read_first>
  <files>src/features/transformacao/types.ts</files>
  <action>
1. Executar SQL no Supabase para adicionar a coluna:

```sql
ALTER TABLE transformacoes ADD COLUMN instrucao_lider text;
```

Usar o Supabase CLI ou o dashboard SQL editor. A coluna e nullable (null = sem instrucao), sem default.

2. Atualizar `src/features/transformacao/types.ts` — adicionar campo `instrucao_lider` ao `TransformacaoItem`:

```typescript
export type InstrucaoLider =
  | 'TRANSFORMACAO_LIBERADA'
  | 'SKU_VAI_CHEGAR'
  | 'PEGAR_NA_VALERIA'
  | 'PEGAR_NA_LOJA'

export interface TransformacaoItem {
  id: string
  sku: string
  quantidade: number
  card_key: string
  numero_pedido: string | null
  separador_id: string | null
  separador_nome: string | null
  status: 'pendente' | 'atribuido' | 'concluido'
  numero_transformacao: number
  created_at: string
  concluido_at: string | null
  instrucao_lider: InstrucaoLider | null
}
```

Manter `TransformacaoCardData` exatamente como esta — sem mudancas (per D-11).

NOTA: O hook `useTransformacaoData` ja faz `.select('*')` na tabela transformacoes, entao o campo `instrucao_lider` sera incluido automaticamente no fetch. O canal realtime `transformacao-realtime` ja escuta `event: '*'` na tabela, entao mudancas em `instrucao_lider` dispararao re-fetch automaticamente (per D-10).
  </action>
  <verify>
    <automated>cd /Users/gabrielcortina/Desktop/naraka-sep-v2 && grep -n "instrucao_lider" src/features/transformacao/types.ts && grep -c "InstrucaoLider" src/features/transformacao/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains `instrucao_lider: InstrucaoLider | null` inside TransformacaoItem
    - types.ts exports `InstrucaoLider` type with exactly 4 values: TRANSFORMACAO_LIBERADA, SKU_VAI_CHEGAR, PEGAR_NA_VALERIA, PEGAR_NA_LOJA
    - TransformacaoCardData interface is UNCHANGED (per D-11)
    - SQL ALTER TABLE executed successfully (column exists in Supabase)
  </acceptance_criteria>
  <done>Coluna instrucao_lider existe no banco, tipo TransformacaoItem inclui instrucao_lider, tipo InstrucaoLider exportado</done>
</task>

<task type="auto">
  <name>Task 2: Criar endpoint PATCH /api/transformacao/instrucao</name>
  <read_first>
    - app/api/transformacao/route.ts
    - src/features/transformacao/types.ts
    - src/lib/supabase/server.ts
    - src/lib/supabase/admin.ts
  </read_first>
  <files>app/api/transformacao/instrucao/route.ts</files>
  <action>
Criar `app/api/transformacao/instrucao/route.ts` com handler PATCH seguindo o padrao exato de auth do route.ts existente:

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { supabaseAdmin } from '@/lib/supabase/admin'

const VALID_INSTRUCOES = [
  'TRANSFORMACAO_LIBERADA',
  'SKU_VAI_CHEGAR',
  'PEGAR_NA_VALERIA',
  'PEGAR_NA_LOJA',
] as const

export async function PATCH(request: NextRequest) {
  // 1. Autenticacao
  const supabase = await createClient()
  const { data: { user }, error: authError } = await supabase.auth.getUser()
  if (authError || !user) {
    return NextResponse.json({ error: 'Nao autenticado' }, { status: 401 })
  }

  // 2. Role check: ONLY admin and lider (per D-08)
  const { data: dbUser } = await supabaseAdmin
    .from('users')
    .select('role')
    .eq('id', user.id)
    .single()

  if (!dbUser || !['admin', 'lider'].includes(dbUser.role)) {
    return NextResponse.json(
      { error: 'Sem permissao para definir instrucao' },
      { status: 403 }
    )
  }

  // 3. Parse body
  let body: unknown
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: 'Body invalido' }, { status: 400 })
  }

  const { transformacao_id, instrucao } = body as {
    transformacao_id: unknown
    instrucao: unknown
  }

  // 4. Validate transformacao_id
  if (!transformacao_id || typeof transformacao_id !== 'string') {
    return NextResponse.json({ error: 'transformacao_id invalido' }, { status: 400 })
  }

  // 5. Validate instrucao — must be one of 4 valid values OR null (to clear)
  if (instrucao !== null && (typeof instrucao !== 'string' || !VALID_INSTRUCOES.includes(instrucao as typeof VALID_INSTRUCOES[number]))) {
    return NextResponse.json({ error: 'Instrucao invalida' }, { status: 400 })
  }

  // 6. Verify transformacao exists
  const { data: transformacao, error: fetchError } = await (supabaseAdmin as any)
    .from('transformacoes')
    .select('id')
    .eq('id', transformacao_id)
    .single()

  if (fetchError || !transformacao) {
    return NextResponse.json({ error: 'Transformacao nao encontrada' }, { status: 404 })
  }

  // 7. Update instrucao_lider
  const { error: updateError } = await (supabaseAdmin as any)
    .from('transformacoes')
    .update({ instrucao_lider: instrucao })
    .eq('id', transformacao_id)

  if (updateError) {
    return NextResponse.json({ error: 'Erro ao atualizar instrucao' }, { status: 500 })
  }

  return NextResponse.json({ success: true })
}
```

Pontos criticos:
- Roles permitidos: SOMENTE `admin` e `lider` (per D-08) — diferente do POST que permite separador
- `instrucao` aceita null (para limpar instrucao) ou um dos 4 valores do enum (per D-06)
- Usa `supabaseAdmin` para write (padrao do projeto)
- `transformacao_id` validado como string nao-vazia (previne SQL injection via parameterized query do Supabase)
- eslint-disable para `as any` no supabaseAdmin (padrao existente no route.ts)
  </action>
  <verify>
    <automated>cd /Users/gabrielcortina/Desktop/naraka-sep-v2 && grep -n "PATCH" app/api/transformacao/instrucao/route.ts && grep -n "VALID_INSTRUCOES" app/api/transformacao/instrucao/route.ts && grep -c "admin.*lider" app/api/transformacao/instrucao/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - File app/api/transformacao/instrucao/route.ts exists
    - File exports `async function PATCH`
    - File contains `VALID_INSTRUCOES` array with exactly 4 values: TRANSFORMACAO_LIBERADA, SKU_VAI_CHEGAR, PEGAR_NA_VALERIA, PEGAR_NA_LOJA
    - Role check allows ONLY `admin` and `lider` (NOT separador, NOT fardista)
    - Returns 401 for unauthenticated, 403 for wrong role, 400 for invalid body/instrucao, 404 for missing transformacao
    - Uses `supabaseAdmin` for database writes (not client supabase)
    - Accepts `instrucao: null` to clear instruction
  </acceptance_criteria>
  <done>Endpoint PATCH /api/transformacao/instrucao funcional com validacao de role (admin/lider), enum, e existencia</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> PATCH /api/transformacao/instrucao | Untrusted input: transformacao_id and instrucao from request body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2-01 | Spoofing | PATCH /api/transformacao/instrucao | mitigate | Auth via createClient() + getUser() at route entry; reject 401 if no user |
| T-07.2-02 | Tampering | instrucao field | mitigate | Validate against VALID_INSTRUCOES allowlist; reject 400 if not in enum or null |
| T-07.2-03 | Elevation of Privilege | Role bypass | mitigate | Role check via supabaseAdmin lookup — only admin/lider allowed; separador/fardista get 403 |
| T-07.2-04 | Injection | transformacao_id parameter | mitigate | Supabase parameterized queries prevent SQL injection; validate as non-empty string |
| T-07.2-05 | Information Disclosure | Error messages | accept | Error messages are generic ("Instrucao invalida"), no internal details leaked |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (type check)
- `grep -r "instrucao_lider" src/features/transformacao/types.ts` shows field in TransformacaoItem
- `grep -r "PATCH" app/api/transformacao/instrucao/route.ts` shows exported handler
- PATCH to /api/transformacao/instrucao with invalid role returns 403
- PATCH with invalid instrucao value returns 400
</verification>

<success_criteria>
- Coluna instrucao_lider existe na tabela transformacoes
- Tipo TypeScript TransformacaoItem inclui instrucao_lider
- Tipo InstrucaoLider exportado com 4 valores
- PATCH endpoint funcional com auth, role check, enum validation
- Zero mudancas em logica existente (route.ts, use-transformacao-data.ts intocados)
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-instrucao-do-lider-por-sku-no-modal-de-transformacao/07.2-01-SUMMARY.md`
</output>
