---
phase: 05-cards-e-ui-foundation
plan: 06
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - supabase/migrations/00004_rls_write_policies.sql
  - src/app/api/cards/progress/route.ts
  - src/app/api/cards/assign/route.ts
autonomous: true
requirements:
  - CARD-06
  - CARD-03

must_haves:
  truths:
    - "RLS policies permitem INSERT/UPDATE em progresso e atribuicoes para usuarios autenticados"
    - "POST /api/cards/progress valida quantidade >= 0 e <= quantidade necessaria antes de gravar"
    - "POST /api/cards/assign verifica role do usuario antes de inserir atribuicao"
    - "Atribuicoes usam tipo 'separador' ou 'fardista' conforme CHECK constraint do schema"
    - "API routes usam admin client para escrita, browser client nao faz upsert direto"
  artifacts:
    - path: "supabase/migrations/00004_rls_write_policies.sql"
      provides: "Policies INSERT/UPDATE para progresso e atribuicoes"
      contains: "CREATE POLICY"
    - path: "src/app/api/cards/progress/route.ts"
      provides: "Endpoint POST para confirmar quantidade e marcar N/E"
      exports: ["POST"]
    - path: "src/app/api/cards/assign/route.ts"
      provides: "Endpoint POST para atribuir usuario a card"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/cards/progress/route.ts"
      to: "src/lib/supabase/admin.ts"
      via: "import createAdminClient"
      pattern: "import.*admin"
    - from: "src/app/api/cards/assign/route.ts"
      to: "src/lib/supabase/server.ts"
      via: "import createServerClient para auth check"
      pattern: "import.*server"
---

<objective>
Migration SQL com RLS write policies e API route handlers para acoes dos cards (progresso e atribuicao) com validacao server-side.

Purpose: Plan 05-05 precisa gravar progresso e atribuicoes no banco via browser. Sem policies de INSERT/UPDATE, RLS bloqueia essas escritas. Alem disso, gravar diretamente do browser sem validacao server-side viola ASVS V5 (input validation). Esta plan cria a camada de API segura que Plan 05-05 vai consumir.

Output: Migration SQL + 2 API route handlers seguros.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cards-e-ui-foundation/05-CONTEXT.md
@.planning/phases/05-cards-e-ui-foundation/05-RESEARCH.md

<interfaces>
From supabase/migrations/00001_initial_schema.sql:
```sql
-- progresso table
CREATE TABLE progresso (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  pedido_id UUID NOT NULL REFERENCES pedidos(id) ON DELETE CASCADE,
  quantidade_separada INTEGER NOT NULL DEFAULT 0,
  status TEXT NOT NULL DEFAULT 'pendente' CHECK (status IN ('pendente', 'parcial', 'completo', 'nao_encontrado', 'aguardar_fardista', 'transformacao')),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- atribuicoes table
CREATE TABLE atribuicoes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  card_key TEXT NOT NULL,
  user_id UUID NOT NULL REFERENCES users(id),
  tipo TEXT NOT NULL CHECK (tipo IN ('separador', 'fardista')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE(card_key, tipo)
);

-- RLS habilitado, apenas policies SELECT existem:
CREATE POLICY "Leitura autenticada" ON progresso FOR SELECT TO authenticated USING (true);
CREATE POLICY "Leitura autenticada" ON atribuicoes FOR SELECT TO authenticated USING (true);
```

From src/lib/supabase/admin.ts:
```typescript
// Supabase client com service_role key — bypassa RLS
export function createAdminClient(): SupabaseClient<Database>
```

From src/lib/supabase/server.ts:
```typescript
export function createServerClient(): SupabaseClient<Database>
```

From src/features/cards/types.ts (Plan 01 creates):
```typescript
export interface CardData {
  card_key: string; grupo_envio: string; tipo: string; importacao_numero: number;
  items: CardItem[]; total_pecas: number; pecas_separadas: number;
  atribuido_a: { id: string; nome: string } | null; urgency: UrgencyTier
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration RLS write policies para progresso e atribuicoes</name>
  <files>supabase/migrations/00004_rls_write_policies.sql</files>
  <read_first>
    supabase/migrations/00001_initial_schema.sql,
    supabase/migrations/00003_alter_reservas_schema.sql
  </read_first>
  <action>
    Criar `supabase/migrations/00004_rls_write_policies.sql` com policies de escrita para as tabelas que os cards precisam atualizar:

    ```sql
    -- RLS write policies para Phase 5: Cards e UI Foundation
    -- Sem estas policies, upserts do browser client sao bloqueados pelo RLS

    -- progresso: qualquer usuario autenticado pode inserir/atualizar progresso
    -- Validacao de role e quantidade e feita no API route handler, nao no RLS
    CREATE POLICY "Escrita autenticada progresso" ON progresso
      FOR INSERT TO authenticated
      WITH CHECK (true);

    CREATE POLICY "Atualizacao autenticada progresso" ON progresso
      FOR UPDATE TO authenticated
      USING (true)
      WITH CHECK (true);

    -- atribuicoes: apenas lider e admin podem inserir/atualizar atribuicoes
    -- Usa claim user_role do JWT (injetado pelo Custom Access Token Hook da Phase 2)
    CREATE POLICY "Escrita lider/admin atribuicoes" ON atribuicoes
      FOR INSERT TO authenticated
      WITH CHECK (
        (current_setting('request.jwt.claims', true)::json->>'user_role') IN ('admin', 'lider')
      );

    CREATE POLICY "Atualizacao lider/admin atribuicoes" ON atribuicoes
      FOR UPDATE TO authenticated
      USING (true)
      WITH CHECK (
        (current_setting('request.jwt.claims', true)::json->>'user_role') IN ('admin', 'lider')
      );

    -- DELETE para atribuicoes (permitir reatribuicao via delete + insert)
    CREATE POLICY "Delete lider/admin atribuicoes" ON atribuicoes
      FOR DELETE TO authenticated
      USING (
        (current_setting('request.jwt.claims', true)::json->>'user_role') IN ('admin', 'lider')
      );
    ```

    IMPORTANTE: A tabela `atribuicoes` tem CHECK constraint `tipo IN ('separador', 'fardista')`. Plan 05-05 deve usar estes valores (NAO 'prateleira'/'fardos') ao inserir. O API route handler no Task 2 vai validar isso.
  </action>
  <verify>
    <automated>cat supabase/migrations/00004_rls_write_policies.sql | grep -c "CREATE POLICY"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/00004_rls_write_policies.sql existe e contem 5 CREATE POLICY statements
    - Arquivo contem "Escrita autenticada progresso" para INSERT em progresso
    - Arquivo contem "Atualizacao autenticada progresso" para UPDATE em progresso
    - Arquivo contem "Escrita lider/admin atribuicoes" com check de user_role IN ('admin', 'lider')
    - Arquivo contem "Atualizacao lider/admin atribuicoes" com check de user_role
    - Arquivo contem "Delete lider/admin atribuicoes" com check de user_role
  </acceptance_criteria>
  <done>Migration SQL com 5 RLS policies: INSERT/UPDATE em progresso para autenticados, INSERT/UPDATE/DELETE em atribuicoes restrito a lider/admin via JWT claim</done>
</task>

<task type="auto">
  <name>Task 2: API route handlers /api/cards/progress e /api/cards/assign com validacao</name>
  <files>
    src/app/api/cards/progress/route.ts,
    src/app/api/cards/assign/route.ts
  </files>
  <read_first>
    src/lib/supabase/server.ts,
    src/lib/supabase/admin.ts,
    src/features/cards/types.ts,
    supabase/migrations/00001_initial_schema.sql
  </read_first>
  <action>
    1. Criar `src/app/api/cards/progress/route.ts`:
       ```typescript
       import { NextRequest, NextResponse } from 'next/server'
       import { createServerClient } from '@/lib/supabase/server'
       import { createAdminClient } from '@/lib/supabase/admin'

       export async function POST(request: NextRequest) {
         // 1. Autenticacao: getUser() para verificar usuario logado
         const supabase = createServerClient()
         const { data: { user }, error: authError } = await supabase.auth.getUser()
         if (authError || !user) {
           return NextResponse.json({ error: 'Nao autenticado' }, { status: 401 })
         }

         // 2. Parse body
         const body = await request.json()
         const { pedido_id, quantidade_separada, status } = body

         // 3. Validacao (T-05-07 Tampering mitigation)
         if (!pedido_id || typeof pedido_id !== 'string') {
           return NextResponse.json({ error: 'pedido_id invalido' }, { status: 400 })
         }
         if (typeof quantidade_separada !== 'number' || quantidade_separada < 0) {
           return NextResponse.json({ error: 'Quantidade deve ser >= 0' }, { status: 400 })
         }
         const validStatus = ['pendente', 'parcial', 'completo', 'nao_encontrado']
         if (!validStatus.includes(status)) {
           return NextResponse.json({ error: 'Status invalido' }, { status: 400 })
         }

         // 4. Verificar que pedido existe e validar quantidade maxima
         const admin = createAdminClient()
         const { data: pedido } = await admin.from('pedidos').select('quantidade').eq('id', pedido_id).single()
         if (!pedido) {
           return NextResponse.json({ error: 'Pedido nao encontrado' }, { status: 404 })
         }
         if (quantidade_separada > pedido.quantidade) {
           return NextResponse.json({ error: 'Quantidade excede o necessario' }, { status: 400 })
         }

         // 5. Upsert progresso
         const { error } = await admin.from('progresso').upsert(
           { pedido_id, quantidade_separada, status, updated_at: new Date().toISOString() },
           { onConflict: 'pedido_id' }
         )
         if (error) {
           return NextResponse.json({ error: 'Erro ao salvar progresso' }, { status: 500 })
         }

         return NextResponse.json({ success: true })
       }
       ```

       NOTA sobre onConflict: a tabela progresso nao tem UNIQUE em pedido_id. O executor deve verificar se ha um index unique ou se precisa usar insert/select+update pattern. Se nao houver unique constraint, fazer SELECT primeiro e INSERT ou UPDATE conforme existencia.

    2. Criar `src/app/api/cards/assign/route.ts`:
       ```typescript
       import { NextRequest, NextResponse } from 'next/server'
       import { createServerClient } from '@/lib/supabase/server'
       import { createAdminClient } from '@/lib/supabase/admin'

       export async function POST(request: NextRequest) {
         // 1. Autenticacao
         const supabase = createServerClient()
         const { data: { user }, error: authError } = await supabase.auth.getUser()
         if (authError || !user) {
           return NextResponse.json({ error: 'Nao autenticado' }, { status: 401 })
         }

         // 2. Verificar role: apenas lider/admin podem atribuir (T-05-08)
         const admin = createAdminClient()
         const { data: dbUser } = await admin.from('users').select('role').eq('id', user.id).single()
         if (!dbUser || !['admin', 'lider'].includes(dbUser.role)) {
           return NextResponse.json({ error: 'Apenas lider ou admin pode atribuir' }, { status: 403 })
         }

         // 3. Parse body
         const body = await request.json()
         const { card_key, user_id, tipo } = body

         // 4. Validacao
         if (!card_key || typeof card_key !== 'string') {
           return NextResponse.json({ error: 'card_key invalido' }, { status: 400 })
         }
         if (!user_id || typeof user_id !== 'string') {
           return NextResponse.json({ error: 'user_id invalido' }, { status: 400 })
         }
         // CRITICO: tipo DEVE ser 'separador' ou 'fardista' — CHECK constraint no schema
         if (!['separador', 'fardista'].includes(tipo)) {
           return NextResponse.json({ error: 'tipo deve ser separador ou fardista' }, { status: 400 })
         }

         // 5. Verificar que o usuario alvo existe e tem o role correto
         const { data: targetUser } = await admin.from('users').select('role, ativo').eq('id', user_id).single()
         if (!targetUser || !targetUser.ativo) {
           return NextResponse.json({ error: 'Usuario nao encontrado ou inativo' }, { status: 404 })
         }
         if (targetUser.role !== tipo) {
           return NextResponse.json({ error: `Usuario nao e ${tipo}` }, { status: 400 })
         }

         // 6. Upsert atribuicao (UNIQUE(card_key, tipo) existe no schema)
         const { error } = await admin.from('atribuicoes').upsert(
           { card_key, user_id, tipo },
           { onConflict: 'card_key,tipo' }
         )
         if (error) {
           return NextResponse.json({ error: 'Erro ao atribuir' }, { status: 500 })
         }

         return NextResponse.json({ success: true })
       }
       ```
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/cards/progress/route.ts contem "export async function POST" e "getUser" e "quantidade_separada" e ">= 0" e "pedido.quantidade"
    - src/app/api/cards/progress/route.ts contem validacao de status: "'pendente', 'parcial', 'completo', 'nao_encontrado'"
    - src/app/api/cards/assign/route.ts contem "export async function POST" e "getUser" e "['admin', 'lider']" e "['separador', 'fardista']"
    - src/app/api/cards/assign/route.ts contem verificacao de role do usuario alvo: "targetUser.role !== tipo"
    - src/app/api/cards/assign/route.ts contem onConflict 'card_key,tipo' para upsert
    - tsc --noEmit passa sem erros
  </acceptance_criteria>
  <done>2 API route handlers criados: /api/cards/progress (valida quantidade 0..max, status valido, pedido existe) e /api/cards/assign (verifica role lider/admin, valida tipo separador/fardista, verifica usuario alvo). Plan 05-05 deve chamar estes endpoints via fetch ao inves de upsert direto do browser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API route | Input nao confiavel (quantidade, card_key, user_id) |
| API route -> Supabase | Queries com admin client bypassa RLS; validacao no handler |
| JWT claim -> RLS policy | user_role no JWT determina permissao de escrita em atribuicoes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | /api/cards/progress | mitigate | Validar quantidade >= 0 e <= pedido.quantidade no route handler. Validar status contra whitelist |
| T-05-10 | Elevation of Privilege | /api/cards/assign | mitigate | Verificar role via getUser() + DB lookup antes de permitir INSERT. RLS policy como segunda barreira |
| T-05-11 | Spoofing | /api/cards/assign user_id | mitigate | Verificar que usuario alvo existe, esta ativo e tem role correto para o tipo de atribuicao |
| T-05-12 | Tampering | atribuicoes.tipo | mitigate | Validar tipo IN ('separador', 'fardista') no handler para respeitar CHECK constraint do schema |
</threat_model>

<verification>
- `cat supabase/migrations/00004_rls_write_policies.sql | grep -c "CREATE POLICY"` retorna 5
- `npx tsc --noEmit` — sem erros
- `grep -c "POST" src/app/api/cards/progress/route.ts` retorna >= 1
- `grep -c "POST" src/app/api/cards/assign/route.ts` retorna >= 1
- `grep "separador.*fardista" src/app/api/cards/assign/route.ts` — presente (validacao tipo)
</verification>

<success_criteria>
- Migration SQL com 5 RLS policies (INSERT/UPDATE progresso para autenticados, INSERT/UPDATE/DELETE atribuicoes para lider/admin)
- API /api/cards/progress valida quantidade (>= 0, <= max), status (whitelist), e existencia do pedido
- API /api/cards/assign verifica role do chamador (lider/admin), valida tipo (separador/fardista), verifica usuario alvo
- Plan 05-05 pode consumir estes endpoints com seguranca em vez de upsert direto do browser
</success_criteria>

<output>
After completion, create `.planning/phases/05-cards-e-ui-foundation/05-06-SUMMARY.md`
</output>
