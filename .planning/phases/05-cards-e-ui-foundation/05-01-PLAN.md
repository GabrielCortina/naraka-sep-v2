---
phase: 05-cards-e-ui-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - tailwind.config.ts
  - src/features/cards/types.ts
  - src/features/cards/lib/deadline-config.ts
  - src/features/cards/lib/card-utils.ts
  - src/features/cards/lib/__tests__/card-utils.test.ts
autonomous: true
requirements:
  - CARD-01
  - CARD-03
  - CARD-04
  - CARD-05
  - CARD-08
  - UIUX-01
  - UIUX-04

must_haves:
  truths:
    - "Tokens CSS de marketplace e urgencia disponiveis no Tailwind como bg-shopee, text-ml, border-l-urgency-overdue, etc."
    - "Fonte Inter configurada via next/font/google em app/layout.tsx com pesos 400 e 700"
    - "Funcao groupByCardKey agrupa pedidos corretamente por card_key"
    - "Funcao getUrgencyTier retorna overdue/warning/ok baseado no prazo do grupo"
    - "Funcao calcProgress retorna pecas separadas / total correto"
    - "Funcao isCardComplete identifica card 100% completo"
    - "Constantes DEADLINES mapeiam os 6 grupos de envio para horarios corretos"
  artifacts:
    - path: "app/globals.css"
      provides: "CSS variables --shopee, --ml, --tiktok, --shein, --urgency-*"
      contains: "--shopee: 14 89% 55%"
    - path: "app/layout.tsx"
      provides: "Inter font via next/font/google com pesos 400 e 700"
      contains: "Inter"
    - path: "tailwind.config.ts"
      provides: "Tailwind colors shopee, ml, tiktok, shein, urgency-*"
      contains: "shopee"
    - path: "src/features/cards/types.ts"
      provides: "Tipos CardData, CardItem, UrgencyTier, MarketplaceKey"
      exports: ["CardData", "CardItem", "UrgencyTier"]
    - path: "src/features/cards/lib/deadline-config.ts"
      provides: "DEADLINES, COLUMN_ORDER, MARKETPLACE_COLORS"
      exports: ["DEADLINES", "COLUMN_ORDER", "MARKETPLACE_COLORS"]
    - path: "src/features/cards/lib/card-utils.ts"
      provides: "groupByCardKey, getUrgencyTier, calcProgress, isCardComplete"
      exports: ["groupByCardKey", "getUrgencyTier", "calcProgress", "isCardComplete"]
    - path: "src/features/cards/lib/__tests__/card-utils.test.ts"
      provides: "Testes unitarios para card-utils"
      min_lines: 80
  key_links:
    - from: "src/features/cards/lib/card-utils.ts"
      to: "src/features/cards/lib/deadline-config.ts"
      via: "import DEADLINES"
      pattern: "import.*DEADLINES.*deadline-config"
    - from: "src/features/cards/lib/card-utils.ts"
      to: "src/features/cards/types.ts"
      via: "import CardData, UrgencyTier"
      pattern: "import.*CardData.*types"
---

<objective>
Design system tokens (cores marketplace e urgencia) no Tailwind/CSS, fonte Inter, tipos do dominio cards, e logica pura de agrupamento/urgencia/progresso com TDD.

Purpose: Estabelecer a fundacao de tipos, tokens visuais e logica de negocio que todos os componentes de UI das proximas plans vao consumir. Tudo testavel sem browser.
Output: CSS variables + Tailwind extend, fonte Inter configurada, tipos TypeScript, funcoes puras testadas (card-utils), constantes de prazos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cards-e-ui-foundation/05-CONTEXT.md
@.planning/phases/05-cards-e-ui-foundation/05-RESEARCH.md
@.planning/phases/05-cards-e-ui-foundation/05-UI-SPEC.md

<interfaces>
<!-- Tipos do banco que alimentam os cards -->

From src/types/database.types.ts:
```typescript
// pedidos table Row
{
  card_key: string
  grupo_envio: string
  tipo: string // "Unitario" | "Kit" | "Combo"
  importacao_numero: number
  sku: string
  quantidade: number
  numero_pedido: string
  metodo_envio: string
  loja: string
  plataforma: string
}

// progresso table Row
{
  pedido_id: string
  quantidade_separada: number
  status: string // "pendente" | "separado" | "parcial" | "nao_encontrado"
}

// atribuicoes table Row
{
  card_key: string
  tipo: string
  user_id: string
}

// reservas table Row
{
  codigo_in: string
  sku: string
  quantidade: number
  endereco: string | null
  importacao_numero: number | null
  status: string
}
```

From src/features/upload/lib/envio-groups.ts:
```typescript
export function classifyEnvio(metodoEnvio: string): string
// Returns one of: 'Shopee SPX', 'ML Flex', 'ML Coleta', 'TikTok Shop', 'Shein', 'Shopee Xpress', 'Outro'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tipos do dominio cards, constantes de prazo, design tokens CSS/Tailwind e fonte Inter</name>
  <files>
    src/features/cards/types.ts,
    src/features/cards/lib/deadline-config.ts,
    app/globals.css,
    app/layout.tsx,
    tailwind.config.ts
  </files>
  <read_first>
    app/globals.css,
    app/layout.tsx,
    tailwind.config.ts,
    src/features/upload/lib/envio-groups.ts,
    src/types/database.types.ts
  </read_first>
  <behavior>
    - DEADLINES['Shopee SPX'] === 11
    - DEADLINES['ML Flex'] === 12
    - DEADLINES['ML Coleta'] === 14
    - DEADLINES['TikTok Shop'] === 15
    - DEADLINES['Shein'] === 16
    - DEADLINES['Shopee Xpress'] === 19
    - COLUMN_ORDER tem 6 entradas ordenadas por prazo crescente
    - MARKETPLACE_COLORS mapeia grupo para { bg, text } com valores hex corretos
  </behavior>
  <action>
    0. **Configurar fonte Inter em `app/layout.tsx` (per D-46, UIUX-01):**
       - Ler `app/layout.tsx` atual. Verificar se Inter ja esta configurada via `next/font/google`.
       - Se NAO estiver: importar `import { Inter } from 'next/font/google'`, criar instancia `const inter = Inter({ subsets: ['latin'], weight: ['400', '700'], display: 'swap', variable: '--font-inter' })`, aplicar `className={inter.variable}` no `<html>` ou `<body>`.
       - Se a fonte atual for diferente de Inter (ex: Geist), SUBSTITUIR pela Inter. Manter qualquer outra configuracao existente (metadata, providers, etc.) intacta.
       - Garantir que `font-family: var(--font-inter)` esteja configurado no `body` via Tailwind (`font-sans` no tailwind.config.ts `fontFamily.sans` apontando para `var(--font-inter)`) ou via CSS direto.

    1. Criar `src/features/cards/types.ts` com:
       - `type UrgencyTier = 'overdue' | 'warning' | 'ok' | 'done'`
       - `type MarketplaceKey = 'Shopee SPX' | 'ML Flex' | 'ML Coleta' | 'TikTok Shop' | 'Shein' | 'Shopee Xpress'`
       - `interface CardItem { sku: string; quantidade_necessaria: number; quantidade_separada: number; status: 'pendente' | 'separado' | 'parcial' | 'nao_encontrado' | 'aguardar_fardista'; pedido_ids: string[]; reservas: { codigo_in: string; quantidade: number; endereco: string | null; status: string }[] }`
       - `interface CardData { card_key: string; grupo_envio: string; tipo: string; importacao_numero: number; items: CardItem[]; total_pecas: number; pecas_separadas: number; atribuido_a: { id: string; nome: string } | null; urgency: UrgencyTier }`

    2. Criar `src/features/cards/lib/deadline-config.ts` com:
       - `export const DEADLINES: Record<string, number>` com os 6 valores de hora (per D-04, D-35): 'Shopee SPX': 11, 'ML Flex': 12, 'ML Coleta': 14, 'TikTok Shop': 15, 'Shein': 16, 'Shopee Xpress': 19
       - `export const COLUMN_ORDER: string[]` = ['Shopee SPX', 'ML Flex', 'ML Coleta', 'TikTok Shop', 'Shein', 'Shopee Xpress'] (per D-04)
       - `export const MARKETPLACE_COLORS: Record<string, { bg: string; text: string }>` = { 'Shopee SPX': { bg: 'bg-shopee', text: 'text-white' }, 'ML Flex': { bg: 'bg-ml', text: 'text-black' }, 'ML Coleta': { bg: 'bg-ml', text: 'text-black' }, 'TikTok Shop': { bg: 'bg-tiktok', text: 'text-black' }, 'Shein': { bg: 'bg-shein', text: 'text-white' }, 'Shopee Xpress': { bg: 'bg-shopee', text: 'text-white' } } (per D-38 a D-41)
       - `export const TYPE_ABBREV: Record<string, string>` = { 'Unitario': 'U.', 'Kit': 'K.', 'Combo': 'C.' } (per D-10)

    3. Adicionar CSS variables em `app/globals.css` dentro de `:root` (ANTES do fechamento `}`):
       ```css
       /* Marketplace colors (D-38 a D-41) */
       --shopee: 14 89% 55%;
       --ml: 54 100% 50%;
       --tiktok: 178 89% 55%;
       --shein: 0 0% 0%;
       /* Urgency colors (D-42 a D-45) */
       --urgency-overdue: 0 72% 51%;
       --urgency-warning: 45 93% 47%;
       --urgency-ok: 142 71% 45%;
       --urgency-done: 142 71% 45%;
       ```
       NAO adicionar essas vars no bloco `.dark` (per D-48, sem dark mode).

    4. Adicionar no `tailwind.config.ts` dentro de `theme.extend.colors`:
       ```typescript
       shopee: "hsl(var(--shopee))",
       ml: "hsl(var(--ml))",
       tiktok: "hsl(var(--tiktok))",
       shein: "hsl(var(--shein))",
       "urgency-overdue": "hsl(var(--urgency-overdue))",
       "urgency-warning": "hsl(var(--urgency-warning))",
       "urgency-ok": "hsl(var(--urgency-ok))",
       ```
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx contem "Inter" importado de "next/font/google" com weight ['400', '700']
    - src/features/cards/types.ts contem "export type UrgencyTier" e "export interface CardData" e "export interface CardItem"
    - src/features/cards/lib/deadline-config.ts contem "export const DEADLINES" e "'Shopee SPX': 11" e "'Shopee Xpress': 19"
    - src/features/cards/lib/deadline-config.ts contem "export const COLUMN_ORDER" e "export const MARKETPLACE_COLORS" e "export const TYPE_ABBREV"
    - app/globals.css contem "--shopee: 14 89% 55%" e "--urgency-overdue: 0 72% 51%" e "--urgency-ok: 142 71% 45%"
    - tailwind.config.ts contem 'shopee: "hsl(var(--shopee))"' e '"urgency-overdue": "hsl(var(--urgency-overdue))"'
    - tsc --noEmit passa sem erros nos novos arquivos
  </acceptance_criteria>
  <done>Fonte Inter configurada via next/font/google. Tipos exportados, constantes de deadline/cores definidas, CSS variables e Tailwind extend configurados</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Logica pura card-utils com TDD (agrupamento, urgencia, progresso, completed)</name>
  <files>
    src/features/cards/lib/card-utils.ts,
    src/features/cards/lib/__tests__/card-utils.test.ts
  </files>
  <read_first>
    src/features/cards/types.ts,
    src/features/cards/lib/deadline-config.ts,
    src/types/database.types.ts
  </read_first>
  <behavior>
    - groupByCardKey: recebe array de pedidos crus do Supabase, retorna Map<string, PedidoRow[]>
    - getUrgencyTier('Shopee SPX'): retorna 'overdue' se hora atual > 11, 'warning' se < 2h, 'ok' se > 2h
    - getUrgencyTier com progress 100%: retorna 'done' independente do horario
    - calcProgress([{quantidade: 10, quantidade_separada: 3}, {quantidade: 5, quantidade_separada: 5}]): retorna { total: 15, separadas: 8, percent: 53 }
    - isCardComplete: true quando percent === 100
    - formatCountdown(7200000): retorna "2h 0min"
    - formatCountdown(5400000): retorna "1h 30min"
    - formatCountdown(-1): retorna null (atrasado, nao formata)
    - aggregateItems: agrupa pedidos por SKU, soma quantidades, merge reservas por SKU
  </behavior>
  <action>
    RED primeiro: criar `src/features/cards/lib/__tests__/card-utils.test.ts` com testes para:
    1. `describe('groupByCardKey')`: pedidos com card_key iguais agrupam, card_keys diferentes separam
    2. `describe('getUrgencyTier')`: mock de Date para testar os 4 tiers (overdue se hora > deadline, warning se < 2h, ok se > 2h, done se progress=100%)
    3. `describe('calcProgress')`: total e separadas corretos, percent arredondado para inteiro
    4. `describe('isCardComplete')`: true quando percent===100, false caso contrario
    5. `describe('formatCountdown')`: formata ms em "Xh Ymin", retorna null se negativo
    6. `describe('aggregateItems')`: agrupa pedidos por SKU somando quantidades, merge status do progresso

    GREEN: criar `src/features/cards/lib/card-utils.ts` com:
    - `export function groupByCardKey(pedidos: PedidoRow[]): Map<string, PedidoRow[]>` — usa Array.from(Map) pattern (compat tsconfig, padrao Phase 03)
    - `export function getUrgencyTier(grupoEnvio: string, progressPercent: number, now?: Date): UrgencyTier` — aceita `now` opcional para testabilidade. Se progressPercent === 100 retorna 'done'. Calcula diff entre deadline (DEADLINES[grupoEnvio]) e now. Se diff <= 0: 'overdue'. Se diff <= 7200000 (2h em ms): 'warning'. Senao: 'ok'
    - `export function calcProgress(items: { quantidade: number; quantidade_separada: number }[]): { total: number; separadas: number; percent: number }` — percent = Math.round((separadas / total) * 100), trata total===0 como percent=0
    - `export function isCardComplete(progress: { percent: number }): boolean` — return progress.percent === 100
    - `export function formatCountdown(diffMs: number): string | null` — se diffMs <= 0 retorna null. Senao: hours = Math.floor(diffMs / 3600000), mins = Math.floor((diffMs % 3600000) / 60000), retorna `${hours}h ${mins}min`
    - `export function aggregateItems(pedidos: PedidoRow[], progressMap: Map<string, ProgressoRow>, reservasBySku: Map<string, ReservaRow[]>): CardItem[]` — agrupa por SKU. Para cada SKU soma quantidade de todos os pedidos, soma quantidade_separada do progresso, coleta pedido_ids, coleta reservas. Status: se todos separados='separado', se algum nao_encontrado='nao_encontrado', se soma_separada < soma_total e > 0 = 'parcial', senao 'pendente'

    Tipo auxiliar `PedidoRow` importado de database.types via `Tables<'pedidos'>`.
    Tipo auxiliar `ProgressoRow` via `Tables<'progresso'>`.
    Tipo auxiliar `ReservaRow` via `Tables<'reservas'>`.
  </action>
  <verify>
    <automated>npx vitest run src/features/cards/lib/__tests__/card-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/features/cards/lib/__tests__/card-utils.test.ts contem "describe('groupByCardKey')" e "describe('getUrgencyTier')" e "describe('calcProgress')" e "describe('aggregateItems')"
    - src/features/cards/lib/card-utils.ts contem "export function groupByCardKey" e "export function getUrgencyTier" e "export function calcProgress" e "export function formatCountdown" e "export function aggregateItems"
    - npx vitest run src/features/cards/lib/__tests__/card-utils.test.ts sai com exit code 0
    - Pelo menos 10 test cases passando
  </acceptance_criteria>
  <done>Todas as funcoes puras de card-utils implementadas e testadas. getUrgencyTier calcula tiers corretamente. calcProgress soma pecas. aggregateItems agrupa por SKU. Testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A neste plan | Logica pura sem acesso a DB ou input de usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | card-utils inputs | accept | Funcoes puras recebem dados ja validados do Supabase; sem input direto do usuario neste plan |
</threat_model>

<verification>
- `npx vitest run src/features/cards/lib/__tests__/card-utils.test.ts` — todos os testes passam
- `npx tsc --noEmit` — sem erros de tipo
- `grep -c 'shopee' tailwind.config.ts` retorna >= 1
- `grep -c '--shopee' app/globals.css` retorna >= 1
- `grep -c 'Inter' app/layout.tsx` retorna >= 1
</verification>

<success_criteria>
- Fonte Inter configurada em app/layout.tsx com pesos 400 e 700 via next/font/google (per D-46)
- Design tokens (marketplace + urgencia) configurados em CSS variables e Tailwind extend
- Tipos CardData, CardItem, UrgencyTier exportados e usaveis
- DEADLINES, COLUMN_ORDER, MARKETPLACE_COLORS, TYPE_ABBREV como constantes exportadas
- card-utils com 6 funcoes puras todas cobertas por testes unitarios verdes
</success_criteria>

<output>
After completion, create `.planning/phases/05-cards-e-ui-foundation/05-01-SUMMARY.md`
</output>
