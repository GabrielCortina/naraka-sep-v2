---
phase: 05-cards-e-ui-foundation
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/features/cards/components/item-modal.tsx
  - src/features/cards/components/numpad-popup.tsx
  - src/features/cards/components/assign-modal.tsx
  - src/features/cards/lib/pdf-generator.ts
  - src/features/cards/lib/__tests__/pdf-generator.test.ts
autonomous: true
requirements:
  - CARD-02
  - CARD-06
  - UIUX-05
  - UIUX-06

must_haves:
  truths:
    - "ItemModal abre com lista de itens do card mostrando SKU, quantidade necessaria e status"
    - "Itens ordenados: disponiveis no topo, bloqueados (AGUARDAR FARDISTA) no final"
    - "Cada item tem botao Confirmar (abre numpad) e Nao Tem (dispara cascata)"
    - "Itens bloqueados tem badge AGUARDAR FARDISTA e botoes desabilitados"
    - "NumpadPopup tem digitos 0-9, backspace e Confirmar verde"
    - "AssignModal lista usuarios filtrados por role"
    - "PDF generator cria checklist com grupo, tipo, importacao e tabela de SKU/quantidade"
  artifacts:
    - path: "src/features/cards/components/item-modal.tsx"
      provides: "Modal de itens do card"
      exports: ["ItemModal"]
    - path: "src/features/cards/components/numpad-popup.tsx"
      provides: "Numpad para confirmar quantidade"
      exports: ["NumpadPopup"]
    - path: "src/features/cards/components/assign-modal.tsx"
      provides: "Modal de atribuicao de usuario"
      exports: ["AssignModal"]
    - path: "src/features/cards/lib/pdf-generator.ts"
      provides: "Funcao generateChecklist para PDF"
      exports: ["generateChecklist"]
  key_links:
    - from: "src/features/cards/components/item-modal.tsx"
      to: "src/features/cards/components/numpad-popup.tsx"
      via: "import NumpadPopup"
      pattern: "import.*NumpadPopup"
    - from: "src/features/cards/components/item-modal.tsx"
      to: "src/features/cards/lib/pdf-generator.ts"
      via: "import generateChecklist"
      pattern: "import.*generateChecklist"
---

<objective>
Modais de interacao (ItemModal com lista de itens, NumpadPopup para quantidade, AssignModal para atribuicao) e geracao de PDF checklist.

Purpose: Estes modais sao as interfaces de trabalho dos separadores/fardistas — onde as acoes reais acontecem. O PDF permite checklist impresso para conferencia manual.
Output: 3 componentes modais + funcao de geracao PDF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cards-e-ui-foundation/05-CONTEXT.md
@.planning/phases/05-cards-e-ui-foundation/05-UI-SPEC.md
@.planning/phases/05-cards-e-ui-foundation/05-RESEARCH.md

<interfaces>
From src/features/cards/types.ts (Plan 01):
```typescript
export interface CardItem {
  sku: string
  quantidade_necessaria: number
  quantidade_separada: number
  status: 'pendente' | 'separado' | 'parcial' | 'nao_encontrado' | 'aguardar_fardista'
  pedido_ids: string[]
  reservas: { codigo_in: string; quantidade: number; endereco: string | null; status: string }[]
}
export interface CardData {
  card_key: string; grupo_envio: string; tipo: string; importacao_numero: number;
  items: CardItem[]; total_pecas: number; pecas_separadas: number;
  atribuido_a: { id: string; nome: string } | null; urgency: UrgencyTier
}
```

From src/features/cards/lib/deadline-config.ts (Plan 01):
```typescript
export const TYPE_ABBREV: Record<string, string>
```

From src/components/ui/dialog.tsx (Plan 02 installs):
```typescript
export { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter, DialogTrigger, DialogClose }
```

From src/components/ui/scroll-area.tsx (Plan 02 installs):
```typescript
export { ScrollArea, ScrollBar }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NumpadPopup e AssignModal</name>
  <files>
    src/features/cards/components/numpad-popup.tsx,
    src/features/cards/components/assign-modal.tsx
  </files>
  <read_first>
    src/components/ui/dialog.tsx,
    src/components/ui/button.tsx,
    src/features/cards/types.ts,
    .planning/phases/05-cards-e-ui-foundation/05-UI-SPEC.md
  </read_first>
  <action>
    1. Criar `src/features/cards/components/numpad-popup.tsx`:
       Props:
       ```typescript
       interface NumpadPopupProps {
         open: boolean
         onOpenChange: (open: boolean) => void
         quantidadeNecessaria: number
         onConfirm: (quantidade: number) => void
       }
       ```
       - Baseado em shadcn Dialog (per D-25)
       - DialogContent: max-w-xs mx-auto
       - Display area no topo:
         - Valor digitado: `<div className="text-2xl font-bold text-center py-4">{value || '0'}</div>`
         - Referencia: `<p className="text-sm text-muted-foreground text-center">Quantidade necessaria: {quantidadeNecessaria}</p>`
       - Grid 3x4 de botoes: `<div className="grid grid-cols-3 gap-2 mt-4">`
         - Botoes 1-9 (linhas 1-3): `<Button variant="outline" className="h-12 text-lg font-bold" onClick={() => appendDigit(d)}>{d}</Button>` — min 48px height per UI-SPEC
         - Linha 4: Backspace (icone Delete Lucide) | 0 | Confirmar
         - Backspace: `<Button variant="outline" className="h-12" onClick={handleBackspace}>` icone Delete
         - Zero: `<Button variant="outline" className="h-12 text-lg font-bold" onClick={() => appendDigit(0)}>0</Button>`
         - Confirmar: `<Button className="h-12 bg-green-600 hover:bg-green-700 text-white font-bold" onClick={handleConfirm}>Confirmar</Button>` (per D-25)
       - Estado: `const [value, setValue] = useState('')`
       - appendDigit: `setValue(prev => prev + String(d))` — limitar a 5 digitos max
       - handleBackspace: `setValue(prev => prev.slice(0, -1))`
       - handleConfirm: `const qty = parseInt(value || '0', 10); if (qty >= 0 && qty <= quantidadeNecessaria) { onConfirm(qty); onOpenChange(false); setValue('') }` — validacao client (per T-05-02, qty entre 0 e necessaria)
       - Reset value quando open muda para true: useEffect
       - Exportar `NumpadPopup` como named export

    2. Criar `src/features/cards/components/assign-modal.tsx`:
       Props:
       ```typescript
       interface AssignModalProps {
         open: boolean
         onOpenChange: (open: boolean) => void
         cardKey: string
         filterRole: 'separador' | 'fardista'
         users: { id: string; nome: string }[]
         currentUserId: string | null
         onAssign: (cardKey: string, userId: string) => void
       }
       ```
       - Baseado em shadcn Dialog
       - DialogContent: max-w-sm
       - DialogHeader: DialogTitle = filterRole === 'separador' ? "Atribuir Separador" : "Atribuir Fardista" (per UI-SPEC Copywriting)
       - Lista de usuarios: `<div className="flex flex-col gap-1 mt-2">`
         - Cada usuario: `<button className={cn("flex items-center gap-2 p-3 rounded-lg w-full text-left hover:bg-muted", user.id === currentUserId && "bg-muted font-bold")} onClick={() => { onAssign(cardKey, user.id); onOpenChange(false) }}>`
           - Icone User (Lucide, 16px)
           - `<span>{user.nome}</span>`
         - Se currentUserId: mostrar check icon ao lado do usuario selecionado
       - Exportar `AssignModal` como named export
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/features/cards/components/numpad-popup.tsx contém "export function NumpadPopup" e "Quantidade necessaria" e "bg-green-600" e "Confirmar" e "grid grid-cols-3"
    - numpad-popup.tsx contém validacao "qty >= 0 && qty <= quantidadeNecessaria"
    - src/features/cards/components/assign-modal.tsx contém "export function AssignModal" e "Atribuir Separador" e "Atribuir Fardista" e "onAssign"
    - tsc --noEmit passa sem erros
  </acceptance_criteria>
  <done>NumpadPopup com grid 3x4, digitos 0-9, backspace, Confirmar verde, validacao 0..max. AssignModal com lista de usuarios filtrada por role.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ItemModal e PDF generator com TDD</name>
  <files>
    src/features/cards/components/item-modal.tsx,
    src/features/cards/lib/pdf-generator.ts,
    src/features/cards/lib/__tests__/pdf-generator.test.ts
  </files>
  <read_first>
    src/components/ui/dialog.tsx,
    src/components/ui/scroll-area.tsx,
    src/features/cards/components/numpad-popup.tsx,
    src/features/cards/types.ts,
    src/features/cards/lib/deadline-config.ts
  </read_first>
  <behavior>
    - generateChecklist recebe CardData e gera PDF com titulo contendo grupo_envio, tipo, importacao
    - generateChecklist cria tabela com colunas SKU, Qtd, Check
    - generateChecklist nao quebra com acentos PT-BR
  </behavior>
  <action>
    1. RED: Criar `src/features/cards/lib/__tests__/pdf-generator.test.ts`:
       - Mock jspdf e jspdf-autotable (vi.mock('jspdf'))
       - Testar que generateChecklist chama doc.text com titulo contendo grupo_envio
       - Testar que autoTable e chamado com head ['SKU', 'Qtd', 'Check']
       - Testar que body tem uma linha por item do card com sku, quantidade e '[ ]'
       - Testar que doc.save e chamado com nome contendo card_key

    2. GREEN: Criar `src/features/cards/lib/pdf-generator.ts`:
       ```typescript
       import jsPDF from 'jspdf'
       import autoTable from 'jspdf-autotable'
       import type { CardData } from '../types'

       export function generateChecklist(card: CardData): void {
         const doc = new jsPDF()
         doc.setFontSize(14)
         doc.text(
           `${card.grupo_envio} - ${card.tipo} - Importacao #${card.importacao_numero}`,
           14, 20
         )

         autoTable(doc, {
           startY: 30,
           head: [['SKU', 'Qtd', 'Check']],
           body: card.items.map(item => [
             item.sku,
             String(item.quantidade_necessaria),
             '[ ]'
           ]),
           styles: { fontSize: 10 },
         })

         doc.save(`checklist-${card.card_key}.pdf`)
       }
       ```

    3. Criar `src/features/cards/components/item-modal.tsx`:
       Props:
       ```typescript
       interface ItemModalProps {
         open: boolean
         onOpenChange: (open: boolean) => void
         card: CardData | null
         onConfirmQuantity: (cardKey: string, sku: string, quantidade: number) => void
         onNaoTem: (cardKey: string, sku: string) => void
       }
       ```
       - Baseado em shadcn Dialog
       - Mobile: DialogContent com `className="sm:max-w-lg max-h-[80vh] p-0 gap-0"` — mobile full-screen via `data-[state=open]:slide-in-from-bottom` (per UI-SPEC)
       - Se card === null: nao renderiza conteudo

       **Header (sticky top):**
       - DialogHeader com padding p-4 border-b
       - DialogTitle: `{card.grupo_envio} - {TYPE_ABBREV[card.tipo] || card.tipo} #{card.importacao_numero}`

       **Body (scrollable):**
       - ScrollArea com `className="flex-1 px-4"` per UI-SPEC
       - Itens ordenados per D-22: disponiveis no topo, bloqueados no final
         ```typescript
         const sortedItems = [...card.items].sort((a, b) => {
           if (a.status === 'aguardar_fardista' && b.status !== 'aguardar_fardista') return 1
           if (a.status !== 'aguardar_fardista' && b.status === 'aguardar_fardista') return -1
           return 0
         })
         ```
       - D-23: Se item tem reservas, cada reserva aparece como sublinha com quantidade especifica
       - D-24: Itens de prateleira (sem reservas) ficam sempre desbloqueados

       **Cada item:**
       - Container: `<div className="flex items-center justify-between py-3 border-b last:border-0">`
       - Esquerda: SKU (`text-sm font-bold`) + quantidade `{item.quantidade_separada}/{item.quantidade_necessaria}` (text-xs text-muted-foreground) + status badge se parcial/separado/nao_encontrado
       - Se status === 'aguardar_fardista': badge `<Badge variant="outline" className="text-xs">AGUARDAR FARDISTA</Badge>` (per D-21)
       - Direita (flex gap-2):
         - Botao Confirmar: `<Button size="sm" variant="outline" onClick={() => openNumpad(item)} disabled={item.status === 'aguardar_fardista'}>Confirmar</Button>` (per D-20)
         - Botao Nao Tem: `<Button size="sm" variant="destructive" onClick={() => onNaoTem(card.card_key, item.sku)} disabled={item.status === 'aguardar_fardista'}>Nao Tem</Button>` (per D-20)
       - Se item.status === 'separado': ambos botoes desabilitados, mostrar check icon verde

       **Footer (sticky bottom):**
       - DialogFooter com padding p-4 border-t
       - `<Button variant="outline" className="w-full" onClick={() => generateChecklist(card)}><Printer className="w-4 h-4 mr-2" />Imprimir Checklist</Button>` (per D-32, D-33)

       **NumpadPopup integrado:**
       - Estado local: `const [numpadOpen, setNumpadOpen] = useState(false)` e `const [selectedItem, setSelectedItem] = useState<CardItem | null>(null)`
       - Ao clicar Confirmar no item: setSelectedItem(item), setNumpadOpen(true)
       - NumpadPopup recebe quantidadeNecessaria do selectedItem e onConfirm chama onConfirmQuantity

       - Exportar `ItemModal` como named export
  </action>
  <verify>
    <automated>npx vitest run src/features/cards/lib/__tests__/pdf-generator.test.ts && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/features/cards/lib/pdf-generator.ts contém "export function generateChecklist" e "jsPDF" e "autoTable" e "['SKU', 'Qtd', 'Check']"
    - src/features/cards/lib/__tests__/pdf-generator.test.ts contém "generateChecklist" e "vi.mock"
    - src/features/cards/components/item-modal.tsx contém "export function ItemModal" e "AGUARDAR FARDISTA" e "NumpadPopup" e "Imprimir Checklist" e "generateChecklist" e "Nao Tem"
    - item-modal.tsx contém "aguardar_fardista" na logica de sort (D-22)
    - item-modal.tsx contém "disabled" condicionado a aguardar_fardista (D-21)
    - vitest run pdf-generator.test.ts sai com exit code 0
    - tsc --noEmit passa sem erros
  </acceptance_criteria>
  <done>ItemModal com lista de itens, botoes Confirmar/Nao Tem, itens bloqueados desabilitados, NumpadPopup integrado, botao Imprimir Checklist no footer. PDF generator testado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client input -> numpad | Quantidade digitada pelo usuario pode ser qualquer numero |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | NumpadPopup | mitigate | Validacao client: qty >= 0 && qty <= quantidadeNecessaria. Validacao server sera no route handler (Phase 7) |
| T-05-05 | Elevation of Privilege | AssignModal | mitigate | Props filterRole restringe lista de usuarios. RLS no Supabase verifica role antes de INSERT em atribuicoes (Plan 05 migration) |
</threat_model>

<verification>
- `npx vitest run src/features/cards/lib/__tests__/pdf-generator.test.ts` — testes passam
- `npx tsc --noEmit` — sem erros
- `ls src/features/cards/components/` — item-modal.tsx, numpad-popup.tsx, assign-modal.tsx existem
</verification>

<success_criteria>
- ItemModal funcional com lista de itens, ordenacao D-22, botoes D-20, badge D-21, print D-32/D-33
- NumpadPopup com grid 3x4, validacao 0..max, Confirmar verde
- AssignModal com lista filtrada por role
- PDF generator cria checklist correto com TDD
</success_criteria>

<output>
After completion, create `.planning/phases/05-cards-e-ui-foundation/05-04-SUMMARY.md`
</output>
