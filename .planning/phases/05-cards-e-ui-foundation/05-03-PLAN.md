---
phase: 05-cards-e-ui-foundation
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/features/cards/components/kanban-column.tsx
  - src/features/cards/components/kanban-board.tsx
  - src/features/cards/components/completed-section.tsx
autonomous: true
requirements:
  - CARD-07
  - CARD-08
  - CARD-09
  - UIUX-02
  - UIUX-03

must_haves:
  truths:
    - "Desktop: colunas horizontais por metodo de envio com scroll horizontal"
    - "Mobile: secoes verticais colapsaveis por metodo de envio"
    - "Colunas ordenadas por prazo (SPX primeiro, Xpress ultimo)"
    - "Colunas vazias nao aparecem"
    - "Cards 100% completos vao para secao CONCLUIDOS automaticamente"
    - "Secao CONCLUIDOS colapsavel, comeca fechada"
    - "Cards atrasados ficam no topo da coluna"
  artifacts:
    - path: "src/features/cards/components/kanban-board.tsx"
      provides: "Layout kanban completo desktop + mobile"
      exports: ["KanbanBoard"]
    - path: "src/features/cards/components/kanban-column.tsx"
      provides: "Coluna individual com cards empilhados"
      exports: ["KanbanColumn"]
    - path: "src/features/cards/components/completed-section.tsx"
      provides: "Secao CONCLUIDOS colapsavel"
      exports: ["CompletedSection"]
  key_links:
    - from: "src/features/cards/components/kanban-board.tsx"
      to: "src/features/cards/components/kanban-column.tsx"
      via: "import KanbanColumn"
      pattern: "import.*KanbanColumn"
    - from: "src/features/cards/components/kanban-board.tsx"
      to: "src/features/cards/components/completed-section.tsx"
      via: "import CompletedSection"
      pattern: "import.*CompletedSection"
---

<objective>
Layout KanbanBoard com colunas por metodo de envio (desktop horizontal, mobile colapsavel vertical) e secao CONCLUIDOS.

Purpose: Montar a estrutura visual principal onde os OrderCards serao renderizados. Layout responsivo e o nucleo da experiencia de uso.
Output: KanbanBoard, KanbanColumn, CompletedSection — layout completo desktop + mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cards-e-ui-foundation/05-CONTEXT.md
@.planning/phases/05-cards-e-ui-foundation/05-UI-SPEC.md
@.planning/phases/05-cards-e-ui-foundation/05-01-SUMMARY.md
@.planning/phases/05-cards-e-ui-foundation/05-02-SUMMARY.md

<interfaces>
From src/features/cards/types.ts (Plan 01):
```typescript
export type UrgencyTier = 'overdue' | 'warning' | 'ok' | 'done'
export interface CardData {
  card_key: string; grupo_envio: string; tipo: string; importacao_numero: number;
  items: CardItem[]; total_pecas: number; pecas_separadas: number;
  atribuido_a: { id: string; nome: string } | null; urgency: UrgencyTier
}
```

From src/features/cards/lib/deadline-config.ts (Plan 01):
```typescript
export const COLUMN_ORDER: string[] // ['Shopee SPX', 'ML Flex', 'ML Coleta', 'TikTok Shop', 'Shein', 'Shopee Xpress']
export const MARKETPLACE_COLORS: Record<string, { bg: string; text: string }>
```

From src/features/cards/lib/card-utils.ts (Plan 01):
```typescript
export function isCardComplete(progress: { percent: number }): boolean
export function calcProgress(items: ...): { total: number; separadas: number; percent: number }
```

From src/features/cards/components/order-card.tsx (Plan 02):
```typescript
export function OrderCard({ card, onOpenModal, onAssign }: { card: CardData; onOpenModal: (key: string) => void; onAssign: (key: string) => void }): JSX.Element
```

From src/features/cards/components/marketplace-badge.tsx (Plan 02):
```typescript
export function MarketplaceBadge({ grupoEnvio }: { grupoEnvio: string }): JSX.Element
```

From src/components/ui/collapsible.tsx (Plan 02 installs):
```typescript
export { Collapsible, CollapsibleTrigger, CollapsibleContent }
```

From src/components/ui/scroll-area.tsx (Plan 02 installs):
```typescript
export { ScrollArea, ScrollBar }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KanbanColumn com layout desktop e mobile colapsavel</name>
  <files>src/features/cards/components/kanban-column.tsx</files>
  <read_first>
    src/components/ui/collapsible.tsx,
    src/features/cards/components/order-card.tsx,
    src/features/cards/components/marketplace-badge.tsx,
    src/features/cards/lib/deadline-config.ts
  </read_first>
  <action>
    Criar `src/features/cards/components/kanban-column.tsx`:

    Props:
    ```typescript
    interface KanbanColumnProps {
      grupoEnvio: string
      cards: CardData[]
      onOpenModal: (cardKey: string) => void
      onAssign: (cardKey: string) => void
    }
    ```

    **Desktop (>=768px) — visivel via `hidden md:block`:**
    - Container: `w-[240px] min-w-[240px] flex flex-col gap-2` (per D-02, 240px dentro do range 220-260)
    - Header: MarketplaceBadge com grupoEnvio + contagem de cards `<span className="text-xs text-muted-foreground ml-1">({cards.length})</span>`
    - Cards empilhados: `<div className="flex flex-col gap-2">` com cada card renderizado por OrderCard
    - Cards ordenados: overdue primeiro, depois warning, depois ok (per D-36 — atrasados no topo)

    **Mobile (<768px) — visivel via `md:hidden`:**
    - Usar Collapsible (shadcn) per D-12
    - CollapsibleTrigger: `<div className="flex items-center justify-between p-3 bg-white rounded-lg shadow-sm">` com MarketplaceBadge + contagem + ChevronDown icon (Lucide, rotaciona quando aberto)
    - CollapsibleContent: cards full-width `<div className="flex flex-col gap-2 mt-2">`
    - Default open: se algum card tem urgency === 'overdue' ou 'warning' (per D-14)
    - `const defaultOpen = cards.some(c => c.urgency === 'overdue' || c.urgency === 'warning')`
    - Usar useState para controlar open, inicializado com defaultOpen

    Ordenacao de cards dentro da coluna (ambos layouts):
    ```typescript
    const sorted = [...cards].sort((a, b) => {
      const tierOrder = { overdue: 0, warning: 1, ok: 2, done: 3 }
      return (tierOrder[a.urgency] ?? 2) - (tierOrder[b.urgency] ?? 2)
    })
    ```

    Exportar `KanbanColumn` como named export.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/features/cards/components/kanban-column.tsx contém "export function KanbanColumn"
    - Arquivo contém "hidden md:block" e "md:hidden" para responsividade
    - Arquivo contém "Collapsible" e "CollapsibleTrigger" e "CollapsibleContent"
    - Arquivo contém "w-[240px]" e "min-w-[240px]"
    - Arquivo contém "tierOrder" para ordenacao por urgencia
    - Arquivo contém "defaultOpen" com check de overdue/warning
    - tsc --noEmit passa sem erros
  </acceptance_criteria>
  <done>KanbanColumn renderiza coluna fixa no desktop e secao colapsavel no mobile, cards ordenados por urgencia</done>
</task>

<task type="auto">
  <name>Task 2: KanbanBoard e CompletedSection com layout responsivo completo</name>
  <files>
    src/features/cards/components/kanban-board.tsx,
    src/features/cards/components/completed-section.tsx
  </files>
  <read_first>
    src/features/cards/components/kanban-column.tsx,
    src/features/cards/lib/deadline-config.ts,
    src/features/cards/lib/card-utils.ts,
    src/components/ui/scroll-area.tsx,
    src/components/ui/collapsible.tsx
  </read_first>
  <action>
    1. Criar `src/features/cards/components/completed-section.tsx`:
       Props: `{ cards: CardData[]; onOpenModal: (key: string) => void; onAssign: (key: string) => void }`
       - Usar Collapsible com defaultOpen={false} (per D-07, comeca fechada)
       - CollapsibleTrigger: `<div className="flex items-center justify-between p-3 bg-white rounded-lg shadow-sm mt-4">` com texto "CONCLUIDOS" (uppercase, font-bold, text-sm, text-muted-foreground) + contagem `({cards.length})` + ChevronDown icon
       - CollapsibleContent: grid de cards completos
         - Desktop: `<div className="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-4 gap-2 mt-2">`
         - Cada card renderizado por OrderCard
       - Se cards.length === 0: nao renderiza nada (return null)
       - Exportar `CompletedSection` como named export

    2. Criar `src/features/cards/components/kanban-board.tsx`:
       Props:
       ```typescript
       interface KanbanBoardProps {
         cards: CardData[]
         onOpenModal: (cardKey: string) => void
         onAssign: (cardKey: string) => void
       }
       ```

       Logica interna:
       - Separar cards completos vs incompletos: `const completed = cards.filter(c => c.urgency === 'done')` e `const active = cards.filter(c => c.urgency !== 'done')` (per CARD-08)
       - Agrupar active por grupo_envio: `const byGroup = new Map<string, CardData[]>()` — iterar active e agrupar
       - Filtrar COLUMN_ORDER para apenas grupos com cards (per D-05, colunas vazias ocultas): `const visibleColumns = COLUMN_ORDER.filter(g => byGroup.has(g))`

       **Desktop layout (>=768px):**
       - Container: `<div className="hidden md:block">`
       - Usar ScrollArea (shadcn) com ScrollBar orientation="horizontal" para scroll horizontal
       - Dentro: `<div className="flex gap-6 p-4">` (per D-06, fundo cinza, gap entre colunas ~24px)
       - Para cada grupo em visibleColumns: `<KanbanColumn grupoEnvio={grupo} cards={byGroup.get(grupo)!} onOpenModal={onOpenModal} onAssign={onAssign} />`

       **Mobile layout (<768px):**
       - Container: `<div className="md:hidden flex flex-col gap-3 p-4">`
       - Para cada grupo em visibleColumns: KanbanColumn (que internamente renderiza Collapsible)

       **Secao CONCLUIDOS (ambos):**
       - `<CompletedSection cards={completed} onOpenModal={onOpenModal} onAssign={onAssign} />`
       - Renderizado abaixo de ambos os layouts (desktop e mobile)

       **Empty state:**
       - Se cards.length === 0: renderizar empty state com icone Package (Lucide, 48px, text-muted-foreground) + "Nenhum pedido para separar" (h2, font-semibold) + "Faca o upload da planilha ERP para comecar a separacao." (text-muted-foreground) per UI-SPEC Copywriting

       - Fundo geral: `<div className="bg-zinc-100 min-h-[calc(100vh-4rem)] rounded-lg">` (per D-06, #f4f4f5 = zinc-100)
       - Exportar `KanbanBoard` como named export
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/features/cards/components/completed-section.tsx contém "export function CompletedSection" e "CONCLUIDOS" e "Collapsible" e "defaultOpen" com valor false
    - src/features/cards/components/kanban-board.tsx contém "export function KanbanBoard" e "ScrollArea" e "COLUMN_ORDER" e "hidden md:block" e "md:hidden" e "Nenhum pedido para separar"
    - kanban-board.tsx contém "CompletedSection" e "KanbanColumn"
    - kanban-board.tsx contém "bg-zinc-100" para fundo
    - tsc --noEmit passa sem erros
  </acceptance_criteria>
  <done>KanbanBoard renderiza layout kanban horizontal desktop com scroll, secoes colapsaveis mobile, colunas ordenadas por prazo, vazias ocultas, secao CONCLUIDOS colapsavel no final</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A neste plan | Componentes de layout sem acesso a DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | KanbanBoard | accept | Board recebe cards ja filtrados — filtragem por role sera no Plan 05 (data layer) |
</threat_model>

<verification>
- `npx tsc --noEmit` — sem erros
- `ls src/features/cards/components/` — kanban-board.tsx, kanban-column.tsx, completed-section.tsx existem
- `grep "COLUMN_ORDER" src/features/cards/components/kanban-board.tsx` — presente
</verification>

<success_criteria>
- KanbanBoard funcional com layout desktop (colunas horizontais com scroll) e mobile (secoes colapsaveis)
- Colunas ordenadas por prazo, vazias ocultas
- CompletedSection colapsavel no final, comeca fechada
- Cards ordenados por urgencia dentro de cada coluna
- Empty state renderizado quando sem pedidos
</success_criteria>

<output>
After completion, create `.planning/phases/05-cards-e-ui-foundation/05-03-SUMMARY.md`
</output>
