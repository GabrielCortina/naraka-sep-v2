---
phase: 05-cards-e-ui-foundation
plan: 05
type: execute
wave: 3
depends_on: ["05-03", "05-04"]
files_modified:
  - src/features/cards/hooks/use-countdown.ts
  - src/features/cards/hooks/use-cards-realtime.ts
  - src/features/cards/hooks/use-card-data.ts
  - src/features/cards/hooks/__tests__/use-countdown.test.ts
  - app/(authenticated)/prateleira/page.tsx
  - app/(authenticated)/fardos/page.tsx
  - supabase/migrations/20260405_realtime_publication.sql
autonomous: false
requirements:
  - CARD-04
  - CARD-06
  - CARD-07
  - UIUX-02
  - UIUX-03
user_setup:
  - service: supabase
    why: "Migration SQL para habilitar Realtime publication nas tabelas"
    dashboard_config:
      - task: "Executar migration via supabase db push ou SQL Editor"
        location: "Supabase Dashboard -> SQL Editor (se nao usar CLI)"

must_haves:
  truths:
    - "useCountdown retorna contagem regressiva atualizada a cada minuto"
    - "useCardsRealtime mantem dados de cards sincronizados com Supabase em tempo real"
    - "useCardData faz query inicial de pedidos, progresso, reservas e atribuicoes e monta CardData[]"
    - "Separador so ve cards atribuidos a ele na pagina /prateleira"
    - "Fardista so ve cards atribuidos a ele na pagina /fardos"
    - "Lider/admin ve todos os cards"
    - "Pagina /prateleira renderiza KanbanBoard com dados reais"
    - "Pagina /fardos renderiza KanbanBoard com dados reais"
    - "Tabelas progresso e atribuicoes estao na Supabase Realtime publication"
  artifacts:
    - path: "src/features/cards/hooks/use-countdown.ts"
      provides: "Hook de contagem regressiva"
      exports: ["useCountdown"]
    - path: "src/features/cards/hooks/use-cards-realtime.ts"
      provides: "Hook de realtime subscription"
      exports: ["useCardsRealtime"]
    - path: "src/features/cards/hooks/use-card-data.ts"
      provides: "Hook de query e montagem de CardData"
      exports: ["useCardData"]
    - path: "app/(authenticated)/prateleira/page.tsx"
      provides: "Pagina de prateleira com KanbanBoard real"
      min_lines: 30
    - path: "app/(authenticated)/fardos/page.tsx"
      provides: "Pagina de fardos com KanbanBoard real"
      min_lines: 30
    - path: "supabase/migrations/20260405_realtime_publication.sql"
      provides: "Migration para habilitar realtime"
      contains: "ALTER PUBLICATION supabase_realtime"
  key_links:
    - from: "app/(authenticated)/prateleira/page.tsx"
      to: "src/features/cards/hooks/use-card-data.ts"
      via: "import useCardData"
      pattern: "import.*useCardData"
    - from: "src/features/cards/hooks/use-card-data.ts"
      to: "src/features/cards/hooks/use-cards-realtime.ts"
      via: "import useCardsRealtime"
      pattern: "import.*useCardsRealtime"
    - from: "src/features/cards/hooks/use-cards-realtime.ts"
      to: "src/lib/supabase"
      via: "import createClient"
      pattern: "import.*supabase"
---

<objective>
Hooks de dados (query, realtime, countdown), migration SQL para publication, e wiring nas paginas /prateleira e /fardos com dados reais do Supabase.

Purpose: Conectar toda a UI construida nos plans anteriores com dados reais do banco. Esta e a plan que faz o sistema funcionar end-to-end. Inclui visibilidade por role (separador/fardista so ve seus cards).
Output: 3 hooks, migration SQL, paginas /prateleira e /fardos funcionais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cards-e-ui-foundation/05-CONTEXT.md
@.planning/phases/05-cards-e-ui-foundation/05-RESEARCH.md
@.planning/phases/05-cards-e-ui-foundation/05-01-SUMMARY.md
@.planning/phases/05-cards-e-ui-foundation/05-02-SUMMARY.md
@.planning/phases/05-cards-e-ui-foundation/05-03-SUMMARY.md
@.planning/phases/05-cards-e-ui-foundation/05-04-SUMMARY.md

<interfaces>
From src/features/cards/types.ts:
```typescript
export interface CardData { card_key, grupo_envio, tipo, importacao_numero, items: CardItem[], total_pecas, pecas_separadas, atribuido_a, urgency }
export interface CardItem { sku, quantidade_necessaria, quantidade_separada, status, pedido_ids, reservas }
```

From src/features/cards/lib/card-utils.ts:
```typescript
export function groupByCardKey(pedidos: PedidoRow[]): Map<string, PedidoRow[]>
export function getUrgencyTier(grupoEnvio: string, progressPercent: number, now?: Date): UrgencyTier
export function calcProgress(items): { total, separadas, percent }
export function aggregateItems(pedidos, progressMap, reservasBySku): CardItem[]
```

From src/features/cards/lib/deadline-config.ts:
```typescript
export const DEADLINES: Record<string, number>
```

From src/features/cards/components/kanban-board.tsx:
```typescript
export function KanbanBoard({ cards, onOpenModal, onAssign }: KanbanBoardProps)
```

From src/features/cards/components/item-modal.tsx:
```typescript
export function ItemModal({ open, onOpenChange, card, onConfirmQuantity, onNaoTem }: ItemModalProps)
```

From src/features/cards/components/assign-modal.tsx:
```typescript
export function AssignModal({ open, onOpenChange, cardKey, filterRole, users, currentUserId, onAssign }: AssignModalProps)
```

From src/lib/supabase/browser.ts:
```typescript
export function createBrowserClient(): SupabaseClient<Database>
```

From src/lib/supabase/server.ts:
```typescript
export function createServerClient(): SupabaseClient<Database>
```

From src/features/auth/lib/role-config.ts:
```typescript
export const ROLE_ROUTES: Record<UserRole, string[]>
// roles: 'admin' | 'lider' | 'separador' | 'fardista'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hook useCountdown com TDD, useCardsRealtime e useCardData</name>
  <files>
    src/features/cards/hooks/use-countdown.ts,
    src/features/cards/hooks/__tests__/use-countdown.test.ts,
    src/features/cards/hooks/use-cards-realtime.ts,
    src/features/cards/hooks/use-card-data.ts
  </files>
  <read_first>
    src/features/cards/lib/deadline-config.ts,
    src/features/cards/lib/card-utils.ts,
    src/features/cards/types.ts,
    src/lib/supabase/browser.ts,
    src/types/database.types.ts
  </read_first>
  <behavior>
    - useCountdown('Shopee SPX'): retorna { countdown: "Xh Ymin" | null, urgency: UrgencyTier } atualizado a cada 60s
    - useCountdown quando past deadline: retorna { countdown: null, urgency: 'overdue' }
    - useCountdown com progress 100%: retorna { countdown: null, urgency: 'done' }
  </behavior>
  <action>
    1. RED: Criar `src/features/cards/hooks/__tests__/use-countdown.test.ts`:
       - Usar vi.useFakeTimers() para controlar Date
       - Testar: antes do deadline retorna ok/warning com countdown string
       - Testar: apos deadline retorna overdue com countdown null
       - Testar: progress 100% retorna done com countdown null
       - Testar: atualiza a cada 60s (vi.advanceTimersByTime(60000))

    2. GREEN: Criar `src/features/cards/hooks/use-countdown.ts`:
       ```typescript
       'use client'
       import { useState, useEffect } from 'react'
       import { DEADLINES } from '../lib/deadline-config'
       import { getUrgencyTier, formatCountdown } from '../lib/card-utils'
       import type { UrgencyTier } from '../types'

       export function useCountdown(grupoEnvio: string, progressPercent: number): { countdown: string | null; urgency: UrgencyTier } {
         const [now, setNow] = useState(() => new Date())

         useEffect(() => {
           const interval = setInterval(() => setNow(new Date()), 60_000) // 1min
           return () => clearInterval(interval)
         }, [])

         const urgency = getUrgencyTier(grupoEnvio, progressPercent, now)
         if (urgency === 'done' || urgency === 'overdue') {
           return { countdown: null, urgency }
         }

         const deadlineHour = DEADLINES[grupoEnvio]
         if (!deadlineHour) return { countdown: null, urgency: 'ok' }
         const deadline = new Date(now)
         deadline.setHours(deadlineHour, 0, 0, 0)
         const diffMs = deadline.getTime() - now.getTime()
         const countdown = formatCountdown(diffMs)
         return { countdown, urgency }
       }
       ```

    3. Criar `src/features/cards/hooks/use-cards-realtime.ts`:
       ```typescript
       'use client'
       import { useEffect } from 'react'
       import { createBrowserClient } from '@/lib/supabase/browser'

       export function useCardsRealtime(onUpdate: () => void) {
         useEffect(() => {
           const supabase = createBrowserClient()
           const channel = supabase
             .channel('cards-realtime')
             .on('postgres_changes', { event: '*', schema: 'public', table: 'progresso' }, () => onUpdate())
             .on('postgres_changes', { event: '*', schema: 'public', table: 'atribuicoes' }, () => onUpdate())
             .on('postgres_changes', { event: '*', schema: 'public', table: 'reservas' }, () => onUpdate())
             .on('postgres_changes', { event: '*', schema: 'public', table: 'trafego_fardos' }, () => onUpdate())
             .subscribe()

           return () => { supabase.removeChannel(channel) }
         }, [onUpdate])
       }
       ```
       Pattern per RESEARCH.md: um unico channel com multiplos .on() para evitar memory leak (Pitfall 2).

    4. Criar `src/features/cards/hooks/use-card-data.ts`:
       ```typescript
       'use client'
       import { useState, useEffect, useCallback } from 'react'
       import { createBrowserClient } from '@/lib/supabase/browser'
       import { groupByCardKey, calcProgress, aggregateItems, getUrgencyTier } from '../lib/card-utils'
       import { useCardsRealtime } from './use-cards-realtime'
       import type { CardData } from '../types'
       ```

       Hook: `export function useCardData(userId: string, userRole: string): { cards: CardData[]; loading: boolean; error: string | null }`

       Logica fetchCards:
       - Query `pedidos` com select('*')
       - Query `progresso` com select('*')
       - Query `reservas` com select('*')
       - Query `atribuicoes` com select('*, users(nome)')
       - Se role === 'separador' ou 'fardista' (per D-29): filtrar por atribuicoes onde user_id === userId. Obter card_keys atribuidos. Filtrar pedidos por esses card_keys.
       - Se role === 'admin' ou 'lider': sem filtro (per D-29)
       - Agrupar pedidos por card_key via groupByCardKey
       - Para cada grupo: montar CardData com aggregateItems, calcProgress, getUrgencyTier
       - Montar atribuido_a a partir de atribuicoes join users

       Realtime: `useCardsRealtime(useCallback(fetchCards, [userId, userRole]))`
       — Cada update no realtime dispara refetch completo (simplicidade > granularidade nesta fase)

       Retornar { cards, loading, error } com estados de loading e toast de erro (per UI-SPEC Copywriting: "Erro ao carregar pedidos. Tente atualizar a pagina.")
  </action>
  <verify>
    <automated>npx vitest run src/features/cards/hooks/__tests__/use-countdown.test.ts && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/features/cards/hooks/use-countdown.ts contém "export function useCountdown" e "'use client'" e "setInterval" e "60_000"
    - src/features/cards/hooks/__tests__/use-countdown.test.ts contém "useFakeTimers" e "useCountdown" e pelo menos 3 test cases
    - src/features/cards/hooks/use-cards-realtime.ts contém "export function useCardsRealtime" e "'use client'" e "postgres_changes" e "progresso" e "atribuicoes" e "removeChannel"
    - src/features/cards/hooks/use-card-data.ts contém "export function useCardData" e "'use client'" e "groupByCardKey" e "aggregateItems" e "useCardsRealtime"
    - use-card-data.ts contém logica de filtragem por role: "separador" e "fardista" e "card_keys"
    - vitest run use-countdown.test.ts sai com exit code 0
    - tsc --noEmit passa sem erros
  </acceptance_criteria>
  <done>3 hooks criados: useCountdown (atualiza a cada 60s), useCardsRealtime (subscription Supabase 4 tabelas), useCardData (query + montagem + filtro por role). useCountdown testado via TDD.</done>
</task>

<task type="auto">
  <name>Task 2: Migration Realtime publication e wiring nas paginas /prateleira e /fardos</name>
  <files>
    supabase/migrations/20260405_realtime_publication.sql,
    app/(authenticated)/prateleira/page.tsx,
    app/(authenticated)/fardos/page.tsx
  </files>
  <read_first>
    app/(authenticated)/prateleira/page.tsx,
    app/(authenticated)/fardos/page.tsx,
    app/(authenticated)/layout.tsx,
    src/features/cards/hooks/use-card-data.ts,
    src/features/cards/components/kanban-board.tsx,
    src/features/cards/components/item-modal.tsx,
    src/features/cards/components/assign-modal.tsx,
    src/lib/supabase/server.ts
  </read_first>
  <action>
    1. Criar `supabase/migrations/20260405_realtime_publication.sql`:
       ```sql
       -- Habilitar Realtime publication para tabelas usadas nos cards
       -- Pitfall 1 do RESEARCH.md: sem isso, subscriptions conectam mas nao recebem eventos
       ALTER PUBLICATION supabase_realtime ADD TABLE progresso;
       ALTER PUBLICATION supabase_realtime ADD TABLE atribuicoes;
       ALTER PUBLICATION supabase_realtime ADD TABLE reservas;
       ALTER PUBLICATION supabase_realtime ADD TABLE trafego_fardos;
       ```

    2. Reescrever `app/(authenticated)/prateleira/page.tsx`:
       - Server component wrapper que obtem user via getUser() + users table
       - Passa userId e userRole para client component PrateleiraClient
       - Criar inline ou como componente separado `PrateleiraClient` ('use client'):
         - Usa useCardData(userId, userRole) para obter cards
         - Filtra cards que tem items de prateleira (items com status !== 'aguardar_fardista' OU items sem reservas) -- prateleira mostra items que vao ser separados da prateleira
         - Renderiza KanbanBoard com cards filtrados
         - Estado local para ItemModal: selectedCardKey, itemModalOpen
         - Estado local para AssignModal: assignCardKey, assignModalOpen
         - Busca lista de separadores via query Supabase users onde role='separador' e ativo=true (para AssignModal filterRole='separador')
         - onOpenModal: abre ItemModal com card selecionado
         - onAssign: abre AssignModal com card selecionado
         - onConfirmQuantity: `await supabase.from('progresso').upsert({ pedido_id, quantidade_separada, status })` — atualiza progresso no banco
         - onNaoTem: `await supabase.from('progresso').upsert({ pedido_id, quantidade_separada: 0, status: 'nao_encontrado' })` — marca como N/E (cascata sera implementada na Phase 7)
         - onAssignUser: `await supabase.from('atribuicoes').upsert({ card_key, user_id, tipo: 'prateleira' }, { onConflict: 'card_key,tipo' })` — atribui separador

       - Metadata: `{ title: 'Prateleira | NARAKA SEP v2' }` — manter

    3. Reescrever `app/(authenticated)/fardos/page.tsx`:
       - Mesma estrutura de PrateleiraClient mas:
         - Filtra cards que tem fardos reservados (items com reservas.length > 0)
         - AssignModal filterRole='fardista'
         - onAssign tipo: 'fardos'
         - Busca lista de fardistas (role='fardista', ativo=true)
       - Metadata: `{ title: 'Fardos | NARAKA SEP v2' }` — manter

    **Nota sobre seguranca (per D-29, T-05-01):**
    - A filtragem por role ocorre em useCardData que filtra por card_keys atribuidos
    - RLS policies no Supabase devem ser adicionadas nas tabelas para garantir server-side (per ASVS V4)
    - Nesta phase, a filtragem client-side via useCardData e suficiente; RLS sera hardened quando as acoes reais forem implementadas (Phase 6/7)
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260405_realtime_publication.sql contém "ALTER PUBLICATION supabase_realtime ADD TABLE progresso" e "ADD TABLE atribuicoes"
    - app/(authenticated)/prateleira/page.tsx contém "useCardData" e "KanbanBoard" e "ItemModal" e "AssignModal" e "Prateleira | NARAKA SEP v2"
    - app/(authenticated)/prateleira/page.tsx contém "separador" na logica de AssignModal filterRole
    - app/(authenticated)/fardos/page.tsx contém "useCardData" e "KanbanBoard" e "ItemModal" e "AssignModal" e "Fardos | NARAKA SEP v2"
    - app/(authenticated)/fardos/page.tsx contém "fardista" na logica de AssignModal filterRole
    - tsc --noEmit passa sem erros
  </acceptance_criteria>
  <done>Migration SQL para Realtime publication criada. Paginas /prateleira e /fardos reescritas com KanbanBoard, ItemModal, AssignModal, dados reais via useCardData, filtragem por role, e acoes de progresso/atribuicao.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao visual e funcional end-to-end</name>
  <files>app/(authenticated)/prateleira/page.tsx, app/(authenticated)/fardos/page.tsx</files>
  <action>
    Verificacao humana do sistema completo de cards. Nenhum codigo a ser escrito — apenas conferir que tudo funciona visualmente e funcionalmente nos dois layouts (desktop e mobile) e com diferentes roles.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>Usuario confirmou que kanban, modais, atribuicao, urgencia e dados reais funcionam corretamente em desktop e mobile.</done>
  <what-built>Sistema completo de cards com kanban, modais, atribuicao, contagem regressiva e dados reais do Supabase. Inclui /prateleira e /fardos funcionais.</what-built>
  <how-to-verify>
    1. Executar migration: `npx supabase db push` (ou colar SQL no Supabase Dashboard > SQL Editor)
    2. Garantir que ha dados no banco (fazer upload de planilha via /upload se necessario)
    3. Acessar /prateleira como lider:
       - Verificar layout kanban horizontal com colunas por metodo de envio
       - Verificar que colunas estao ordenadas por prazo (SPX primeiro)
       - Verificar cores dos badges de marketplace
       - Verificar contagem regressiva/urgencia nos cards
       - Clicar em um card: modal abre com lista de itens
       - Clicar Confirmar em um item: numpad abre, digitar quantidade, confirmar
       - Clicar Atribuir: modal de selecao de separador abre
    4. Redimensionar para mobile (<768px):
       - Verificar que colunas viram secoes colapsaveis
       - Verificar que secoes urgentes ficam expandidas
       - Verificar que cards sao full-width
    5. Acessar /fardos como lider:
       - Verificar que mostra cards com fardos reservados
       - Atribuicao mostra lista de fardistas
    6. Acessar /prateleira como separador:
       - Verificar que so ve cards atribuidos a ele (ou nenhum se nao atribuido)
    7. Verificar secao CONCLUIDOS: se algum card tem 100%, deve estar na secao colapsavel no final
  </how-to-verify>
  <resume-signal>Digitar "aprovado" se tudo funciona, ou descrever problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Supabase | Queries de pedidos/progresso/atribuicoes passam pelo client Supabase |
| User role filter | Separador/fardista so deve ver cards atribuidos |
| Numpad input -> progresso UPDATE | Quantidade confirmada atualiza banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | useCardData | mitigate | Filtragem client-side por user_id para separador/fardista. RLS policy sera adicionada em Phase 6/7 para enforcement server-side |
| T-05-07 | Tampering | onConfirmQuantity | mitigate | Validacao client no NumpadPopup (0 <= qty <= necessaria). Server-side constraint na Phase 7 route handler |
| T-05-08 | Elevation of Privilege | onAssignUser | mitigate | Apenas lider/admin ve botao Atribuir (UI guard). RLS policy em atribuicoes verificara role antes de INSERT |
</threat_model>

<verification>
- `npx vitest run src/features/cards/hooks/__tests__/use-countdown.test.ts` — testes passam
- `npx tsc --noEmit` — sem erros
- `npm run test` — suite completa verde
- Checkpoint: verificacao visual mobile + desktop + funcionalidade completa
</verification>

<success_criteria>
- useCountdown atualiza contagem a cada 60s com tiers corretos
- useCardsRealtime conecta a 4 tabelas com cleanup correto
- useCardData monta CardData[] com filtragem por role
- Migration SQL habilita Realtime publication
- /prateleira mostra kanban com dados reais, modal funcional, atribuicao de separadores
- /fardos mostra kanban com dados reais, modal funcional, atribuicao de fardistas
- Checkpoint humano confirma funcionamento end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/05-cards-e-ui-foundation/05-05-SUMMARY.md`
</output>
