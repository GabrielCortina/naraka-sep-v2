---
phase: 05-cards-e-ui-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/cards/components/progress-bar.tsx
  - src/features/cards/components/urgency-badge.tsx
  - src/features/cards/components/marketplace-badge.tsx
  - src/features/cards/components/order-card.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/collapsible.tsx
  - src/components/ui/scroll-area.tsx
  - src/components/ui/progress.tsx
autonomous: true
requirements:
  - CARD-03
  - CARD-05

must_haves:
  truths:
    - "Componentes shadcn Dialog, Collapsible, ScrollArea e Progress instalados e funcionais"
    - "jspdf e jspdf-autotable instalados"
    - "ProgressBar renderiza barra de 4px com cor de urgencia"
    - "UrgencyBadge mostra 'ATRASADO' ou contagem regressiva com cor correta"
    - "MarketplaceBadge mostra nome do marketplace em uppercase com cor oficial"
    - "OrderCard compoe badge + urgencia + progresso + pecas em layout correto"
  artifacts:
    - path: "src/features/cards/components/progress-bar.tsx"
      provides: "Barra de progresso 4px com cor urgencia"
      exports: ["ProgressBar"]
    - path: "src/features/cards/components/urgency-badge.tsx"
      provides: "Badge/contagem regressiva"
      exports: ["UrgencyBadge"]
    - path: "src/features/cards/components/marketplace-badge.tsx"
      provides: "Badge colorido marketplace"
      exports: ["MarketplaceBadge"]
    - path: "src/features/cards/components/order-card.tsx"
      provides: "Card composto com todas as linhas D-10"
      exports: ["OrderCard"]
  key_links:
    - from: "src/features/cards/components/order-card.tsx"
      to: "src/features/cards/components/progress-bar.tsx"
      via: "import ProgressBar"
      pattern: "import.*ProgressBar"
    - from: "src/features/cards/components/order-card.tsx"
      to: "src/features/cards/components/urgency-badge.tsx"
      via: "import UrgencyBadge"
      pattern: "import.*UrgencyBadge"
---

<objective>
Instalar dependencias (shadcn components + jspdf) e criar os 4 componentes atomicos/compostos: ProgressBar, UrgencyBadge, MarketplaceBadge, OrderCard.

Purpose: Estes componentes sao os blocos de construcao visuais que o KanbanBoard e os modais vao consumir. Precisam estar prontos antes do layout.
Output: 4 componentes React reutilizaveis + dependencias instaladas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cards-e-ui-foundation/05-CONTEXT.md
@.planning/phases/05-cards-e-ui-foundation/05-UI-SPEC.md
@.planning/phases/05-cards-e-ui-foundation/05-RESEARCH.md

<interfaces>
<!-- Tipos e constantes criados no Plan 01 -->
<!-- Se Plan 01 nao executou ainda, estes arquivos serao criados pelo executor do Plan 01 -->

From src/features/cards/types.ts (Plan 01 creates):
```typescript
export type UrgencyTier = 'overdue' | 'warning' | 'ok' | 'done'
export type MarketplaceKey = 'Shopee SPX' | 'ML Flex' | 'ML Coleta' | 'TikTok Shop' | 'Shein' | 'Shopee Xpress'
export interface CardData {
  card_key: string
  grupo_envio: string
  tipo: string
  importacao_numero: number
  items: CardItem[]
  total_pecas: number
  pecas_separadas: number
  atribuido_a: { id: string; nome: string } | null
  urgency: UrgencyTier
}
```

From src/features/cards/lib/deadline-config.ts (Plan 01 creates):
```typescript
export const MARKETPLACE_COLORS: Record<string, { bg: string; text: string }>
export const TYPE_ABBREV: Record<string, string>
```

From src/components/ui/card.tsx (existing shadcn):
```typescript
export { Card, CardHeader, CardContent, CardFooter, CardTitle, CardDescription }
```

From src/components/ui/badge.tsx (existing shadcn):
```typescript
export { Badge, badgeVariants }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependencias shadcn e jspdf</name>
  <files>
    src/components/ui/dialog.tsx,
    src/components/ui/collapsible.tsx,
    src/components/ui/scroll-area.tsx,
    src/components/ui/progress.tsx,
    package.json
  </files>
  <read_first>
    package.json,
    src/components/ui/card.tsx
  </read_first>
  <action>
    Executar comandos em sequencia:
    1. `npx shadcn@latest add dialog collapsible scroll-area progress --yes` — adiciona 4 componentes shadcn com Radix UI
    2. `npm install jspdf jspdf-autotable` — biblioteca PDF para checklist
    3. Verificar que os 4 arquivos existem em src/components/ui/ e que package.json tem jspdf e jspdf-autotable
  </action>
  <verify>
    <automated>ls src/components/ui/dialog.tsx src/components/ui/collapsible.tsx src/components/ui/scroll-area.tsx src/components/ui/progress.tsx && grep -c "jspdf" package.json</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/dialog.tsx existe e contém "DialogContent"
    - src/components/ui/collapsible.tsx existe e contém "Collapsible"
    - src/components/ui/scroll-area.tsx existe e contém "ScrollArea"
    - src/components/ui/progress.tsx existe e contém "Progress"
    - package.json contém "jspdf" e "jspdf-autotable"
  </acceptance_criteria>
  <done>4 componentes shadcn e 2 libs PDF instalados</done>
</task>

<task type="auto">
  <name>Task 2: Componentes atomicos ProgressBar, UrgencyBadge, MarketplaceBadge e OrderCard</name>
  <files>
    src/features/cards/components/progress-bar.tsx,
    src/features/cards/components/urgency-badge.tsx,
    src/features/cards/components/marketplace-badge.tsx,
    src/features/cards/components/order-card.tsx
  </files>
  <read_first>
    src/components/ui/card.tsx,
    src/components/ui/badge.tsx,
    src/features/cards/types.ts,
    src/features/cards/lib/deadline-config.ts,
    .planning/phases/05-cards-e-ui-foundation/05-UI-SPEC.md
  </read_first>
  <action>
    NOTA: Este task depende dos tipos criados pelo Plan 01. Se executando em paralelo (Wave 1), o executor deve garantir que Plan 01 Task 1 completou primeiro (tipos e constantes existem).

    1. Criar `src/features/cards/components/progress-bar.tsx`:
       - Props: `{ percent: number; urgency: UrgencyTier }`
       - Renderiza div container h-1 (4px) w-full bg-muted rounded-full
       - Div interna com width={percent}% e cor baseada no urgency:
         - 'overdue': bg-urgency-overdue
         - 'warning': bg-urgency-warning
         - 'ok': bg-urgency-ok
         - 'done': bg-urgency-ok/40 (40% opacity)
       - Transicao suave: `transition-all duration-300`
       - Exportar `ProgressBar` como named export

    2. Criar `src/features/cards/components/urgency-badge.tsx`:
       - Props: `{ urgency: UrgencyTier; countdown: string | null }`
       - Se urgency === 'overdue': renderiza `<span className="text-urgency-overdue font-bold text-sm">ATRASADO</span>`
       - Se urgency === 'done': nao renderiza nada (return null)
       - Se urgency === 'warning': renderiza `<span className="text-urgency-warning font-bold text-sm">{countdown}</span>`
       - Se urgency === 'ok': renderiza `<span className="text-urgency-ok font-bold text-sm">{countdown}</span>`
       - Per D-37: adicionar `animate-pulse` class por 150ms quando urgency muda (usar key prop com urgency para force remount com animacao)
       - Exportar `UrgencyBadge` como named export

    3. Criar `src/features/cards/components/marketplace-badge.tsx`:
       - Props: `{ grupoEnvio: string }`
       - Importar MARKETPLACE_COLORS de deadline-config
       - Renderizar Badge (shadcn) com className do MARKETPLACE_COLORS[grupoEnvio].bg e .text
       - Texto: grupoEnvio em uppercase, font-bold, text-xs (0.75rem) per D-50
       - Fallback se grupo nao mapeado: bg-muted text-foreground
       - Exportar `MarketplaceBadge` como named export

    4. Criar `src/features/cards/components/order-card.tsx`:
       - Props: `{ card: CardData; onOpenModal: (cardKey: string) => void; onAssign: (cardKey: string) => void }`
       - Usar Card (shadcn) como base, className: "border-l-4 bg-white shadow-sm rounded-lg cursor-pointer" per D-08, D-09
       - Borda esquerda por urgency (per D-08):
         - 'overdue': border-l-urgency-overdue
         - 'warning': border-l-urgency-warning
         - 'ok': border-l-urgency-ok
         - 'done': border-l-urgency-ok/40
       - Layout interno (CardContent com padding p-3, per D-09):
         **Linha 1** (flex items-center gap-2 justify-between):
           - MarketplaceBadge com grupoEnvio
           - `<span className="text-xs font-bold text-muted-foreground">{TYPE_ABBREV[card.tipo] || card.tipo}</span>` (per D-10)
           - `<span className="text-xs font-bold text-muted-foreground">#{card.importacao_numero}</span>` (per D-51)
           - Spacer (flex-1)
           - Botao Atribuir: `<button onClick={(e) => { e.stopPropagation(); onAssign(card.card_key) }}>` com icone Lucide `User` (16px). Se card.atribuido_a: text-blue-600 + nome truncado max-w-[60px]. Se nao: text-muted-foreground (per D-10)
         **Linha 2** (mt-1):
           - UrgencyBadge com urgency e countdown
         **Linha 3** (mt-1):
           - ProgressBar com percent e urgency
         **Linha 4** (flex justify-between mt-1):
           - `<span className="text-xs font-bold">{progress.percent}%</span>`
           - `<span className="text-xs text-muted-foreground">{card.pecas_separadas}/{card.total_pecas} pecas</span>` (per D-53)
       - onClick no card inteiro: `onOpenModal(card.card_key)`
       - Exportar `OrderCard` como named export
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/features/cards/components/progress-bar.tsx contém "export function ProgressBar" e "bg-urgency-overdue" e "h-1"
    - src/features/cards/components/urgency-badge.tsx contém "export function UrgencyBadge" e "ATRASADO" e "text-urgency-overdue"
    - src/features/cards/components/marketplace-badge.tsx contém "export function MarketplaceBadge" e "MARKETPLACE_COLORS" e "uppercase"
    - src/features/cards/components/order-card.tsx contém "export function OrderCard" e "border-l-4" e "border-l-urgency-overdue" e "TYPE_ABBREV" e "pecas"
    - tsc --noEmit passa sem erros
  </acceptance_criteria>
  <done>4 componentes visuais criados: ProgressBar (4px com cor urgencia), UrgencyBadge (ATRASADO/countdown), MarketplaceBadge (cor marketplace), OrderCard (card composto com as 4 linhas per D-10)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A neste plan | Componentes de apresentacao sem acesso direto a DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | OrderCard | accept | Card recebe dados ja filtrados por role; filtragem ocorre na camada de dados (Plan 05) |
</threat_model>

<verification>
- `npx tsc --noEmit` — sem erros
- `ls src/features/cards/components/` — 4 arquivos .tsx
- `ls src/components/ui/dialog.tsx src/components/ui/collapsible.tsx` — existem
- `grep "jspdf" package.json` — presente
</verification>

<success_criteria>
- 4 componentes shadcn adicionados (Dialog, Collapsible, ScrollArea, Progress)
- jspdf e jspdf-autotable instalados
- ProgressBar, UrgencyBadge, MarketplaceBadge, OrderCard criados e compilando
- OrderCard segue exatamente o layout de 4 linhas per D-10
</success_criteria>

<output>
After completion, create `.planning/phases/05-cards-e-ui-foundation/05-02-SUMMARY.md`
</output>
